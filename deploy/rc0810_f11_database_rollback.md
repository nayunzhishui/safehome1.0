# RC0810-F11 数据库 profile 回滚

1. 停止新版本应用流量，不继续自动建表或执行迁移。
2. 确认当前数据库仍为批准的 MySQL 实例，并保存脱敏 schema/migration head。
3. 将应用镜像回滚到前一应用版本；不得回退已执行的数据迁移。
4. 使用前一版本再次执行只读 ready 检查；失败时保持流量关闭并恢复新版本应用。
5. validation SQLite 仅用于合成数据复现，不得替代或覆盖生产 MySQL。

本任务没有执行生产迁移、生产连接或数据写入。合成夹具只比较 ID、数量、状态、版本和归属。
