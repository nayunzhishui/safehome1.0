# AGENTS.md

本文件是 safehome1.0 项目的长期工作规则。所有后续开发、重构、文档更新和自动化代理任务都必须遵守本文件。

## 1. 项目名称

本项目名称包括：

- safehome1.0
- 安心陪伴
- ReadFeedback 家长情绪管理支持系统

## 2. 项目定位

本项目是一个基于 UP 跨诊断情绪调节框架的家长非评判陪伴训练系统。

系统目标不是诊断儿童、家长或家庭关系，而是帮助家长记录情绪事件、识别互动模式、获得支持性反馈，并完成可练习、可打卡、可复盘的陪伴训练。

## 3. MVP 1.0 核心闭环

MVP 1.0 必须围绕以下核心闭环推进：

目标设定 -> 情绪事件记录 -> 情绪与互动模式识别 -> 非诊断反馈 -> UP训练卡推送 -> 家长练习与打卡 -> 周度报告 -> 人工督导补充

任何新增功能都应优先服务这个闭环，不应过早扩展到与闭环无关的复杂功能。

## 4. 开发结构

项目目录按以下结构理解和维护：

- `backend`：Flask + SQLite 后端。
- `apps/miniprogram`：微信小程序端。
- `apps/web`：React/Vite 网页端。
- `content`：训练卡、反馈规则、知情同意、隐私文本。
- `docs`：项目文档、API、数据库字段、开发日志。
- `shared`：共享类型、常量、mock 数据。

开发时不要让小程序端和网页端形成两套不一致的数据结构。共享字段、状态、枚举和 mock 数据应优先放在 `shared` 或通过统一文档维护。

## 5. 当前开发原则

当前编程负责人是零基础用户。因此所有说明都必须清楚、具体，并给出可复制命令。

工作时必须遵守：

- 不要一次性大改。
- 每次只完成一个小任务。
- 改动前先说明计划。
- 改动后列出修改文件、运行命令、测试命令。
- 不要删除现有文档和内容库。
- 不要把小程序端和网页端做成两套不一致的数据结构。
- 优先保持项目可运行、可理解、可交接。
- 遇到多种实现方式时，选择最小、最稳、最容易解释的一种。

## 6. 心理学与伦理边界

所有面向家长的反馈必须满足：

- 非诊断。
- 非标签化。
- 支持性。
- 非评判。

不得写出类似以下表述：

- “孩子有焦虑症”
- “孩子是抑郁”
- “家长是控制型人格”
- “家长存在人格问题”
- “这个家庭属于病态互动”

AI 或规则反馈只能做：

- 模式提示。
- 训练建议。
- 风险提示。
- 鼓励家长观察、记录、暂停和练习。

高风险内容应提示人工督导或转介。高风险内容包括但不限于自伤、自杀、伤害他人、严重暴力、虐待、忽视、失控风险、急性精神危机等。

系统不得替代临床诊断、治疗、危机干预或法律判断。

## 7. 技术阶段

当前阶段：MVP 1.0 后端最小可运行版本。

开发优先级：

1. 优先实现 `backend`。
2. 后端跑通后再做 `apps/web` 网页端管理后台。
3. 最后做 `apps/miniprogram` 小程序端流程联调。

除非用户明确要求，不要在后端尚未跑通前投入大量精力做复杂前端页面。

## 8. 每次任务完成后的文档更新

每次任务完成后，必须更新：

- `docs/当前进度交接.md`
- `docs/开发日志.md`

更新内容应包括：

- 本次完成了什么。
- 修改了哪些文件。
- 如何运行。
- 如何测试。
- 下一步建议。

如果某次任务只修改规则文件或用户明确要求暂不更新文档，应在最终回复中说明原因。
