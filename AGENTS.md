# AGENTS.md

本文档是 `safehome1.0 / 安心陪伴 / ReadFeedback` 项目的长期 agent 工作规则。所有后续开发、重构、文档更新和自动化任务都必须遵守。

## 1. 项目定位

本项目是一个基于 UP 跨诊断情绪调节框架的家长非评判陪伴训练系统。

系统目标不是诊断儿童、家长或家庭关系，而是帮助家长：

- 记录具体情绪事件；
- 识别亲子互动模式；
- 获得非诊断、支持性、非评判反馈；
- 完成可练习、可打卡、可复盘的陪伴训练；
- 在需要时提交人工督导反馈。

## 2. MVP 1.0 核心闭环

MVP 1.0 必须围绕以下闭环推进：

目标设定 -> 情绪事件记录 -> 情绪与互动模式识别 -> 非诊断反馈 -> UP 训练卡推荐 -> 家长练习与打卡 -> 周度报告 -> 人工督导补充

任何新增功能都应优先服务这个闭环，不应过早扩展到与闭环无关的复杂功能。

## 3. 项目目录规则

目录按以下职责维护：

- `backend`：Flask + SQLite 后端。
- `apps/miniprogram`：微信小程序端。
- `apps/web`：React/Vite 网页端。
- `content`：训练卡、反馈规则、知情同意、隐私文本。
- `docs`：项目文档、API、数据库字段、开发日志、交接文档。
- `shared`：前后端共享类型、常量、mock 数据。

开发时不要让小程序端和网页端形成两套不一致的数据结构。共享字段、状态、枚举和 mock 数据应优先放在 `shared` 或通过统一文档维护。

## 4. 当前技术阶段

当前阶段：MVP 1.0 已完成以下基础能力：

- `backend` Flask + SQLite 后端 MVP 已完成；
- API 文档已与真实后端 API 对齐；
- `shared` 共用字段、常量和 mock 数据已建立；
- 网页端最小 API client 已建立；
- 小程序端 `wx.request` 请求封装已建立；
- 网页端和小程序端最小联调入口已建立；
- 小程序端最小联调已通过，已完成三步数据流：创建情绪事件记录、生成即时反馈、获取训练卡推荐。

下一阶段优先级：

1. 不再继续扩展测试页，而是开始开发 MVP 1.0 正式小程序家长端流程。
2. 第一批正式页面优先开发：
   - `pages/home/index`：首页；
   - `pages/diary-form/index`：情绪事件记录页；
   - `pages/feedback-result/index`：即时反馈页；
   - `pages/training-card/index`：UP训练卡推荐页。
3. 保留 `pages/integration-test/index` 作为长期联调测试页，不要删除。
4. 小程序正式核心流程跑通后，再开发网页端最小管理后台：
   - 情绪记录列表；
   - 情绪记录详情；
   - 数据导出入口。
5. 暂缓登录注册、复杂AI、周报正式页、人工督导正式页、社群、多媒体上传和正式部署。

除非用户明确要求，不要继续扩展联调测试页，也不要在正式小程序核心流程跑通前投入大量复杂网页后台开发。

## 5. 开发原则

当前用户是零基础到初级阶段，因此所有说明都必须清晰、具体，并尽量给出可复制命令。

工作时必须遵守：

- 不要一次性大改。
- 每次只完成一个清晰的小任务。
- 改动前先说明计划。
- 改动后列出修改文件、运行命令、测试命令和下一步建议。
- 不要删除现有文档和内容库，除非用户明确要求。
- 不要把小程序端和网页端做成两套不一致的数据结构。
- 优先保持项目可运行、可理解、可交接。
- 遇到多种实现方式时，选择最小、最稳、最容易解释的一种。

## 6. 功能增减与迭代规则

本项目允许后续添加、减少或暂缓功能，但必须遵守以下规则。

### 6.1 核心闭环优先

任何功能增减都不得破坏 MVP 1.0 的核心闭环：

目标设定 -> 情绪事件记录 -> 情绪与互动模式识别 -> 非诊断反馈 -> UP训练卡推荐 -> 家长练习与打卡 -> 周度报告 -> 人工督导补充

当前最小核心链路是：

情绪事件记录 -> 非诊断反馈 -> UP训练卡推荐

如果新增功能不能服务这条主线，应暂缓。

### 6.2 优先通过 content 扩展，不优先改代码

后续如果只是新增训练卡、反馈规则、伦理文案或说明文本，优先修改：

- `content/training_cards.json`
- `content/feedback_rules.json`
- `content/consent.md`
- `content/privacy.md`

不要为了简单内容扩展而优先改后端、数据库或前端页面。

### 6.3 普通功能增减难度分级

低风险、优先支持的变更：

- 新增训练卡；
- 新增反馈规则；
- 调整非诊断反馈文案；
- 新增小程序普通展示页面；
- 新增网页端普通查看页面；
- 隐藏非核心页面入口。

中等风险、需要先分析的变更：

- 新增练习打卡正式页；
- 新增周度报告页；
- 新增网页后台记录列表和详情；
- 新增导出字段；
- 新增训练卡管理或反馈规则管理页面。

高风险、必须后置的变更：

- 登录注册；
- 微信授权登录；
- 复杂权限系统；
- 真实AI分析；
- 危机风险识别和转介流程；
- 语音、图片、视频上传；
- 云数据库迁移；
- 正式部署和公网访问；
- 任何可能涉及临床诊断、危机干预或未成年人隐私风险的功能。

### 6.4 添加功能前必须先分析

新增任何功能前，必须先回答：

1. 该功能是否属于 MVP 1.0 核心闭环；
2. 是否需要修改数据库；
3. 是否需要新增或修改 API；
4. 是否需要修改 `shared/types` 或 `shared/constants`；
5. 是否影响小程序端和网页端的一致性；
6. 是否影响 `content/training_cards.json` 或 `feedback_rules.json`；
7. 是否存在心理学与伦理风险；
8. 最小实现方案是什么；
9. 如何测试；
10. 如何回滚。

在用户确认前，不要直接写代码。

### 6.5 减少或暂缓功能时优先隐藏入口

如果需要减少功能，不要直接删除后端 API、数据库字段或内容库。

优先采用：

- 隐藏页面入口；
- 在文档中标记为“暂缓”；
- 保留后端接口；
- 保留数据库字段；
- 保留测试页；
- 确认核心链路仍能跑通。

尤其不要随意删除：

- `POST /api/diaries`
- `POST /api/feedback/generate`
- `GET /api/cards/recommend`
- `pages/integration-test/index`

这些属于核心测试和核心链路，不应轻易删除。

### 6.6 字段变更必须谨慎

数据库字段、API字段和 shared 类型字段不要轻易删除或重命名。

如果确实要改字段，必须同步修改：

- `docs/API接口文档.md`
- `docs/数据库字段说明.md`
- `shared/types/api.ts`
- `shared/constants/api.ts`
- `apps/miniprogram` 相关页面和 services
- `apps/web` 相关页面和 services
- `backend` routes/services/models
- mock 数据和测试数据
- `docs/当前进度交接.md`、`docs/开发日志.md`、`docs/开发说明.md`

### 6.7 保留测试页和联调入口

`pages/integration-test/index` 是长期保留的开发测试页。

它用于验证：

- 小程序端是否能请求后端；
- 情绪事件记录是否能创建；
- 即时反馈是否能生成；
- 训练卡推荐是否能返回。

正式页面开发时，不要删除该测试页。

### 6.8 每次功能变更后必须验证

每次新增、减少或暂缓功能后，至少验证：

1. 后端 `/healthz` 正常；
2. 小程序端核心链路正常；
3. 网页端如果涉及相关页面，也要能正常打开；
4. 不出现诊断化、标签化、责备性反馈文案；
5. `git status` 中没有误提交 `node_modules`、`dist`、`db`、`venv`、`pycache` 等本地运行产物。

## 7. 心理学与伦理边界

所有面向家长的反馈必须满足：

- 非诊断；
- 非标签化；
- 支持性；
- 非评判；
- 聚焦具体行为、情绪、场景和可练习动作。

不得写出类似以下表达：

- “孩子有焦虑症”；
- “孩子是抑郁”；
- “家长是控制型人格”；
- “家长存在人格问题”；
- “这个家庭属于病态互动”。

AI 或规则反馈只能做：

- 模式提示；
- 训练建议；
- 风险提示；
- 鼓励家长观察、记录、暂停和练习。

高风险内容应提示人工督导或转介。高风险内容包括但不限于自伤、自杀、伤害他人、严重暴力、虐待、忽视、失控风险、急性精神危机等。

系统不得替代临床诊断、治疗、危机干预或法律判断。

## 8. 每次任务结束后的强制文档更新

每次任务结束前，必须更新以下三个文件：

- `docs/开发日志.md`
- `docs/当前进度交接.md`
- `docs/开发说明.md`

更新内容必须包括：

1. 本轮完成了什么；
2. 新增或修改了哪些文件；
3. 当前能运行到哪一步；
4. 已完成哪些测试或验证；
5. 已知问题；
6. 下一轮新对话应该从哪里继续；
7. 下一轮 Codex 对话的启动提示词。

`docs/开发说明.md` 面向零基础开发负责人，必须使用简单明了的语言。每次开发结束后，都要在该文档中补充：

1. 本次开发使用了哪些技术；
2. 本次实现了哪些功能；
3. 零基础用户应该如何理解这些技术；
4. 后续应该按什么路线继续学习。

如果某次任务只做了非常小的问答、没有文件改动，仍应在最终回复中说明“本轮未更新开发日志、交接文档和开发说明的原因”。如果已经进行了代码、文档、配置或内容库改动，则必须更新这三份文档。

## 9. Git 规则

提交前应先查看：

```powershell
git status
git diff --name-only
```

不要提交本地运行产物，例如：

- `*.sqlite3`
- `*.db`
- `__pycache__/`
- `.venv/`
- `node_modules/`

提交信息应简洁说明本次任务，例如：

```powershell
git add backend content docs AGENTS.md
git commit -m "实现 backend MVP 并更新交接文档"
git push
```
