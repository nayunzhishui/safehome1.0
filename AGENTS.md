# AGENTS.md

本文档是 `safehome1.0 / 安心陪伴 / ReadFeedback` 项目的长期 agent 工作规则。所有后续开发、重构、文档更新和自动化任务都必须遵守。

## 1. 项目定位

本项目是一个基于 UP 跨诊断情绪调节框架的家长非评判陪伴训练系统。

系统目标不是诊断儿童、家长或家庭关系，而是帮助家长：

- 记录具体情绪事件；
- 识别亲子互动模式；
- 获得非诊断、支持性、非评判反馈；
- 完成可练习、可打卡、可复盘的陪伴训练；
- 在需要时提交人工督导反馈。

## 2. MVP 1.0 核心闭环

MVP 1.0 必须围绕以下闭环推进：

目标设定 -> 情绪事件记录 -> 情绪与互动模式识别 -> 非诊断反馈 -> UP 训练卡推荐 -> 家长练习与打卡 -> 周度报告 -> 人工督导补充

任何新增功能都应优先服务这个闭环，不应过早扩展到与闭环无关的复杂功能。

## 3. 项目目录规则

目录按以下职责维护：

- `backend`：Flask + SQLite 后端。
- `apps/miniprogram`：微信小程序端。
- `apps/web`：React/Vite 网页端。
- `content`：训练卡、反馈规则、知情同意、隐私文本。
- `docs`：项目文档、API、数据库字段、开发日志、交接文档。
- `shared`：前后端共享类型、常量、mock 数据。

开发时不要让小程序端和网页端形成两套不一致的数据结构。共享字段、状态、枚举和 mock 数据应优先放在 `shared` 或通过统一文档维护。

## 4. 当前技术阶段

当前阶段：backend Flask + SQLite MVP 1.0 已完成最小可运行版本。

下一阶段优先级：

1. 核对并更新 API 文档，使文档与实际后端一致。
2. 补齐 `shared` 中的共用字段、常量和 mock 数据。
3. 建立 `apps/web` 的最小 API 调用层。
4. 建立 `apps/miniprogram` 的最小请求封装。
5. 再进入小程序端和网页端页面开发。

除非用户明确要求，不要在后端 API 和共享数据结构稳定前投入大量复杂前端页面开发。

## 5. 开发原则

当前用户是零基础到初级阶段，因此所有说明都必须清晰、具体，并尽量给出可复制命令。

工作时必须遵守：

- 不要一次性大改。
- 每次只完成一个清晰的小任务。
- 改动前先说明计划。
- 改动后列出修改文件、运行命令、测试命令和下一步建议。
- 不要删除现有文档和内容库，除非用户明确要求。
- 不要把小程序端和网页端做成两套不一致的数据结构。
- 优先保持项目可运行、可理解、可交接。
- 遇到多种实现方式时，选择最小、最稳、最容易解释的一种。

## 6. 心理学与伦理边界

所有面向家长的反馈必须满足：

- 非诊断；
- 非标签化；
- 支持性；
- 非评判；
- 聚焦具体行为、情绪、场景和可练习动作。

不得写出类似以下表达：

- “孩子有焦虑症”；
- “孩子是抑郁”；
- “家长是控制型人格”；
- “家长存在人格问题”；
- “这个家庭属于病态互动”。

AI 或规则反馈只能做：

- 模式提示；
- 训练建议；
- 风险提示；
- 鼓励家长观察、记录、暂停和练习。

高风险内容应提示人工督导或转介。高风险内容包括但不限于自伤、自杀、伤害他人、严重暴力、虐待、忽视、失控风险、急性精神危机等。

系统不得替代临床诊断、治疗、危机干预或法律判断。

## 7. 每次任务结束后的强制文档更新

每次任务结束前，必须更新以下三个文件：

- `docs/开发日志.md`
- `docs/当前进度交接.md`
- `docs/开发说明.md`

更新内容必须包括：

1. 本轮完成了什么；
2. 新增或修改了哪些文件；
3. 当前能运行到哪一步；
4. 已完成哪些测试或验证；
5. 已知问题；
6. 下一轮新对话应该从哪里继续；
7. 下一轮 Codex 对话的启动提示词。

`docs/开发说明.md` 面向零基础开发负责人，必须使用简单明了的语言。每次开发结束后，都要在该文档中补充：

1. 本次开发使用了哪些技术；
2. 本次实现了哪些功能；
3. 零基础用户应该如何理解这些技术；
4. 后续应该按什么路线继续学习。

如果某次任务只做了非常小的问答、没有文件改动，仍应在最终回复中说明“本轮未更新开发日志、交接文档和开发说明的原因”。如果已经进行了代码、文档、配置或内容库改动，则必须更新这三份文档。

## 8. Git 规则

提交前应先查看：

```powershell
git status
git diff --name-only
```

不要提交本地运行产物，例如：

- `*.sqlite3`
- `*.db`
- `__pycache__/`
- `.venv/`
- `node_modules/`

提交信息应简洁说明本次任务，例如：

```powershell
git add backend content docs AGENTS.md
git commit -m "实现 backend MVP 并更新交接文档"
git push
```
