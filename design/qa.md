# 任务二十首批设计 QA

## 2026-07-23 T36-F06 QA 增量

- PASS：Web 与小程序均使用“草稿—预览—确认—发送”同一交付心智模型。
- PASS：预览、确认、发送各自为独立请求，按钮文案描述动作结果，不再出现一键保存并发送。
- PASS：预览卡展示最终正文、不可变版本和非诊断边界；发送回执展示版本、状态和时间。
- PASS：参与者消息列表展示版本，撤回后明确提示忽略旧版本，历史记录不被删除。
- PASS：移动端交付节点可在窄屏内四等分；主操作最小高度沿用 88rpx。
- PASS：Web 生产构建通过，React/TypeScript 无编译错误。
- BLOCKED：Chrome 扩展控制运行时返回“failed to write kernel assets”，因此未取得本轮桌面/窄屏截图。
- MANUAL：微信开发者工具、iOS/Android 真机、系统大字体与读屏验收仍需负责人生成证据，不自动签字。

prototype: `http://127.0.0.1:5173/dashboard`
source: `design/context.md`、`design/audit.md`、任务二十计划、问题截图

## 视觉

- PASS：1440px 下研究工作台保持“矩阵—单人档案”主从结构，卡片、间距、圆角和低饱和绿与既有设计系统一致。
- PASS：430px 下无横向溢出，侧栏默认收起为“打开导航”，避免完整后台导航占满首屏。
- PASS：参与者详情的指标和值在卡片中居中；模块按测评、日记、训练、项目、关系试点、人工支持、消息分区。
- PASS：页面未暴露 `pending_review`、`one_open_question` 等内部状态键。
- PASS：空数据使用说明性空态，不以“异常”“失败”暗示参与者状态。

## 行为

- PASS：管理员登录后输入受控后台令牌，可读取参与者矩阵并打开单人档案。
- PASS：用户 ID/昵称检索入口、参与者选择、档案切换可操作。
- PASS：430px 后台导航可展开和收起，焦点目标为原生按钮。
- PASS：研究者档案原始填写只读，反馈和备注通过独立路径保存。

## 需求符合性

- PASS：研究者工作台已覆盖任务二十要求的主要数据模块。
- PASS：研究者仅看分配对象、督导/管理员看全体的服务端范围控制已实现并有测试。
- PASS：阶段性反馈使用“观察—依据—下一小步—待讨论问题”，发送后走版本化报告和消息链路。
- PASS：通用成长仪表盘将 1—10 温度、不同测评量尺和次数指标分开。
- 待外部验收：微信开发者工具、iOS/Android 真机、小程序空/错/成功态截图。
- 待后续批次：反馈规则全量内容复核、情绪事件可选区折叠、网站其余后台页面逐页视觉收口。

## 可访问性

- PASS：研究工作台搜索、参与者选择和移动导航均使用原生表单/按钮。
- PASS：430px 与 1440px 无横向页面溢出。
- 手工待验收：读屏顺序、系统字体放大、Windows 高对比模式、微信端读屏。

## 自动化证据

- 1440px、430px Playwright：参与者矩阵可见、日记原文可见、内部状态无泄漏、无横向溢出。
- 截图：`.codex_tmp/task20-research-dashboard-desktop.png`、`.codex_tmp/task20-research-dashboard-narrow.png`。
- 初次后台加载在令牌录入前会产生预期 401；录入令牌并刷新后通过。另有浏览器图标 404，不影响业务。

## 结论

首批 Web 研究工作台为 READY；小程序相关视觉改动仍需微信开发者工具与真机验收，任务二十整体尚未封版。

## 2026-07-17 剩余页面收口复测

- PASS：42 张训练卡完成逐卡复核；唯一必要修订是把“命名孩子情绪”改为“试探性核对孩子可能的感受”。
- PASS：训练卡、规则、反馈、督导、打卡、内容审核和研究工作台不再把审核、风险、释放策略或质量状态的内部键作为主文案展示。
- PASS：Playwright 覆盖 17 个后台路由，430px 与 1440px 均无横向溢出；430px 所有可见按钮最小高度为 44px。
- PASS：`/content/review` 的长量表标题、课程摘要和审核卡片在窄屏可换行，不再撑宽页面。
- PASS：Web TypeScript、Vite build、内容校验及 294 个后端测试通过。
- 外部待验收：本机未发现微信开发者工具 CLI；Android/iOS 真机、系统字体放大、真实双账号、订阅消息模板与 CloudBase 发布未执行。

结论：任务二十本地自动化范围 READY；发布结论必须等待外部验收。

## 2026-07-24 T36-F12 身份生命周期设计 QA

- PASS：小程序“我的”页只显示登录方式是否已关联，不显示OpenID、手机号摘要或匿名标识，降低隐私暴露和认知负担。
- PASS：撤销动作使用独立确认弹窗并明确“退出所有设备但保留记录”；最后一种登录方式在服务端阻断。
- PASS：复杂账号合并没有塞入参与者端，只在admin Web安全工作台按候选、确认、执行、核对、撤销逐步呈现。
- PASS：Web与小程序沿用暖白、低饱和绿、圆角卡片和既有按钮层级；新增动作保持清晰主次，不使用诊断或责备文案。
- PASS：Web typecheck/production build、小程序40页结构审计和JS语法通过。
- PASS：Chrome实测`/security/privacy`桌面1440px与窄屏430px；两种视口均无横向溢出，合并工作台标题、说明和逐步操作可见，窄屏导航正确折叠。
- MANUAL：微信开发者工具、Android/iOS、大字体、读屏、真实合并双账号和CloudBase发布后视觉/行为验收待负责人完成，不自动签字。

结论：F12 Web代码、构建与Chrome双视口设计QA为READY；微信和真机视觉验收仍是外部门禁。
# 2026-07-25 T36-F13 在线分析工作台

- 设计上下文：延续米白背景、低饱和森林绿、圆角卡片和低认知负担；在线分析固定研究者影子模式，不使用诊断式图标或结论色。
- 信息层级：先展示待处理/聚合结果/冻结终止三个摘要，再按状态查看任务；结果只显示覆盖率、未知率、样本量和质量状态。
- 跨端边界：Web承担任务队列和质量查看；小程序仅加入只读“在线分析”标签，不在移动端暴露执行器、恢复或删除。
- 自动验收：Chrome 1440px与430px均无横向溢出；窄屏摘要改为单列，触控按钮保持完整；移除继承的误导性“+”装饰。
- 待人工：真实研究者任务密度、长版本号、读屏和微信开发者工具/Android/iOS仍需外部门禁证据，当前不作发布批准。
## 2026-07-26 T36-F15 AI问答沙盒

- 信息层级：先展示参与者关闭与治理门禁，再进入合成会话、回答/来源/不确定性、固定评测和人工复核。
- 内容边界：回答区显式展示不确定性与非诊断、非治疗、非正式反馈说明。
- 交互：Chrome本地隔离账号验证页面可渲染、合成会话可创建、问答可执行、来源及不确定性区域可见。
- 兼容：前端对旧服务尚未返回provider_policy时显示“等待新版本服务”，避免部署版本错位导致白屏。
