# 反馈核对页代码复现审查

## 实现范围

- 在共享 `therapeutic-flow-step` 的 `mode="feedback"` 中加入细批注脊，并将动态标签、标题和正文置于同一开放式正文区。
- 删除反馈正文下方与页头语义重复的固定说明。
- 四个选项、选中值、仅“不像”出现的文本域、动态保存状态、返回与保存操作继续使用原绑定和事件。

未修改页面 JS、共享组件 JS、participant flow、API、字段、版本、幂等、草稿、安全门禁或路由。

## 小字门禁

- 保留：四个选项的操作后果、动态保存状态、条件输入提示、离线/错误/空状态说明。
- 删除：固定句“这是一份可讨论、可不同意、可修订的阶段性理解”，因为页头已说明相同边界。
- 未新增：装饰性标签、图标说明、模型能力宣传、机器字段、版本号、ISO 时间或重复免责声明。
- 最小辅助字号为 `24rpx`；关键反馈正文、选项和错误恢复均不使用 Caption。

## Loop 1–4

1. 视觉：ImageGen 的留白批注纸、Figma 组件和共享 feedback 模式一致。
2. UI：批注脊轻量；连续中文正文与四个等权选项均可扫读。
3. UX：不同意和需要时间不是错误；只有“不像”增加必要输入负担。
4. 状态：默认、四种识别、必填补充、读取、空反馈、保存、离线、错误和安全暂停完整。

## Harness

- 组件：复用共享 flow、Button、PageState、ChoiceOption 和 Textarea；没有页面级重复组件。
- 业务：最近已发送反馈、识别值、不同意见说明和提交语义保持完整。
- 工程：只修改共享 UI 模板与样式，不触碰后端、数据库、API、content、shared 或业务 JS。

结论：Preview 与全量 Harness 已通过，本页本地完成；真机统一验收延期。
