# 首页代码复现与 Loop 审查

状态：`local_loops_passed_device_visual_pending`
日期：2026-08-10
基准：Figma `Screen/Home/Default`（`30:2`）及其五种状态。

## 实现范围

- 首页按“顶栏 → 情绪温度计 → 测一测/情绪日记 → 今天的一小步 → 如何开始 → 更多 → 最近记录 → 阶段性反馈”原顺序复现。
- “情绪天气”纠正为真实“情绪温度计”，继续使用现有当天温度记录和 `pages/thermometer/index`。
- “如何开始”收束为单行入口；支持性反馈继续进入已有记录生成流程。
- 最近记录有数据时进入 `pages/diary-history/index`，空状态仍进入日记表单。
- 阶段性反馈继续展示真实汇总、计数、场景、情绪、下一步和非诊断边界。
- `journey-action-card` 仅增加可继承的局部视觉变量，关系试点页的默认外观与业务事件未改变。

## Loop

- 视觉：开放式排版、细分隔、深绿唯一主行动与陶土橙按钮已对齐 Figma。
- UI：正文不低于 24rpx，主要触控目标不低于 88rpx；消息红点继续由真实未读数量控制。
- UX：主任务唯一；记录、反馈、训练和人工支持路径保持原语义。
- 状态：今天的一小步八种业务状态保留；最近记录和阶段反馈补齐 Loading、Empty、Error/弱网恢复。
- 长内容：标题和摘要使用单行或两行截断，完整内容仍在目标页查看。

## 验证

- `node --check`：通过。
- 设计令牌、UI governance、非 UI client audit：通过。
- 微信开发者工具 `preview`：通过，AppID `wxd548597e78862269`，包体约 1.4 MB。
- 自动模拟器截图：未完成；桌面控制运行时不可用。
- Android/iOS 真机、大字体、读屏：待人工证据，不标记通过。
