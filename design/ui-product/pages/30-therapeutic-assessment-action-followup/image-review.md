# 行动回看页 ImageGen 审查

- 选定图：`assets/imagegen-selected-v1.png`

## 可继承

- 先展示原行动、目的和停止条件，再进入本次状态与回看，任务顺序正确。
- “中途停止”和“仍待了解”使用普通选中态，没有失败或警告暗示。
- 回看输入是页面主体；训练卡仅作为可选次级操作。
- 页头边界只出现一次，没有页尾重复说明。

## Figma 修正

- 使用 Noto Sans SC，不沿用概念图的衬线大标题。
- 去掉训练卡按钮的装饰图标；按钮本身仅在 `training_card_id` 存在时显示。
- 原行动区改为开放式摘录，降低完整卡片外框的存在感。
- “原计划—本次回看”使用细线、节点与短竹节，不做大号时间轴插画。
- 手机窄屏状态按钮允许自然换行或单列，不缩小文字。
- 保留动态错误与保存 loading，不增加草稿状态、计数或页面脚注。

结论：构图与功能通过，按上述修正进入 Figma。
