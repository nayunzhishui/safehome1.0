# 最近一次事件页 ImageGen 审查

采用：`assets/imagegen-selected-v1.png`

- 通过：单一具体片段输入，未生成多字段、标签、量尺、时间轴或 AI 分析。
- Figma 校正：标题缩回 H1；进度采用数字在左的共享顺序；主按钮使用纯色 action token；第 3 步删除已在第 1 步确认过的重复边界小字。
- 继续复用 `TherapeuticTextarea`，补 Default、DraftRestored、Saving、Offline、LoadError、SafetyPaused、LongContent。
- 不展示 evidence kind、scope、幂等键、接口字段或内部机器状态。

结论：方向通过，经共享设计系统校正后进入 Figma。
