# 个性化训练方案页 ImageGen 审查

采用稿：`assets/imagegen-default-v1.png`

结论：`pass_with_figma_corrections`

- 通过：练习节奏、提醒权限、推荐练习和独立状态矩阵的层级合理。
- 纠正：目标输入真实上限是 200，不采用概念图 `0/20`。
- 纠正：阶段、频率、日期、状态、到期摘要和推荐内容全部使用真实动态数据，不写死示例。
- 纠正：提醒只保留现有“暂未开放/已同意/已使用/已关闭/需要时再开启”及对应真实按钮。
- 纠正：无匹配状态没有“调整练习节奏”事件，不绘制不存在的按钮。
- 纠正：推荐卡不引入插画或新字段，继续使用真实标题、时长和“开始”。
- 字体统一为 `Noto Sans SC`，减少概念图的嵌套卡片和小字。
