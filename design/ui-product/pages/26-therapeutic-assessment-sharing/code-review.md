# 资料与共享页代码复现审查

## 实现范围

本页直接使用已完成并编译通过的 `therapeutic-flow-step` 选择模式：进度、三项单选、动态保存状态、返回、保存和 PageState 均已真实实现，无需增加页面级 WXML/WXSS 或硬编码文案。

未修改页面 JS、共享组件 JS、flow utility、evidence、API、scope、版本、幂等、草稿或路由。

## 小字门禁

- 保留：三条选择后果说明与动态保存状态。
- 删除/禁止：装饰性解释、重复边界、接口字段释义、虚构权限说明和无操作价值的提示。

## Loop 1–4

1. 视觉：ImageGen、Figma 与共享选择页面一致。
2. UI：单一问题、三项选择和一个主操作，信息密度克制。
3. UX：每项说明直接说明真实共享后果，避免误选。
4. 状态：默认、三种选中、保存、离线、错误、安全暂停和长内容完整。

## Harness

- 组件：只复用现有 ChoiceOption、Button、PageState，没有为复用堆叠内容。
- 业务：共享范围映射、版本、幂等、草稿、恢复和安全语义完整。
- 工程：无页面级代码增量，不触碰后端、数据库、content、shared 或业务 JS。

结论：待 Preview 与全量 Harness 通过后完成；真机统一验收延期。
