# 项目详情页 ImageGen 审查

采用稿：`assets/imagegen-default-v1.png`

结论：`pass_with_figma_corrections`

- 通过：开放式长页、真实小节页签、编号步骤、书写/反思、两个 slider、两个 checkbox、单一正式提交与本人记录的层级清楚。
- 删除：视觉稿中的 `SafeHome / Scheme A` 展示字样、装饰性装订孔和示例植物；这些不是页面业务内容。
- 纠正：项目标题、版本、构念、受众、条件、步骤和问题全部使用真实绑定，不采用图中示例值。
- 纠正：`measurementPlan` 是独立的开始前/练习中/完成后记录节奏，不能与 `sessions` 小节页签合并。
- 纠正：不使用“筛查”“研究类型”等新增措辞；负面体验继续使用现有 checkbox 原文，不推断风险或诊断。
- 纠正：Default participant 与 Preview read-only 分开设计；Preview 中真实隐藏填写、保存、slider、checkbox 与提交，而不是只加标签。
- 纠正：保留 Loading、Load error、Missing、Submitting、Submit success、Submit error、Long content 状态。
- 保留：手帐页签是该页唯一视觉记忆点；不重复粗侧线、不把每个章节做成卡片。
