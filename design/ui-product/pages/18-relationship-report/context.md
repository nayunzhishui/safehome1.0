# 关系阶段性报告页设计上下文

## UIproduct2 冻结（2026-09-08）

### 本轮实施增量

已实现：Figma458:396（已交付合成样例）、462:407（待交付）、465:407（保存假设）。正文30rpx，独立阶段解释、共同核对、维度、讨论与导出。复用反馈组件的真实editorial布尔属性；假设按钮disabled映射既有保存锁。JS仅增加日期展示字段与导出配色，不改请求、报告对象或访问条件。

小字预算：仅日期、版本、草稿状态、字符计数和简短状态标签使用26rpx；任务、边界和错误说明为正文，不缩小以塞入。全部页面统一检查/真机验收待完成。

待人工交付与已交付报告严格分开；已交付先阶段解释、再共同核对和依据，原始数据/假设/动态分层；待交付不画空大卡。保留全部风险、导出与反馈动作。

清朗青瓷主题，正文30rpx、元信息26rpx、触控96rpx，先Figma后代码，不改API/后端，最终全页检查待完成。


版本：2026-08-11 冻结版

## Goal

让参与者先看懂报告处于什么阶段、当前阶段性解释是什么，再按需核对数据与假设；避免把参考位置呈现为诊断、能力排名或固定关系标签。

## 硬约束

- 保留 `getRelationshipReport`、人工核对未发送分支、四步状态轨迹与注意提示。
- 保留 `feedback-rating`、机制假设三选一核对、动态记录、讨论问题、项目任务与脱敏长图导出。
- 保留保存假设反馈、报告评价与导出埋点的全部事件、字段和状态。
- 不修改 JS、API、后端、数据库、报告状态或研究流程语义。
- 不新增关系评分、匹配度、能力等级、人格标签、自动诊断、分享排名或虚构维度。

## Frozen direction

方案 A“研究手记报告”：开放页头与状态轨迹 → 阶段性解释 → 用户核对 → 数据轮廓 → 矛盾与假设 → 连续记录 → 讨论与任务 → 边界和脱敏导出。

## 状态矩阵

- Loading；LoadError；DeliveryPending；Delivered；AttentionNotice；SavingFeedback；Exporting；LongContent。
