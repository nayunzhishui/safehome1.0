# 关系探索任务页 Figma 审查

- 页面：`19 Relationship Task`（`219:2`）
- 绘画态：`219:3`，截图 `assets/figma-drawing-v2.png`
- 句子补全态：`220:8`，截图 `assets/figma-sentence-v2.png`
- 状态矩阵：`220:1600`，截图 `assets/figma-states-v1.png`

## 审查结论

- 两个真实 `taskType` 分屏复现，未将绘画与句子补全混合为一个表单。
- 绘画态覆盖画布、撤销/重做/清空、画外音、计数、授权、边界和提交；画布示例只是不具象线条。
- 句子态严格使用七个现有情境，默认只展开第一题；已填写提示留给真实 `answered` 状态。
- 草稿恢复、保存中、保存失败、提交中、校验 toast、风险转人工与长内容均进入状态矩阵。
- 仅复用现有 Primary Button；画布、工具、输入与折叠题保持页面级结构，未过度组件化。
- 68 个文本节点仅使用 Noto Sans SC Regular/Medium；0 个未绑定实色填充或描边；无绘画增强、AI 解读、进度分、分享或云草稿。

结论：通过，可进入前端复现。
