# 研究者移动工作台需求冻结

1. 居中标题“研究者工作台”，标题下无说明小字；移动端职责说明独立成信息条。
2. 最近同步改为标题外的轻量操作行。
3. 横向工作区导航保持真实能力项，但统一为可读标签，降低胶囊感和溢出感。
4. 待处理默认态先显示三个关键摘要，再显示队列；系统运行状态压缩为开放摘要行。
5. 列表和详情保留真实元数据，但正文/必要辅助文字不小于 24rpx；删除重复边界，不删法律、授权和交付后果。
6. 侧边粗线改为贴合模块标题的短标记或顶部状态线，不贯穿卡片。
7. 不修改 JS、API、数据库、权限与业务语义。
