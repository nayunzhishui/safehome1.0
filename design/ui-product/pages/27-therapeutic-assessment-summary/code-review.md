# 提交前摘要页代码复现审查

## 实现范围

- 在共享 `therapeutic-flow-step` 的 `mode="summary"` 中实现纵向双稿对照。
- 原话、系统整理、空值提示、动态保存状态、返回与继续仍使用原绑定和事件。
- 对照脊由纯 WXML/WXSS 结构实现，可随长文本自然增高。

未修改页面 JS、共享组件 JS、participant flow、API、字段、版本、幂等、草稿、安全门禁或路由。

## 小字门禁

- 保留：原话不会被覆盖的操作边界、动态保存状态、缺失内容提示和错误恢复说明。
- 未新增：装饰性解释、系统能力宣传、机器字段翻译、重复免责声明或显而易见的界面复述。

## Loop 1–4

1. 视觉：ImageGen 的双稿书页、Figma 组件与共享 summary 代码一致。
2. UI：中文正文纵向阅读；对照脊轻量，不与内容争夺注意力。
3. UX：两版等权展示，主操作仍明确为“保留原话并继续”。
4. 状态：默认、长内容、缺失版本、读取、保存、离线、错误和安全暂停完整。

## Harness

- 组件：复用共享 flow 组件，不增加页面级重复 WXML/WXSS。
- 业务：原话保护、动态字段和流程语义完整。
- 工程：只修改 UI 模板与样式，不触碰后端、数据库、content、shared 或业务 JS。

结论：待 Preview 与全量 Harness 通过后完成；真机统一验收延期。
