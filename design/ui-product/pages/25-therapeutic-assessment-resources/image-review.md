# 例外与资源页 ImageGen 审查

采用：`assets/imagegen-selected-v1.png`

- 通过：单一资源回想输入，没有人物/做法/环境卡片、标签、推荐或 AI 分析；重复底部边界已删除。
- Figma 校正：标题缩回 H1；进度与前页共用数字在左骨架；主按钮使用纯色 token；草稿状态靠近输入区并保持动态职责。
- 复用 `TherapeuticTextarea`，覆盖 Default、DraftRestored、Saving、Offline、LoadError、SafetyPaused、LongContent。
- 不展示 evidence kind、scope、来源或机器字段。

结论：方向通过，进入 Figma。
