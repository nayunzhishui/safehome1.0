# 登录页代码复现与 Loop / Harness 审查

状态：`local_loops_passed_device_review_deferred`
日期：2026-08-10
基准：Figma `Login/Default`（`61:803`）及 Loading、Unavailable、Error、LongContent、MustChangePassword 五种状态。

## 实现范围

- 仅修改 `pages/login/index.wxml` 与 `index.wxss`，没有修改 JS、接口、认证判断、数据绑定或页面跳转。
- 移除外层通用卡片，改为开放式编辑排版；保留 SafeHome、登录说明、快捷登录、账号密码和注册入口的真实顺序。
- 微信、手机号、账号密码和首次强制改密仍完全复用原事件与状态条件。
- 原隐私说明、能力不可用提示、加载和错误文案继续由真实数据控制；未新增验证码、找回密码、游客登录或新认证路径。
- 因页面继续使用原生导航栏，Figma 顶部间距在代码中按小程序导航栏后的内容区适配。

## Loop

- 视觉：开放式留白、深绿主行动、细分隔和 16px 圆角与 Figma 对齐；无插画、渐变和装饰卡片。
- UI：主要按钮 88rpx，输入框 104rpx；正文不低于 24rpx。白字主按钮对比度约 4.74:1，辅助文字约 4.62:1，错误文字局部加深后约 4.53:1。
- UX：登录方式按“快捷登录 → 账号密码”分组；手机号隐私说明紧邻对应入口；错误通过文字和颜色共同表达。
- 状态：Default、Loading、Unavailable、Error、LongContent、MustChangePassword 均映射到已有代码状态；注册入口在加载期间仍保持原交互语义。

## Harness

- 视觉：WXML/WXSS 尺寸、层级、颜色和间距逐项对照 Figma；本轮无法通过桌面控制抓取模拟器截图，因此不把像素级实机一致性标记为通过。
- 组件：登录页复用全局色彩令牌和原生按钮/输入框；无新增依赖、无重复 JS 组件。
- UX：持久标签、状态文本、`aria-live`、不小于 44px 的点击目标均保留。读屏、200% 大字体和焦点播报留待最终批次人工验收。
- 工程：微信开发者工具 `preview` 编译通过，包体约 1.4 MB；未出现 WXSS 编译错误，未改后端、API、数据库、共享类型或业务逻辑。

## 验证

- `node --check`、页面 JSON 解析、资源校验、53 页前端审计：通过。
- T23 视觉系统 375 / 430 / 768 / 1440 与 100% / 200% 矩阵：通过。
- T33 触控、对比度、焦点、表单关联、溢出和动效检查：通过；仅保留项目级 `miniprogram_page_inventory_drift`，与本页改造无关。
- 微信开发者工具 `preview`：通过，AppID `wxd548597e78862269`，未进行真机验收。

结论：本页本地闭环 `READY`；Android/iOS 真机、读屏和大字体按冻结规则延期到全部页面本地完成后统一验收。
