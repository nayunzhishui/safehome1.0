# 应用指南 · 四模块深度开发补丁(20260726)

本轮改动以 **git 补丁**形式交付,一次性应用、可回滚、可审阅。所有改动均已在云端通过回归。

## 一、如何应用(在你的仓库根 `D:\codex\workspace\safehome1.0`)

```powershell
# 1) 先建分支,便于回滚
git checkout -b codex/四模块深度开发-20260726

# 2) 预检补丁是否能干净应用
git apply --check "docs\10Claude协作\safehome_四模块深度开发_20260726.patch"

# 3) 应用(保留提交历史)
git am "docs\10Claude协作\safehome_四模块深度开发_20260726.patch"
#   如果 git am 报错,改用:  git apply "…patch"  然后自行 commit
```

## 二、应用后请本机回归(顺序)

```powershell
cd backend
python -m pytest tests -q                              # 后端全量
python scripts\validate_content.py                     # 内容校验
python scripts\audit_task36_f16_therapeutic_assessment.py
python scripts\audit_task36_f15_ai_qa.py
cd ..\apps\web
npm run typecheck && npm run build                     # Web
cd ..\..
node scripts\audit_task23_visual_system.mjs            # 视觉审计(需 chrome)
```

预期:后端全绿(云端沙盒缺少的 `outputs/` 产物/`.git` 证据类用例在你本机应通过);F16/F15 审计 PASS;tsc 归零;build 通过;视觉审计四视口 PASS。

## 三、本补丁包含(均为非越权类)
- 治疗性评估(F16):状态机/乐观锁/高风险入队/证据非空/复核分离/迁移脚本/类型/scope_label/注册表重跑。
- 情感计算:聚合出口值级约束、隐私门独立校验、词典 26→72、否定附加信号、ALLOWED_LABELS 修复。
- AI 接入:prompt 模板实体 + grounding 校验 + 结论词升级 + 真实成本 + 真超时 + per-user 熔断(provider 仍 fake、参与者入口仍关死)。
- 参与者闭环:情绪温度计记录后非评判回执。
- 前端:Web 顶层 ErrorBoundary + lazy 重试;批次 A/D(token 单源化/颜色精确映射/字号收敛/tabBar 对齐/发布卫生/视觉审计入 CI)。
- 新增测试:治疗评估硬化 7、情感 4、AI 4;快照字面量回填。

## 四、本补丁**不含**(按你指示本轮跳过)
- 越权红线(关系报告可见性 RL1–RL4、治疗评估参与者自选研究者 TA-a、展示提权)。
- 前端 B1/B2 逐页 page-state/无障碍(需逐批截图验收)。
- 温度计→训练卡因果链的客户端两行(后端已就绪)。

## 五、颜色/字号人工确认
- `outputs/hex_manual_review.md`:176 处渐变/阴影/低频 hex 待设计确认(脚本未自动改,避免视觉漂移)。
- `outputs/design_token_mismatch_report.md`:warning/danger/web 主色 3 处 token 与现值不一致,待设计决策。
- 字号收敛为 22–23rpx→24rpx 的最小提升;建议应用后对关键页(首页/测评/结果/训练卡/复盘)截图抽验。

细节见 `docs/00_当前事实基准/开发日志_20260726_四模块深度开发.md`。
