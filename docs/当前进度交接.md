# 当前进度交接

## 1. 项目基本信息

- 项目名称：safehome1.0 / 安心陪伴 / ReadFeedback 家长情绪管理支持系统
- 项目路径：`D:\codex\workspace\safehome1.0`
- 项目定位：基于 UP 跨诊断情绪调节框架的家长非评判陪伴训练系统
- MVP 1.0 核心闭环：目标设定 -> 情绪事件记录 -> 情绪与互动模式识别 -> 非诊断反馈 -> UP训练卡推送 -> 家长练习与打卡 -> 周度报告 -> 人工督导补充

## 2. 当前已经完成

- 项目骨架已完成。
- `docs` 已包含项目说明、并行开发任务清单、API接口文档、数据库字段说明。
- `content` 已包含 `training_cards.json`、`feedback_rules.json`、`consent.md`、`privacy.md`。
- `backend`、`apps/miniprogram`、`apps/web`、`shared` 已建立基本目录。

## 3. 当前正在做

- 第二阶段：实现 `backend` 后端 MVP。
- 技术栈：Flask + SQLite。
- 后端需要支撑小程序端和网页端共用 API。

## 4. 下一步任务

按顺序完成：

1. 补齐 `backend/app.py`、`config.py`、`database.py`、`models.py`、`requirements.txt`
2. 实现 routes：`goals`、`diaries`、`feedback`、`cards`、`checkins`、`reports`、`supervision`、`admin`
3. 实现 services：`feedback_service`、`card_service`、`report_service`
4. 使用 `content/feedback_rules.json` 生成非诊断即时反馈
5. 使用 `content/training_cards.json` 推荐 UP 训练卡
6. 添加 `seed_data/seed.py`
7. 添加 `backend/README.md`
8. 用 `curl` 或测试脚本验证接口

## 5. 不要做的事

- 不要先做复杂小程序页面。
- 不要先做复杂网页界面。
- 不要接入真实AI API。
- 不要加入登录注册复杂权限。
- 不要加入视频、语音、社群。
- 不要改变 MVP 1.0 的功能边界。

## 6. 每次新对话启动提示词

请阅读 `AGENTS.md`、`docs/当前进度交接.md`、`docs/API接口文档.md`、`docs/数据库字段说明.md`。先不要写代码，先复述当前项目状态和下一步计划，然后等待我确认。
