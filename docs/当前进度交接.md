# 当前进度交接

更新时间：2026-05-20

项目路径：`D:\codex\workspace\safehome1.0`

## 1. 当前项目定位

`safehome1.0 / 安心陪伴 / ReadFeedback` 是一个面向家长的情绪管理支持系统。MVP 1.0 的核心闭环是：

目标设定 -> 情绪事件记录 -> 情绪与互动模式识别 -> 非诊断反馈 -> UP 训练卡推荐 -> 家长练习与打卡 -> 周度报告 -> 人工督导补充

当前开发策略是：小程序端与网页端并行开发，但两端必须共用同一套 Flask + SQLite 后端 API、共享字段结构和内容库。

## 2. 当前已经完成

### backend MVP 1.0

已完成 Flask + SQLite 后端最小可运行版本：

1. 建立 Flask 应用入口，并注册后端 API 蓝图。
2. 建立 SQLite 数据库初始化逻辑。
3. 建立 8 张 MVP 数据表。
4. 实现目标设定、情绪事件记录、规则反馈、训练卡、练习打卡、周报、人工督导、后台导出 API。
5. 使用 `content/training_cards.json` 作为训练卡来源。
6. 使用 `content/feedback_rules.json` 作为非诊断反馈规则来源。
7. 新增 `backend/seed_data/seed.py`，可生成演示数据。
8. 新增 `backend/README.md`，说明后端安装、初始化、启动和测试方式。

### API 文档与 shared

已完成：

1. `docs/API接口文档.md` 已与当前真实后端 API 对齐。
2. `shared/types/api.ts` 已定义 MVP 1.0 API 类型、请求结构和响应结构。
3. `shared/constants/api.ts` 已定义 API 路径、默认用户、状态枚举、常见场景和伦理边界文案。
4. `shared/mock-data/mvp.ts` 已提供小程序端与网页端共用 mock 数据。
5. `shared/README.md` 已说明 `shared` 的职责和使用原则。

### 前端最小 API 调用层

本轮已完成：

1. `apps/web/src/services/safehomeApi.ts`
   - 提供网页端最小 API client。
   - 默认后端地址为 `http://127.0.0.1:5000`。
   - 已封装目标、日记、反馈、训练卡、打卡、周报、督导、导出 URL。
   - 引用 `shared/constants/api.ts` 和 `shared/types/api.ts`，避免网页端另起一套字段结构。

2. `apps/miniprogram/services/api.js`
   - 提供原生微信小程序 `wx.request` Promise 封装。
   - 默认后端地址为 `http://127.0.0.1:5000`。
   - 已封装与网页端一致的最小接口方法。

3. `docs/前端联调说明.md`
   - 记录后端启动方式；
   - 记录网页端 API client 用法；
   - 记录小程序端请求封装用法；
   - 记录微信开发者工具本地联调注意事项；
   - 明确最小联调顺序。

## 3. 本轮新增或修改文件

### apps/web

- `apps/web/src/services/safehomeApi.ts`

### apps/miniprogram

- `apps/miniprogram/services/api.js`

### docs

- `docs/前端联调说明.md`
- `docs/当前进度交接.md`
- `docs/开发日志.md`

## 4. 当前能运行到哪一步

当前后端可以运行到 Flask API MVP 阶段。

启动方式：

```powershell
cd D:\codex\workspace\safehome1.0\backend
pip install -r requirements.txt
python seed_data\seed.py
python app.py
```

健康检查：

```text
http://127.0.0.1:5000/healthz
```

当前网页端和小程序端已经有最小 API 调用封装，但还没有页面入口。因此当前能做到：

1. 后端可启动；
2. API 文档可作为联调契约；
3. 网页端有 `safeHomeApi` 可供页面调用；
4. 小程序端有 `createSafeHomeApi` 可供页面调用；
5. 下一步可以用最小页面或临时测试入口跑通三条链路：
   - 创建情绪事件记录；
   - 生成即时反馈；
   - 获取训练卡推荐。

## 5. 本轮验证

已完成：

- 读取并确认 `apps/web/src/services/safehomeApi.ts` 文件存在。
- 读取并确认 `docs/前端联调说明.md` 文件存在。
- 尝试运行 `node --check apps\miniprogram\services\api.js`。

验证限制：

- 当前本机 `node.exe` 执行时报 `Access is denied`，因此未能完成 Node 语法检查。
- 当前 `apps/web` 尚未安装 TypeScript/Vite 依赖，因此未运行 `tsc` 或 `vite build`。

## 6. 已知问题

1. 当前后端没有登录、鉴权和真实用户系统，默认测试用户为 `demo-parent`。
2. 当前反馈为规则匹配，不接入 AI，也不进行复杂心理评估。
3. 当前风险识别只保留规则字段和人工督导入口，尚未完成高风险危机处理流程。
4. 当前 SQLite 适合 MVP 和本地试点，不适合正式多人长期并发使用。
5. 当前 CSV 导出没有权限限制，正式部署前必须加后台鉴权。
6. 当前小程序端和网页端只有 API 调用封装，还没有页面入口。
7. 当前本机 `node.exe` 权限被拒绝，需要后续修复 Node 环境后再运行前端构建检查。
8. `shared` 当前是 TypeScript 文件；如果原生微信小程序不能直接复用 TS，需要后续补充 JSON 或 JS 版共享常量。

## 7. 下一轮应该从哪里继续

下一轮建议从“最小联调测试入口”继续，而不是直接做完整页面。

建议顺序：

1. 先修复或确认本机 Node/npm 可正常运行。
2. 为 `apps/web` 补齐最小 Vite/React 依赖和入口文件，创建一个极简联调页。
3. 极简联调页只测试三步：
   - 创建情绪事件记录；
   - 生成即时反馈；
   - 获取训练卡推荐。
4. 小程序端也只建一个最小测试页，调用 `apps/miniprogram/services/api.js`。
5. 两端都能联调后，再开始正式页面开发。

## 8. 下一轮 Codex 对话启动提示词

```text
请继续 safehome1.0 项目，项目路径为 D:\codex\workspace\safehome1.0。

请先阅读：
1. AGENTS.md
2. docs/当前进度交接.md
3. docs/开发日志.md
4. docs/API接口文档.md
5. docs/前端联调说明.md

当前 backend Flask + SQLite MVP 已完成，API 文档已与实际后端对齐，shared 已补齐共用类型、常量和 mock 数据，apps/web 与 apps/miniprogram 已有最小 API 请求封装。下一步请先不要开发复杂页面，先建立最小联调测试入口，验证创建情绪事件记录、生成即时反馈、获取训练卡推荐三个数据流。

注意：上一轮本机执行 node --check 时出现 Access is denied，请先检查 Node/npm 是否可运行，再决定如何进行前端验证。

要求：
1. 不要改乱目录结构；
2. 不要引入复杂 AI 调用；
3. 不要实现登录鉴权；
4. 不要先开发复杂页面；
5. 所有面向家长的反馈文案必须保持非诊断、非标签化、支持性、非评判；
6. 任务结束后必须更新 docs/开发日志.md 和 docs/当前进度交接.md。
```
