# 任务36 F19全量验收与外部门禁证据说明

日期：2026-07-26

## 1. 当前结论

任务36 F00—F19适用工程项已在本地完成。F19全量自动验收通过，但这只代表代码、契约和本地可自动化证据通过，不代表微信、CloudBase、真机、伦理或生产发布获批。

当前状态：

- `engineering_complete_local=true`
- `external_validation_complete=false`
- `release_approved=false`
- 临时展示越权继续保留原范围，但不计入正式权限验收。

## 2. 自动验收范围

统一入口：

```powershell
python scripts/run_task36_f19_acceptance.py run --include-cloud-probe --output .codex_tmp/task36_f19_acceptance.json
python scripts/run_task36_f19_acceptance.py verify --output .codex_tmp/task36_f19_acceptance.json
```

已通过：

- 后端全量：614项；
- shared：内容校验、API契约无漂移、兼容矩阵；
- Web：TypeScript和production build；
- 小程序：41页结构、JS/JSON/组件引用和移动研究工作台；
- 安全：密钥/敏感字段扫描、安全与运营注册表；
- 故障恢复：可靠性审计、迁移/备份/恢复/回滚隔离矩阵；
- 云端：只读`health/ready/auth capabilities`与既有故障快照；
- 文档：计划、事实基准、API、数据库、部署和协作记录路径完整。

机器证据：`outputs/task36_f19_acceptance.json`。该文件只保存命令、返回码、耗时、输出字节数和SHA-256，不保存命令输出正文、token、Secret、手机号、OpenID或参与者原文。

## 3. 本轮问题loop

全量回归先后发现：

1. 小程序页面已增至41页，但体验注册表校验仍硬编码40页；
2. 治疗性评估摘要卡声明三列但缺少`display:grid`；
3. 过期租约缺少安全重领和并发乐观锁；
4. 在线分析任务列表需要逐项执行对象范围过滤；
5. 反馈交付和治疗性评估部分更新缺少命中行数校验；
6. 参与者创建治疗性评估时不应自选研究者。

以上问题已按最小范围修复并纳入回归。没有执行删除历史数据、生产迁移、微信Secret修改、隧道启动或权限放宽。

## 4. 外部门禁证据清单

以下项目均保持`evidence_pending`，只能由负责人或获授权人员完成：

- 研究者一次性密码接收、首次修改和轮换；
- 外部访问者身份、访问范围、有效期和撤销；
- 微信AppID、隐私指引、手机号能力和云托管可信身份确认；
- 微信开发者工具、Android、iOS、拒绝授权、过期授权和双账号；
- 真实研究者分配、参与者收信/已读/撤回；
- 心理、治疗性评估、伦理、安全、隐私和生产负责人签字；
- CloudBase/MySQL生产迁移、备份恢复、回滚和发布批准。

## 5. 异常恢复与回滚

- 验收执行器支持`--resume`，只复用已经通过的命令，失败项修复后可继续；
- 原始命令输出不落入Git，降低敏感信息泄漏风险；
- 发布前若任一外部门禁失败，保持入口关闭或旧读路径，不删除加法表、历史版本、审计和证据；
- CloudBase容器包只生成本地制品，上传和发布仍由负责人决定。
