# 数据字典

最后更新时间：2026-06-04

本文档用于说明 `GET /api/admin/export` 导出的 CSV 字段。导出数据仅用于本地试点核对、研究资料整理和授权范围内分析；导出前必须确认用户授权、脱敏要求和高风险记录处理边界。

## 1. 导出前检查

```text
1. 已确认导出场景属于项目试点、研究整理或督导复核需要。
2. 已确认用户授权，尤其是 research_authorization / research_consent。
3. 已确认是否包含高风险或需复核记录。
4. 对外汇报、归档或共享前，应再次去除可识别身份信息。
5. 不把 CSV 原始数据直接外发给无授权人员。
6. 不把导出结果用于诊断、治疗、危机干预或法律判断。
7. 同步 CSV 导出默认 `limit=1000`，最大 `limit=5000`。
```

## 2. 导出类型

| type | 中文名称 | 数据来源 | 主要用途 | 脱敏说明 |
|---|---|---|---|---|
| `goals` | 目标数据 | `goals` | 查看目标设定情况 | 含 `user_id`，对外使用前需替换为匿名编号 |
| `diaries` | 情绪记录 | `emotion_diaries` | 查看具体情绪事件和回应线索 | 可能含自由文本，属于敏感数据 |
| `feedback` | 反馈结果 | `feedback_results` | 查看规则反馈和推荐训练卡 | 不应作为诊断结论 |
| `checkins` | 练习尝试记录 | `checkins` | 查看训练卡练习后的复盘 | 可能含复盘文本，需谨慎外发 |
| `assessments` | 测一测结果 | `assessment_results` | 查看工作表/测评提交结果 | 学生画像自由文本默认不导出原文 |
| `profile` | 学生画像摘要 | `student_profiles` 转换 | 查看画像摘要、风险状态和推荐任务 | 使用 `anonymous_id`，不导出自由文本原文 |
| `student_profiles` | 学生画像摘要 | `student_profiles` 转换 | 同 `profile` | 使用 `anonymous_id`，不导出自由文本原文 |
| `records` | 研究摘要 | `records` 转换 | 统一研究摘要导出 | 使用 `anonymous_id`，高风险需二次确认 |
| `student_followups` | 学生追踪记录 | `student_profile_followups` 转换 | 查看复测、任务尝试和状态评分 | 使用 `anonymous_id`，只导出文本长度和关键词摘要 |
| `sandplay` | 沙盘表达记录 | `student_sandplay_entries` 转换 | 查看沙盘任务摘要 | 使用 `anonymous_id`，不导出完整反思原文 |
| `parent_assessments` | 家长双量表摘要 | `parent_assessment_submissions` 转换 | 查看家长报告和量表均分 | 使用匿名编号，仍可能含研究批次信息 |
| `raw_wide` | 家长原始宽表 | `parent_assessment_submissions` 转换 | 研究清洗和统计准备 | 仅导出同意研究使用的数据 |
| `long` | 家长长表 | `parent_assessment_submissions` 转换 | 统计分析长表 | 仅导出同意研究使用的数据 |
| `codebook` | 量表题项说明 | 内容库 | 理解题项、维度和反向计分 | 不含用户数据 |
| `reports` | 周报记录 | `weekly_reports` | 查看周度复盘记录 | 不作为成绩单或诊断结论 |
| `supervision` | 督导请求 | `supervision_requests` | 查看人工补充支持请求 | 可能含联系方式和敏感文本 |
| `cards` | 训练卡内容 | `training_cards` | 查看训练卡内容快照 | 不含用户数据 |

## 3. 通用字段字典

| 字段名 | 中文含义 | 数据类型 | 是否脱敏 | 是否敏感 | 研究用途 | 缺失说明 |
|---|---|---|---|---|---|---|
| `id` | 记录 ID | string | 否 | 低 | 关联同一条业务记录 | 不应为空 |
| `user_id` | 用户 ID | string | 否 | 中 | 按用户聚合数据 | 对外使用前应替换为匿名编号 |
| `anonymous_id` | 匿名用户 ID | string | 是 | 低 | 脱敏后聚合分析 | 由 `user_id` 哈希生成或业务写入 |
| `created_at` | 创建时间 | datetime string | 否 | 低 | 时间顺序、试用活跃度 | 无时间则无法做趋势分析 |
| `updated_at` | 更新时间 | datetime string | 否 | 低 | 判断记录是否更新 | 部分表没有该字段 |
| `risk_level` | 风险等级 | enum | 不适用 | 高 | 人工关注和安全筛查 | 缺失时不能判断风险状态 |
| `requires_review` | 是否需要人工复核 | 0/1 或 boolean | 不适用 | 高 | 复核队列筛选 | 缺失时默认不能视为已安全 |
| `export_allowed` | 是否允许导出 | 0/1 | 不适用 | 中 | 控制研究导出范围 | 0 表示不应进入研究导出 |
| `data_quality` | 数据质量标记 | string | 不适用 | 中 | 判断完整、重复、修正等情况 | 缺失表示未做质量标记 |
| `research_authorization_status` | 匿名研究授权状态 | string | 不适用 | 中 | 判断画像研究授权状态 | 缺失表示未记录或未同步 |
| `consent_summary_json` | 同意状态摘要 | JSON string | 不适用 | 中 | 查看同意版本和授权状态 | 缺失表示旧数据或未记录 |

## 4. 核心业务字段

| 字段名 | 中文含义 | 数据类型 | 是否脱敏 | 是否敏感 | 研究用途 | 缺失说明 |
|---|---|---|---|---|---|---|
| `scene` | 亲子互动或目标场景 | text | 否 | 中 | 识别高频场景 | 可为空，但会影响场景统计 |
| `event_description` | 情绪事件描述 | text | 否 | 高 | 理解具体事件背景 | 可能含身份线索，对外使用前需脱敏 |
| `raw_text` | 原始记录文本 | text | 否 | 高 | 仅限内部核对 | 不建议对外导出或展示 |
| `parent_emotion` | 家长主要情绪 | string | 不适用 | 中 | 情绪频次分析 | 可为空 |
| `parent_emotion_intensity` | 家长情绪强度 | integer | 不适用 | 中 | 观察强度变化 | 缺失时不能做强度趋势 |
| `child_emotion` | 孩子主要情绪 | string | 不适用 | 中 | 亲子互动线索 | 可为空 |
| `behavior` | 当时回应 | text | 否 | 高 | 识别可练习回应 | 可能含具体身份线索 |
| `supportive_feedback` | 支持性反馈 | text | 不适用 | 中 | 反馈质量复核 | 不代表诊断或治疗建议 |
| `recommended_card_ids_json` | 推荐训练卡 ID | JSON string | 不适用 | 低 | 训练卡推荐分析 | high 风险时应为空或不推荐普通卡 |
| `card_id` | 训练卡 ID | string | 不适用 | 低 | 练习尝试统计 | 缺失时无法关联训练卡 |
| `completed` | 是否记录练习尝试 | 0/1 | 不适用 | 低 | 练习积累统计 | 不作为完成率考核 |
| `emotion_before` | 练习前情绪强度 | integer | 不适用 | 中 | 观察练习前后变化 | 可为空 |
| `emotion_after` | 练习后情绪强度 | integer | 不适用 | 中 | 观察练习前后变化 | 可为空 |
| `reflection` | 练习复盘 | text | 否 | 高 | 查看做到哪一步和下一步尝试 | 可能含身份或敏感线索 |

## 5. 画像与研究摘要字段

| 字段名 | 中文含义 | 数据类型 | 是否脱敏 | 是否敏感 | 研究用途 | 缺失说明 |
|---|---|---|---|---|---|---|
| `assessment_result_id` | 对应测评结果 ID | string | 不适用 | 中 | 关联测评和画像 | 早期数据可能为空 |
| `profile_code` | 画像编码 | string | 不适用 | 中 | 画像类型统计 | 缺失时无法分类 |
| `profile_name` | 画像名称 | string | 不适用 | 中 | 阶段性画像说明 | 不是人格、疾病或固定判断 |
| `confidence` | 置信度 | number | 不适用 | 中 | 判断结果稳定度 | 缺失时应谨慎解释 |
| `dimension_summary` | 维度摘要 | text | 不适用 | 中 | 研究者快速理解维度 | 由 JSON 转换生成 |
| `recommended_card_ids` | 推荐任务 ID | string | 不适用 | 低 | 推荐任务统计 | high 风险时不应推荐普通训练 |
| `scores_json` | 分数 JSON | JSON string | 不适用 | 高 | 内部复核和模型解释 | 原始分数属于敏感研究数据 |
| `data_json` | 研究摘要 JSON | JSON string | 部分 | 高 | 多模块研究汇总 | 需查看内部字段后再外发 |
| `module_type` | 研究模块类型 | string | 不适用 | 低 | 按模块筛选 `records` | 可为空但不利于分析 |
| `source_id` | 来源业务 ID | string | 不适用 | 中 | 关联原业务记录 | 缺失时无法追溯来源 |

## 6. 家长双量表字段

| 字段名 | 中文含义 | 数据类型 | 是否脱敏 | 是否敏感 | 研究用途 | 缺失说明 |
|---|---|---|---|---|---|---|
| `participant_code` | 参与者编号 | string | 部分 | 中 | 研究批次内追踪 | 对外使用前需确认是否可识别 |
| `research_consent` | 研究授权 | 0/1 | 不适用 | 中 | 判断是否允许研究导出 | 0 不应进入研究分析 |
| `research_consent_status` | 研究授权状态 | string | 不适用 | 中 | 快速区分 agreed / declined | 由 `research_consent` 转换 |
| `study_batch` | 研究批次 | string | 不适用 | 低 | 分批统计 | 可为空 |
| `source_channel` | 来源渠道 | string | 不适用 | 中 | 招募渠道分析 | 可能暴露试点来源 |
| `profile_key` | 家长报告类型 | string | 不适用 | 中 | 支持性反馈分类 | 不是诊断标签 |
| `SCS_SF_CN_12_total` | 自我慈悲总分 | number | 不适用 | 高 | 量表统计 | 缺失时不能做该量表分析 |
| `SCS_SF_CN_12_mean` | 自我慈悲均分 | number | 不适用 | 高 | 量表统计 | 缺失时不能做均值比较 |
| `IUS_12_CN_total` | 不确定性耐受总分 | number | 不适用 | 高 | 量表统计 | 缺失时不能做该量表分析 |
| `IUS_12_CN_mean` | 不确定性耐受均分 | number | 不适用 | 高 | 量表统计 | 缺失时不能做均值比较 |
| `quality_flags` | 数据质量标记 | string | 不适用 | 中 | 判断异常、过快或缺失 | 空值表示暂无质量标记 |
| `duration_seconds` | 填写用时 | integer | 不适用 | 中 | 数据质量判断 | 缺失时无法判断填写速度 |

## 7. 高风险导出规则

```text
profile / student_profiles / records 如果包含 high 风险或需复核记录，后端会要求 confirm_high_risk=true。
该确认只表示导出人知道数据敏感，不代表可以随意外发。
导出页面和 CSV 预览不应展示高风险自由文本原文。
```

## 8. 缺失值解释

| 缺失形式 | 含义 | 建议处理 |
|---|---|---|
| 空字符串 | 用户未填写或字段不适用 | 统计前按缺失处理 |
| `null` | 数据库允许为空 | 不要强行解释为 0 |
| `[]` | 当前没有列表项 | 可解释为没有推荐/没有记录 |
| `{}` | 当前没有结构化摘要 | 需要回看来源记录或标记为缺失 |
| `empty` | 导出接口没有记录 | 表示当前筛选条件下无数据 |
