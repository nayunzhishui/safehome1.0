# 仓库污染与敏感文件扫描记录

更新时间：2026-06-05

## 本轮扫描

已检查：

- `git ls-files`
- `.env`、SQLite/DB、`backups/`、`node_modules/`、`dist/`、`__pycache__/`、`.pytest_cache/`
- `outputs/**/Default/Cache/**`
- `outputs/**/Default/Code Cache/**`
- `outputs/**/Default/GPUCache/**`

## 处理结果

- 发现并移除已跟踪浏览器缓存文件：1582 个。
- 删除范围仅限 `outputs/**/Default/Cache/**`、`outputs/**/Default/Code Cache/**`、`outputs/**/Default/GPUCache/**`。
- 未删除 `outputs` 中的正式截图、HTML、PPT 预览、报告布局和脚本文件。
- 未发现已跟踪 `.env`、数据库文件、`node_modules`、`dist`、`backups` 或 private-user 数据。

## 忽略规则

`.gitignore` 已补充：

```gitignore
outputs/**/Default/Cache/
outputs/**/Default/Code Cache/
outputs/**/Default/GPUCache/
outputs/**/Default/Service Worker/
```

## 复查结果

```text
git ls-files -- outputs/**/Default/Cache/** outputs/**/Default/Code Cache/** outputs/**/Default/GPUCache/** -> 0
敏感运行产物扫描 -> 无命中
```
