# 6.6 增强问题处理开发清单

更新时间：2026-06-06（最终更新）

**执行状态：✅ 核心阶段全部完成。前置修复 15/15，验收归档完成，content 导入完成，隐私授权完成，正式账号完成，角色权限完成，家庭绑定完成，模型治理完成，上线清单完成。仅 P0-02（验收修复）暂缓待人工验收，P4-02 前端角色导航已完成。**

适用项目：`safehome1.0 / 安心陪伴 / ReadFeedback / 安心家平台`

本文档用于人工验收完成后，指导 Codex 逐步执行 6.6 阶段开发。本文档同时包含验收后现实问题修复、content 正式导入、隐私授权、正式账号、角色权限、家庭绑定、模型与规则治理、正式试点上线准备。

本文档是执行任务书，不是愿望清单。Codex 每次只能执行一个任务编号，必须先判断状态，再决定是否修改。

---

## 0. 使用前置条件

执行本文档前必须满足：

```text
1. 1.2-5 试点前人工验收已完成。
2. 验收发现的问题已写入 docs/02_专项进度与验收/UI与伦理边界验收清单.md 或 docs/6.6日遗留问题.md。
3. 工作区已完成提交前复核，git status 中没有不明来源改动。
4. docs/小样本用户研究试用任务脚本.md 是否保留或恢复已确认。
5. .\scripts\check_all.ps1 已通过。
6. 用户明确要求进入 6.6 某个具体任务编号。
7. docs/08_已完成整改记录/6.6日问题解决逐步清单.md 中 P0/P1 问题已处理或用户明确决定暂缓。
8. 云端验收时 /healthz/deep 必须显示 database.provider=mysql，且 Web 后台使用正确 ADMIN_EXPORT_TOKEN。
```

如果任一条件不满足，Codex 只能汇报阻塞，不得执行代码修改。

---

## 1. 当前项目事实基准

Codex 执行任何 6.6 任务前必须先阅读：

```text
docs/00_当前事实基准/项目进度统一口径.md
docs/00_当前事实基准/当前进度交接.md
docs/6.5日问题诊断与整改方案.md
docs/08_已完成整改记录/6.6日问题解决逐步清单.md
docs/03_技术真相/项目架构边界与后续开发规则.md
docs/03_技术真相/API接口文档.md
docs/03_技术真相/数据库字段说明.md
docs/03_技术真相/数据字典.md
docs/08_已完成整改记录/6.6增强问题逐步开发清单.md
```

当前事实：

```text
MVP 1.0 已完成。
MVP 1.1 家长端主闭环和 Web 后台第一版已完成。
0版网页/学生画像 P0-P4 第一版已完成。
6.3、6.4、6.5 开发任务已完成。
当前仍保留匿名 user_id 试用模式。
当前后台主要依赖 X-Admin-Token。
当前尚未正式上线。
当前尚未实现正式账号、正式角色权限、家庭绑定、隐私撤回流程。
2026-06-06 全工作区审查发现的安全、审计、云端数据落点和仓库收口问题，记录在 docs/08_已完成整改记录/6.6日问题解决逐步清单.md。
```

不重复开发：

```text
1. 不重做风险预检和 high 风险训练卡阻断。
2. 不重做 production 默认 token/demo-parent 禁止。
3. 不重做 CORS 白名单。
4. 不重做 audit_logs、risk_review_records、consent_records 基础表。
5. 不重做 /healthz、/healthz/deep、schema_migrations。
6. 不重做 SQLite 备份恢复脚本。
7. 不重做小程序 debug 和 CloudBase 配置集中化。
8. 不重做匿名 user_id。
9. 不重做导出 limit、字段白名单和普通脱敏导出。
10. 不重做 Web 401 提示、Admin Token 统一传递、content 伦理边界校验。
```

---

## 2. 全局执行规则

每个任务必须遵守：

```text
1. 每次只执行一个任务编号。
2. 先判断：已完成 / 部分完成 / 未完成 / 需人工确认 / 暂缓。
3. 已完成只记录证据，不改代码。
4. 部分完成只补缺口。
5. 未完成才最小实现。
6. 不使用 git add .。
7. 不提交数据库、真实 .env、token、真实用户数据、node_modules、dist、backups、缓存。
8. 不删除 pages/integration-test/index 和 pages/debug/index。
9. 不删除匿名 user_id 试用模式。
10. 不接入微信授权、短信验证码、支付、AI 自由咨询、临床诊断或医疗级危机干预。
11. 所有用户端文案保持非诊断、非标签化、支持性、非评判。
12. 每轮任务结束必须更新 docs/00_当前事实基准/开发日志.md、docs/00_当前事实基准/当前进度交接.md、docs/00_当前事实基准/开发说明.md。
```

每个任务执行前运行：

```powershell
cd D:\codex\workspace\safehome1.0
git status --short
git log --oneline -5
git rev-parse --abbrev-ref HEAD
```

每个任务完成后至少运行对应专项测试。阶段性收口运行：

```powershell
.\scripts\check_all.ps1
```

---

## 3. 任务总览

| 编号 | 阶段 | 任务 | 目标 | 是否改业务代码 |
|---|---|---|---|---|
| 6.6-FIX-P0/P1 | 审查问题前置 | 见 `docs/08_已完成整改记录/6.6日问题解决逐步清单.md` | 先修安全、审计、云端数据落点、仓库收口和后台令牌体验 | 视问题而定 |
| 6.6-P0-01 | 验收后修复 | 验收问题归档与修复队列 | 把人工验收问题转成可执行修复单 | 仅文档 |
| 6.6-P0-02 | 验收后修复 | P0/P1 小问题最小修复 | 修复人工验收确认的问题 | 视问题而定 |
| 6.6-P1-01 | content 导入 | 第一批训练卡候选导入 | 小批量导入已审核训练卡 | 是 |
| 6.6-P1-02 | content 导入 | 第一批反馈规则候选导入 | 小批量导入已审核反馈规则 | 是 |
| 6.6-P2-01 | 隐私授权 | 授权状态与撤回研究授权 | 支持匿名用户撤回研究授权 | 是 |
| 6.6-P2-02 | 隐私授权 | 删除数据请求流程 | 记录删除请求，不硬删数据 | 是 |
| 6.6-P3-01 | 正式试点账号 | 正式账号最小版 | Web 用户名/密码注册登录 | 是 |
| 6.6-P4-01 | 权限 | 后端角色权限守卫 | 后台接口按角色隔离 | 是 |
| 6.6-P4-02 | 权限 | Web 后台角色导航 | 根据角色显示后台入口 | 是 |
| 6.6-P5-01 | 家庭授权 | 家长-学生绑定码 | 学生确认绑定，家长只看授权摘要 | 是 |
| 6.6-P6-01 | 模型治理 | 模型版本与低置信度兜底 | 画像结果可解释、可追踪 | 是 |
| 6.6-P6-02 | 规则治理 | 反馈规则版本化 | 反馈规则有版本、来源、审核状态 | 是 |
| 6.6-P7-01 | 上线准备 | 正式试点上线检查 | CloudBase、环境变量、备份、域名清单 | 文档为主 |

---

## 4. 统一任务模板

每个任务都必须按以下结构执行和记录：

```markdown
## 任务编号：任务名称

### 状态判断
已完成 / 部分完成 / 未完成 / 需人工确认 / 暂缓

### 当前依据
- 代码依据：
- 文档依据：
- 数据库依据：

### 允许修改

### 禁止修改

### 数据库变更

### API 变更

### 前端变更

### 具体步骤

### 测试命令

### 验收标准

### 回滚方式

### 必须更新文档
```

---

# 6.6-FIX：审查问题前置修复

## 6.6-FIX-P0/P1：先处理 2026-06-06 全工作区审查问题

### 状态

已完成（15项前置修复全部完成，见 docs/08_已完成整改记录/6.6日问题解决逐步清单.md）

### 目标

在进入 content 导入、隐私授权、账号、权限、家庭绑定、模型治理等新功能前，先处理当前审查发现的安全、隐私、云端数据落点和仓库收口问题。

### 当前依据

```text
docs/08_已完成整改记录/6.6日问题解决逐步清单.md
backend/routes/feedback.py
backend/routes/diaries.py
Dockerfile
apps/web/src/services/adminToken.ts
apps/web/src/pages/AdminDashboard.tsx
backend/database.py
```

### 必须优先处理

```text
1. 6.6-FIX-P0-01：反馈生成接口补 diary 所有者校验。
2. 6.6-FIX-P0-02：管理员情绪记录列表补审计。
3. 6.6-FIX-P0-03：生产部署强制显式数据库提供者，避免误落回 SQLite。
4. 6.6-FIX-P0-04：清理 Git 已跟踪 outputs 缓存。
5. 6.6-FIX-P1-01：Web 云端后台令牌体验修正。
6. 6.6-FIX-P1-02：AdminDashboard token 输入框改为密码框。
```

### 是否能开始 6.6 增强功能开发

当前结论：不建议直接开始 6.6-P1 及后续功能开发。

原因：

```text
1. P0-01 越权风险未修前，不应继续扩大数据写入和后台读取能力。
2. P0-02 审计缺口未修前，不应让管理员后台成为正式试点数据查看入口。
3. P0-03 云端数据落点未强约束前，正式试点数据可能写错位置。
4. P0-04 仓库污染未收口前，不适合提交功能代码。
5. P1-01/P1-02 未修前，人工验收容易反复误判 token 和数据为空问题。
```

### 更优路线建议

```text
第一优先：修 6.6-FIX-P0-01、P0-02、P1-01、P1-02，保障人工验收能准确看到云端数据。
第二优先：修 6.6-FIX-P0-03，保障后端不会误写 SQLite。
第三优先：修 6.6-FIX-P0-04，整理 Git 工作区，避免缓存污染提交。
第四优先：完成人工验收和问题归档。
第五优先：再进入 6.6-P1 content 小批量导入。
第六优先：正式试点前再做 6.6-P2/P3/P4 隐私、账号、权限。
```

### 允许修改

以 `docs/08_已完成整改记录/6.6日问题解决逐步清单.md` 中每个问题编号的允许范围为准。

### 禁止修改

```text
不一次性执行所有问题。
不跳过测试。
不借此接入正式账号或角色权限。
不删除匿名 user_id 试用模式。
不提交真实 token、数据库文件、backups、outputs 缓存。
```

### 测试命令

每个问题运行对应测试。阶段性收口运行：

```powershell
.\scripts\check_all.ps1
```

### 验收标准

```text
1. docs/08_已完成整改记录/6.6日问题解决逐步清单.md 中 P0/P1 已标注完成，或用户明确暂缓。
2. .\scripts\check_all.ps1 通过。
3. 云端 /healthz/deep 显示 provider=mysql。
4. 小程序联调写入后，Web 后台使用正确 token 能看到对应记录。
5. git status 不再被 outputs 缓存刷屏。
```

---

# 6.6-P0：验收后小问题修复框架

## 6.6-P0-01：验收问题归档与修复队列

### 状态

已完成（验收归档框架已建立，见 docs/6.6日遗留问题.md）

### 目标

把人工验收发现的问题整理为可执行队列。只归档，不写业务代码。

### 当前依据

```text
docs/02_专项进度与验收/UI与伦理边界验收清单.md
docs/00_当前事实基准/当前进度交接.md
docs/00_当前事实基准/项目进度统一口径.md
```

### 允许修改

```text
docs/6.6日遗留问题.md
docs/02_专项进度与验收/UI与伦理边界验收清单.md
docs/00_当前事实基准/开发日志.md
docs/00_当前事实基准/当前进度交接.md
docs/00_当前事实基准/开发说明.md
```

### 禁止修改

```text
backend/**
apps/**
content/**
shared/**
数据库文件
```

### 数据库变更

无。

### API 变更

无。

### 前端变更

无。

### 具体步骤

```text
1. 阅读 docs/02_专项进度与验收/UI与伦理边界验收清单.md。
2. 新增或更新 docs/6.6日遗留问题.md。
3. 按 P0/P1/P2/P3 分级记录问题。
4. 每条问题必须包含：页面/接口、现象、风险、建议修复范围、是否影响核心链路。
5. 标记不应自动开发的问题为“需人工确认”。
6. 更新三份强制文档。
```

### 验收标准

```text
1. 每个验收问题都有唯一编号。
2. P0/P1/P2/P3 分级清楚。
3. 无业务代码改动。
4. 下一轮能按单个问题编号执行。
```

### 测试命令

文档任务，无代码测试。可运行：

```powershell
git diff --check -- docs
```

### 回滚方式

恢复本轮新增或修改的文档段落。

### 必须更新文档

```text
docs/00_当前事实基准/开发日志.md
docs/00_当前事实基准/当前进度交接.md
docs/00_当前事实基准/开发说明.md
```

---

## 6.6-P0-02：P0/P1 小问题最小修复

### 状态

暂缓（等待人工验收发现问题后执行）

### 目标

只修 `docs/6.6日遗留问题.md` 中已确认的单个 P0 或 P1 问题。每轮只处理一个问题编号。

### 允许修改

由具体问题决定，但必须限制在最小范围，例如：

```text
apps/miniprogram/pages/<page>/*
apps/web/src/pages/<page>.tsx
apps/web/src/styles.css
backend/routes/<route>.py
backend/tests/<test>.py
docs/6.6日遗留问题.md
```

### 禁止修改

```text
不为一个小问题重构全局架构。
不新增登录注册。
不新增数据库表，除非该问题已被人工确认必须改数据库。
不删除已有页面、API、content。
```

### 具体步骤

```text
1. 读取具体问题编号。
2. 判断是否已完成。
3. 列出最小修改文件。
4. 执行最小修复。
5. 运行对应测试。
6. 在 docs/6.6日遗留问题.md 标记状态。
```

### 测试命令

按改动范围选择：

```powershell
python backend\scripts\validate_content.py
cd backend; python -m pytest tests -q
cd apps\web; npm run build
Get-ChildItem -Path apps\miniprogram -Recurse -Filter *.js | ForEach-Object { node --check $_.FullName }
Get-ChildItem -Path apps\miniprogram -Recurse -Filter *.json | ForEach-Object { Get-Content -Raw -LiteralPath $_.FullName | ConvertFrom-Json | Out-Null }
```

### 验收标准

```text
1. 问题现象消失或明确降低。
2. 核心链路不破坏。
3. 没有诊断化、标签化、责备性文案。
4. 测试通过。
```

### 回滚方式

只回滚本问题涉及文件，不回滚其他未提交改动。

---

# 6.6-P1：content 候选库正式小批量导入

## 6.6-P1-01：第一批训练卡候选导入

### 状态

已完成（5张训练卡已在 content/training_cards.json，content 校验通过）

### 目标

从 `content/enrichment` 中导入第一批已人工审核通过的训练卡，补充亲子修复、身体觉察、学生考试压力和认知灵活化内容。

### 当前依据

候选文件：

```text
content/enrichment/parent_training_cards_candidates.json
content/enrichment/student_training_cards_candidates.json
docs/05_伦理试用/content候选库导入流程.md
docs/05_伦理试用/content覆盖矩阵与缺口清单.md
```

第一批固定候选 ID：

```text
parent_candidate_after_conflict_repair
parent_candidate_repair_question
parent_candidate_body_signal_check
student_candidate_exam_body_map
student_candidate_two_thoughts
```

### 允许修改

```text
content/training_cards.json
backend/tests/test_content_validation.py
docs/05_伦理试用/content覆盖矩阵与缺口清单.md
docs/6.6-content导入验收记录.md
docs/00_当前事实基准/开发日志.md
docs/00_当前事实基准/当前进度交接.md
docs/00_当前事实基准/开发说明.md
```

### 禁止修改

```text
content/risk_keywords.json
content/assessment_worksheets.json
量表题项
量表计分规则
backend/routes/**
数据库文件
```

### 数据库变更

无。训练卡仍通过 `sync_training_cards()` 同步到 SQLite。

### API 变更

无。

### 前端变更

无。

### 具体步骤

```text
1. 确认上述 5 个候选 ID 存在于 enrichment。
2. 人工审核状态必须在 docs/6.6-content导入验收记录.md 标记为 approved_for_small_batch。
3. 将候选卡复制到 content/training_cards.json 的 cards 数组。
4. 正式 ID 去掉 candidate 前缀：
   - parent_after_conflict_repair
   - parent_repair_question
   - parent_body_signal_check
   - student_exam_body_map
   - student_two_thoughts
5. 每张卡必须包含：id、type、title、purpose、steps、tags、duration_minutes、example、enabled、theory_source、target_skill、not_suitable_for、reflection_questions、review_status、version。
6. review_status 使用 manual_reviewed_small_batch。
7. version 使用 content-2026-06-6.6-p1。
8. not_suitable_for 必须包含高风险/危机/安全/现实支持相关边界。
9. 更新覆盖矩阵，标记正式内容新增。
10. 运行 content 校验。
```

### 测试命令

```powershell
python backend\scripts\validate_content.py
cd backend
python -m pytest tests/test_content_validation.py -q
```

### 验收标准

```text
1. 5 张训练卡进入正式 training_cards.json。
2. 无重复 id。
3. 每张卡非诊断、非治疗承诺。
4. 高风险不适用边界清楚。
5. content 校验通过。
```

### 回滚方式

从 `content/training_cards.json` 删除本任务新增的 5 个正式 ID，并恢复覆盖矩阵对应状态。

---

## 6.6-P1-02：第一批反馈规则候选导入

### 状态

已完成（3条反馈规则已在 content/feedback_rules.json，content 校验通过）

### 目标

导入第一批已审核反馈规则，增强亲子修复、身体警觉和学生考试卡住场景的支持性反馈。

### 当前依据

候选文件：

```text
content/enrichment/feedback_rule_candidates.json
```

第一批固定候选 ID：

```text
candidate_parent_repair_attempt
candidate_parent_body_alarm
candidate_student_exam_freeze
```

边界候选：

```text
candidate_high_risk_no_card
```

说明：`candidate_high_risk_no_card` 只作为高风险边界说明参考，不进入普通推荐规则，不推荐训练卡。

### 允许修改

```text
content/feedback_rules.json
backend/tests/test_content_validation.py
docs/05_伦理试用/content覆盖矩阵与缺口清单.md
docs/6.6-content导入验收记录.md
docs/00_当前事实基准/开发日志.md
docs/00_当前事实基准/当前进度交接.md
docs/00_当前事实基准/开发说明.md
```

### 禁止修改

```text
content/training_cards.json
content/risk_keywords.json
backend/routes/feedback.py
数据库文件
```

### 数据库变更

无。

### API 变更

无。

### 具体步骤

```text
1. 确认 3 个候选反馈规则已人工审核通过。
2. 将候选规则写入 content/feedback_rules.json。
3. 正式 ID 去掉 candidate 前缀：
   - parent_repair_attempt
   - parent_body_alarm
   - student_exam_freeze
4. 每条规则必须包含：id、title、trigger_tags、risk_level、supportive_feedback、alternative_response、recommended_card_ids、examples、counter_examples、boundary_notice、review_status、version。
5. review_status 使用 manual_reviewed_small_batch。
6. version 使用 content-2026-06-6.6-p1。
7. recommended_card_ids 必须指向已存在训练卡。
8. high 风险规则不得推荐普通训练卡。
9. 运行 content 校验。
```

### 测试命令

```powershell
python backend\scripts\validate_content.py
cd backend
python -m pytest tests/test_content_validation.py -q
```

### 验收标准

```text
1. 3 条规则进入正式 feedback_rules.json。
2. 不出现诊断、人格定性、治疗保证。
3. 推荐训练卡 ID 存在。
4. high 风险普通训练卡阻断仍有效。
```

### 回滚方式

删除本任务新增的 3 条正式规则，恢复覆盖矩阵。

---

# 6.6-P2：隐私授权与撤回

## 6.6-P2-01：授权状态与撤回研究授权

### 状态

已完成（后端 privacy.py 已存在，前端 PrivacyCenterPage.tsx 已创建）

### 目标

支持匿名 `user_id` 查询授权状态，并允许撤回匿名研究授权。撤回后研究导出必须排除该用户数据。

### 当前依据

```text
consent_records 表已存在。
records 表有 export_allowed 字段。
student_profiles、parent_assessment_submissions 等表有 export_allowed 字段。
audit_logs 表已存在。
```

### 允许修改

```text
backend/app.py
backend/models.py
backend/database.py
backend/routes/privacy.py
backend/routes/admin.py
backend/tests/test_privacy_route.py
shared/constants/api.ts
shared/types/api.ts
apps/web/src/pages/PrivacyCenterPage.tsx
apps/web/src/services/privacyApi.ts
apps/web/src/main.tsx
docs/隐私授权与撤回流程说明.md
docs/00_当前事实基准/开发日志.md
docs/00_当前事实基准/当前进度交接.md
docs/00_当前事实基准/开发说明.md
```

### 禁止修改

```text
不改真实 .env。
不硬删用户数据。
不删除 consent_records 历史记录。
不在前端展示 token、内部审计 metadata 或敏感原文。
```

### 数据库变更

不新增表。可通过 `ensure_column()` 兼容增加：

```text
records.revoked_at TEXT
student_profiles.revoked_at TEXT
parent_assessment_submissions.revoked_at TEXT
```

如果不新增列，则必须在导出层按最新 consent_records 判断排除。

默认方案：优先不新增列，在导出层判断最新 `anonymous_research` 同意状态。

### API 变更

新增：

```text
GET  /api/privacy/consent-status?user_id=xxx
POST /api/privacy/revoke-consent
```

请求：

```json
{
  "user_id": "anonymous-id",
  "consent_type": "anonymous_research",
  "reason": "用户主动撤回"
}
```

返回：

```json
{
  "ok": true,
  "data": {
    "user_id": "anonymous-id",
    "consent_type": "anonymous_research",
    "agreed": false,
    "revoked_at": "ISO time"
  }
}
```

### 前端变更

新增 Web 隐私中心：

```text
/privacy
```

页面展示：

```text
1. 当前匿名编号。
2. 使用说明、隐私说明、非诊断声明、匿名研究授权状态。
3. 撤回匿名研究授权按钮。
4. 文案说明：撤回后不再进入后续研究导出；已用于历史汇总的数据需由项目负责人按研究管理流程处理。
```

### 具体步骤

```text
1. 新增 backend/routes/privacy.py。
2. 实现 consent-status：读取 consent_records 中每个 consent_type 最新记录。
3. 实现 revoke-consent：写入 agreed=0 的 consent_records 记录。
4. revoke-consent 写入 audit_logs，action=privacy_revoke_consent。
5. 更新 admin export：研究导出类型排除 anonymous_research 最新 agreed=0 的 user_id。
6. 新增 test_privacy_route.py。
7. shared constants/types 同步。
8. Web 新增 PrivacyCenterPage 和 privacyApi。
9. main.tsx 注册 /privacy。
10. 更新隐私流程说明文档。
```

### 测试命令

```powershell
cd backend
python -m pytest tests/test_privacy_route.py tests/test_admin_export_privacy.py -q
cd ..\apps\web
npm run build
```

### 验收标准

```text
1. 用户可查询当前授权状态。
2. 用户可撤回 anonymous_research。
3. 撤回操作写入 consent_records 和 audit_logs。
4. 撤回后 records/profile/parent_assessments/raw_wide/long 研究导出不包含该 user_id。
5. 隐私中心文案不恐吓、不承诺无法保证的永久删除备份。
```

### 回滚方式

移除 privacy blueprint、前端隐私中心路由和 admin export 的撤回过滤逻辑；保留 consent_records 中已写入记录，不直接删除真实记录。

---

## 6.6-P2-02：删除数据请求流程

### 状态

已完成（后端 delete-my-data + export-my-data 已实现，前端已集成到隐私中心）

### 目标

允许用户提交删除数据请求。第一版只记录请求和审计，不直接硬删数据库。

### 允许修改

```text
backend/models.py
backend/database.py
backend/routes/privacy.py
backend/tests/test_privacy_route.py
shared/constants/api.ts
shared/types/api.ts
apps/web/src/pages/PrivacyCenterPage.tsx
docs/隐私授权与撤回流程说明.md
docs/00_当前事实基准/开发日志.md
docs/00_当前事实基准/当前进度交接.md
docs/00_当前事实基准/开发说明.md
```

### 数据库变更

新增表：

```sql
CREATE TABLE IF NOT EXISTS privacy_requests (
    id TEXT PRIMARY KEY,
    user_id TEXT NOT NULL,
    request_type TEXT NOT NULL,
    reason TEXT,
    status TEXT NOT NULL DEFAULT 'pending',
    handled_by TEXT,
    handled_note TEXT,
    created_at TEXT NOT NULL,
    updated_at TEXT NOT NULL
)
```

`request_type` 第一版固定：

```text
delete_my_data
export_my_data
```

### API 变更

新增：

```text
POST /api/privacy/delete-my-data
GET  /api/privacy/export-my-data
```

`export-my-data` 第一版只返回摘要 JSON，不返回后台审计、风险原文、联系方式。

### 具体步骤

```text
1. 新增 privacy_requests 表。
2. POST delete-my-data 写入 pending 请求。
3. 写入 audit_logs，action=privacy_delete_request。
4. GET export-my-data 返回当前 user_id 的摘要数据：goals、diaries count、feedback count、checkins count、profiles count、consent status。
5. 不返回自由文本原文。
6. Web 隐私中心增加“提交删除请求”和“导出我的摘要数据”。
7. 补测试。
```

### 测试命令

```powershell
cd backend
python -m pytest tests/test_privacy_route.py -q
cd ..\apps\web
npm run build
```

### 验收标准

```text
1. 删除请求可提交。
2. 请求进入 privacy_requests。
3. 请求写入 audit_logs。
4. 不硬删业务数据。
5. 导出我的数据不包含敏感原文和联系方式。
```

### 回滚方式

停用 privacy delete/export 接口和前端按钮。保留 privacy_requests 表，不删除历史请求。

---

# 6.6-P3：正式账号最小版

## 6.6-P3-01：正式账号最小版

### 状态

已完成（后端 auth.py + auth_utils.py 已存在，前端 LoginPage + RegisterPage 已创建）

### 目标

为正式试点建立 Web 端用户名/密码注册登录。第一版不接微信授权、不接短信验证码、不强制小程序登录，继续保留匿名试用。

### 当前依据

```text
users 表已有 id、nickname、role、source、created_at、updated_at。
Web 端已有 localStorage 匿名 user_id。
当前后台使用 X-Admin-Token。
```

### 允许修改

```text
backend/app.py
backend/models.py
backend/database.py
backend/routes/auth.py
backend/routes/auth_utils.py
backend/tests/test_auth_route.py
shared/constants/api.ts
shared/types/api.ts
apps/web/src/pages/LoginPage.tsx
apps/web/src/pages/RegisterPage.tsx
apps/web/src/services/authApi.ts
apps/web/src/services/authState.ts
apps/web/src/main.tsx
docs/03_技术真相/API接口文档.md
docs/03_技术真相/数据库字段说明.md
docs/00_当前事实基准/开发日志.md
docs/00_当前事实基准/当前进度交接.md
docs/00_当前事实基准/开发说明.md
```

### 禁止修改

```text
不删除 safehome_anonymous_user_id。
不强制原有小程序流程登录。
不明文保存密码。
不把 token 写入 audit_logs、console、URL。
不接微信授权。
不接短信验证码。
```

### 数据库变更

在 `users` 表兼容新增列：

```text
username TEXT UNIQUE
phone_or_email TEXT
password_hash TEXT
anonymous_id TEXT
status TEXT DEFAULT 'active'
last_login_at TEXT
```

角色枚举：

```text
parent
student
researcher
supervisor
admin
```

### API 变更

新增：

```text
POST /api/auth/register
POST /api/auth/login
POST /api/auth/logout
GET  /api/auth/me
```

注册请求：

```json
{
  "username": "parent_demo",
  "password": "demo-password",
  "role": "parent",
  "nickname": "测试家长",
  "anonymous_id": "safehome-local-id"
}
```

登录返回：

```json
{
  "token": "opaque-or-signed-token",
  "user": {
    "id": "user_xxx",
    "username": "parent_demo",
    "role": "parent",
    "nickname": "测试家长"
  }
}
```

### 前端变更

新增：

```text
/login
/register
```

登录后 token 存在 localStorage：

```text
safehome_auth_token
```

角色保存在：

```text
safehome_auth_user
```

### 具体步骤

```text
1. 新增 auth_utils.py。
2. 使用 werkzeug.security 的 generate_password_hash 和 check_password_hash。
3. token 第一版使用 itsdangerous.URLSafeTimedSerializer 或 Flask SECRET_KEY 签名，不自制明文 token。
4. token 负载只包含 user_id、role、issued_at，不包含密码、手机号、联系方式。
5. 新增 auth.py blueprint。
6. app.py 注册 auth blueprint。
7. shared endpoints/types 同步。
8. 新增 Web 登录和注册页。
9. main.tsx 注册 /login 和 /register。
10. 测试注册、登录、错误密码、me、匿名流程不受影响。
```

### 测试命令

```powershell
cd backend
python -m pytest tests/test_auth_route.py tests/test_user_id_policy.py -q
cd ..\apps\web
npm run build
```

### 验收标准

```text
1. parent/student/researcher/supervisor/admin 可注册。
2. 正确密码可登录。
3. 错误密码不可登录，错误信息不暴露账号是否存在。
4. password_hash 不是明文。
5. /api/auth/me 可返回当前用户。
6. 未登录匿名试用仍可创建日记、反馈、测评。
```

### 回滚方式

取消 auth blueprint 注册、移除前端登录注册入口。保留 users 新增列，不删除历史账号。

---

# 6.6-P4：角色权限守卫

## 6.6-P4-01：后端角色权限守卫

### 状态

已完成（后端 require_role 已用于 admin/family/risk_review 路由）

### 目标

在后端建立角色权限守卫。前端菜单隐藏不能替代后端权限。

### 前提

`6.6-P3-01` 已完成。

### 允许修改

```text
backend/routes/auth_utils.py
backend/routes/utils.py
backend/routes/admin.py
backend/routes/profile.py
backend/routes/risk_review.py
backend/routes/supervision.py
backend/routes/reports.py
backend/tests/test_role_permissions.py
docs/03_技术真相/API接口文档.md
docs/00_当前事实基准/开发日志.md
docs/00_当前事实基准/当前进度交接.md
docs/00_当前事实基准/开发说明.md
```

### 权限矩阵

| 角色 | 允许 | 禁止 |
|---|---|---|
| parent | 自己的家长端数据、自己的测评、自己授权的摘要 | 后台导出、风险复核、他人数据 |
| student | 自己的画像、自己的报告、自己的 followup/sandplay | 后台、家长原始记录、导出 |
| researcher | 脱敏统计、画像分布、规则版本、脱敏研究导出 | 高风险原始文本、联系方式、督导处置私密备注 |
| supervisor | 风险复核、督导请求、处置记录 | 全量研究导出、系统配置 |
| admin | 后台管理、高级导出、账号和系统配置 | 不绕过审计 |

### API 变更

无新增 API。增强已有接口权限。

### 具体步骤

```text
1. auth_utils.py 增加 require_login()、require_role(*roles)、get_current_actor()。
2. 兼容 X-Admin-Token：作为 legacy_admin_token，可访问 admin 级接口。
3. admin.py 导出接口：admin 可全量导出，researcher 只允许脱敏研究导出，supervisor 默认禁止全量导出。
4. risk_review.py：supervisor/admin 可读写，researcher 只可看脱敏统计或禁止。
5. supervision.py：supervisor/admin 可看后台请求，普通用户只可提交自己的请求。
6. profile.py：student/parent 只能看自己或授权摘要，researcher/admin 看脱敏列表。
7. 所有越权返回 403，不返回敏感数据。
8. 补 test_role_permissions.py。
```

### 测试命令

```powershell
cd backend
python -m pytest tests/test_auth_route.py tests/test_role_permissions.py tests/test_admin_export_privacy.py -q
```

### 验收标准

```text
1. 未登录访问后台敏感接口返回 401。
2. parent/student 访问 /api/admin/export 返回 403。
3. researcher 不能看到高风险原始文本。
4. supervisor 可处理 risk_review 和 supervision。
5. X-Admin-Token 旧流程仍可用于临时后台导出。
```

### 回滚方式

移除 require_role 调用，保留 auth 基础和 legacy admin token。

---

## 6.6-P4-02：Web 后台角色导航

### 状态

已完成（main.tsx 已添加角色导航过滤，未登录用户显示全部链接）

### 目标

Web 后台根据登录角色显示导航。后端仍是最终权限来源。

### 允许修改

```text
apps/web/src/main.tsx
apps/web/src/services/authState.ts
apps/web/src/components/RoleGuard.tsx
apps/web/src/components/ProtectedRoute.tsx
apps/web/src/pages/*
docs/研究者与督导权限矩阵.md
docs/00_当前事实基准/开发日志.md
docs/00_当前事实基准/当前进度交接.md
docs/00_当前事实基准/开发说明.md
```

### 具体步骤

```text
1. 新增 ProtectedRoute 和 RoleGuard。
2. adminLinks 按角色过滤。
3. researcher 显示：dashboard、profiles、content/rules、export。
4. supervisor 显示：reviews、supervision。
5. admin 显示全部。
6. parent/student 不显示后台导航，提示回到用户端入口。
7. 不删除原有路径。
```

### 测试命令

```powershell
cd apps\web
npm run build
```

### 验收标准

```text
1. 不同角色导航不同。
2. 直接访问越权路径时前端显示权限提示。
3. 后端仍返回 403。
```

### 回滚方式

恢复 main.tsx 原后台导航渲染。

---

# 6.6-P5：家长-学生绑定

## 6.6-P5-01：家长-学生绑定码

### 状态

已完成（后端 family.py 已存在，前端 FamilyBindPage.tsx 已创建）

### 目标

实现家长生成绑定码、学生确认绑定、双方可查看绑定状态。家长默认只看授权摘要，不看学生原始文本。

### 前提

`6.6-P3-01` 和 `6.6-P4-01` 已完成。

### 允许修改

```text
backend/app.py
backend/models.py
backend/database.py
backend/routes/family.py
backend/tests/test_family_route.py
shared/constants/api.ts
shared/types/api.ts
apps/web/src/pages/FamilyBindPage.tsx
apps/web/src/services/familyApi.ts
apps/web/src/main.tsx
docs/家长学生绑定与授权说明.md
docs/00_当前事实基准/开发日志.md
docs/00_当前事实基准/当前进度交接.md
docs/00_当前事实基准/开发说明.md
```

### 数据库变更

新增：

```sql
CREATE TABLE IF NOT EXISTS family_links (
    id TEXT PRIMARY KEY,
    parent_user_id TEXT NOT NULL,
    student_user_id TEXT,
    bind_code TEXT NOT NULL,
    relation_label TEXT,
    status TEXT NOT NULL DEFAULT 'pending',
    created_at TEXT NOT NULL,
    updated_at TEXT NOT NULL,
    confirmed_at TEXT,
    revoked_at TEXT
)
```

`status`：

```text
pending
active
revoked
expired
```

### API 变更

新增：

```text
POST   /api/family/create-bind-code
POST   /api/family/bind-student
GET    /api/family/members
DELETE /api/family/unbind
```

### 具体步骤

```text
1. parent 生成 6 位绑定码，有效期第一版可不强制过期，但状态必须可撤销。
2. student 输入绑定码确认绑定。
3. 写入 consent_records：parent_view_student_summary agreed=1。
4. family members 返回摘要，不返回学生自由文本原文。
5. DELETE unbind 将 status 改为 revoked，不硬删。
6. 写 audit_logs。
7. 新增 Web FamilyBindPage。
8. 补测试。
```

### 测试命令

```powershell
cd backend
python -m pytest tests/test_family_route.py tests/test_role_permissions.py -q
cd ..\apps\web
npm run build
```

### 验收标准

```text
1. parent 可生成绑定码。
2. student 可确认绑定。
3. parent 不能单方面绑定学生。
4. parent 默认看不到学生原始文本。
5. 可撤销绑定。
6. 绑定和撤销写审计。
```

### 回滚方式

停用 family blueprint 和前端页面；保留 family_links 表，不删除历史绑定记录。

---

# 6.6-P6：模型与规则治理

## 6.6-P6-01：模型版本与低置信度兜底

### 状态

已完成（profile_service.py 已有 model_version/confidence/fallback）

### 目标

让学生画像结果明确记录模型版本、规则版本、低置信度兜底和模型失败时的安全输出。

### 允许修改

```text
backend/services/profile_service.py
backend/routes/profile.py
backend/tests/test_profile_flow.py
content/student_profile_rules.json
docs/03_技术真相/API接口文档.md
docs/03_技术真相/数据库字段说明.md
docs/模型版本与画像兜底说明.md
docs/00_当前事实基准/开发日志.md
docs/00_当前事实基准/当前进度交接.md
docs/00_当前事实基准/开发说明.md
```

### 数据库变更

优先复用现有字段：

```text
student_profiles.model_version
student_profiles.rules_version
student_profiles.confidence
student_profiles.requires_review
student_profiles.boundary_notice
student_profiles.data_quality
```

无新增表。

### API 变更

`POST /api/profile` 和 `GET /api/profile-results/<id>` 返回中必须包含：

```text
model_version
rules_version
confidence
requires_review
boundary_notice
low_confidence_reason
fallback_used
```

### 具体步骤

```text
1. 在 profile_service 中集中定义 PROFILE_MODEL_VERSION 和 PROFILE_RULES_VERSION。
2. confidence < 0.7 时 requires_review=1。
3. 低置信度时 profile_name 使用“继续观察型支持画像”或等效弱化表达。
4. 低置信度不输出强标签，不推荐复杂训练。
5. 模型/规则异常时返回 fallback_used=true 的支持性兜底，不崩溃。
6. 文档说明画像不是诊断，不做人格定性。
7. 补测试。
```

### 测试命令

```powershell
cd backend
python -m pytest tests/test_profile_flow.py -q
```

### 验收标准

```text
1. 画像结果含 model_version 和 rules_version。
2. 低置信度弱化展示并进入人工复核。
3. 异常兜底不生成诊断化表达。
4. high 风险仍不推荐普通训练卡。
```

### 回滚方式

恢复 profile_service 规则输出逻辑。

---

## 6.6-P6-02：反馈规则版本化

### 状态

已完成（feedback_rules.json 规则已有 review_status/version 字段）

### 目标

让正式反馈规则具备版本、来源、风险等级、审核状态和后台展示。

### 允许修改

```text
content/feedback_rules.json
backend/scripts/validate_content.py
backend/tests/test_content_validation.py
apps/web/src/pages/RulesManagement.tsx
docs/非诊断反馈规则库说明.md
docs/03_技术真相/API接口文档.md
docs/00_当前事实基准/开发日志.md
docs/00_当前事实基准/当前进度交接.md
docs/00_当前事实基准/开发说明.md
```

### 数据库变更

无。

### API 变更

无新增接口。`/content/rules` 后台展示字段增强。

### 具体步骤

```text
1. 每条 feedback rule 必须有 version、theory_source、risk_level、review_status、enabled。
2. validate_content.py 增加字段校验。
3. RulesManagement 显示版本、来源、风险等级、审核状态。
4. high 风险规则不得有普通 recommended_card_ids。
5. 文档说明反馈规则不是诊断。
```

### 测试命令

```powershell
python backend\scripts\validate_content.py
cd backend
python -m pytest tests/test_content_validation.py -q
cd ..\apps\web
npm run build
```

### 验收标准

```text
1. content 校验通过。
2. 后台可读版本和审核状态。
3. 规则文案非诊断、非标签化。
```

### 回滚方式

恢复 feedback_rules.json 和 RulesManagement 本任务改动。

---

# 6.6-P7：正式试点上线准备

## 6.6-P7-01：正式试点上线检查清单

### 状态

已完成（正式试点上线检查清单.md 已创建）

### 目标

建立正式试点上线前的可执行检查清单。只写文档和检查命令，不部署、不备案、不发布。

### 允许修改

```text
docs/02_专项进度与验收/正式试点上线检查清单.md
docs/04_部署联调/服务器与数据库部署检查清单.md
docs/04_部署联调/小程序云托管调试说明.md
docs/04_部署联调/数据库备份与恢复说明.md
docs/06_产品规划/后续上线措施.md
docs/00_当前事实基准/开发日志.md
docs/00_当前事实基准/当前进度交接.md
docs/00_当前事实基准/开发说明.md
```

### 禁止修改

```text
不修改真实 .env。
不执行部署。
不上传小程序。
不改 CloudBase 控制台。
不创建真实域名或备案。
```

### 具体步骤

```text
1. 新增 docs/02_专项进度与验收/正式试点上线检查清单.md。
2. 列出 APP_ENV=production 必填环境变量。
3. 列出 ADMIN_EXPORT_TOKEN、ALLOWED_ORIGINS、DATABASE_PATH、SECRET_KEY 检查项。
4. 列出 CloudBase env/service/path 检查项。
5. 列出 /healthz 和 /healthz/deep 验证命令。
6. 列出 SQLite 备份和恢复演练命令。
7. 列出 Web build 和小程序 JS/JSON 检查命令。
8. 列出数据导出脱敏抽查。
9. 列出正式发布前人工伦理文案抽查。
10. 列出回滚策略。
```

### 测试命令

```powershell
.\scripts\check_all.ps1
python backend\scripts\check_sqlite_ops.py
```

### 验收标准

```text
1. 检查清单能交给零基础负责人逐项执行。
2. 不包含真实 token。
3. 不承诺自动完成备案或发布。
4. 明确上线仍需人工确认。
```

### 回滚方式

删除或恢复本任务新增文档。

---

## 5. 阶段推荐顺序

人工验收后建议按以下顺序执行：

```text
第零阶段：先执行 docs/08_已完成整改记录/6.6日问题解决逐步清单.md 中 6.6-FIX-P0/P1。
第一阶段：6.6-P0-01 -> 6.6-P0-02
第二阶段：6.6-P1-01 -> 6.6-P1-02
第三阶段：6.6-P2-01 -> 6.6-P2-02
第四阶段：6.6-P3-01
第五阶段：6.6-P4-01 -> 6.6-P4-02
第六阶段：6.6-P5-01
第七阶段：6.6-P6-01 -> 6.6-P6-02
第八阶段：6.6-P7-01
```

如果目标是尽快进入正式试点，最小顺序：

```text
6.6-FIX-P0-01
6.6-FIX-P0-02
6.6-FIX-P0-03
6.6-FIX-P1-01
6.6-FIX-P1-02
6.6-P0-01
6.6-P0-02
6.6-P2-01
6.6-P3-01
6.6-P4-01
6.6-P7-01
```

如果目标是继续小样本优化，最小顺序：

```text
6.6-FIX-P0-01
6.6-FIX-P0-02
6.6-FIX-P1-01
6.6-FIX-P1-02
6.6-P0-01
6.6-P0-02
6.6-P1-01
6.6-P1-02
```

---

## 6. Codex 单任务启动提示词模板

```text
请继续 safehome1.0 项目，项目路径为 D:\codex\workspace\safehome1.0。

请执行 docs/08_已完成整改记录/6.6增强问题逐步开发清单.md 中的任务【填写任务编号】。

要求：
1. 只执行这一个任务编号。
2. 先阅读 docs/00_当前事实基准/项目进度统一口径.md、docs/00_当前事实基准/当前进度交接.md、docs/6.5日问题诊断与整改方案.md、docs/08_已完成整改记录/6.6日问题解决逐步清单.md、docs/03_技术真相/项目架构边界与后续开发规则.md 和本任务全文。
3. 先判断任务状态：已完成 / 部分完成 / 未完成 / 需人工确认 / 暂缓。
4. 已完成只记录证据，不重复开发。
5. 部分完成只补缺口。
6. 不删除匿名 user_id 试用模式。
7. 不接入 AI 自由咨询、临床诊断、医疗级危机干预、微信授权、短信验证码或支付。
8. 修改后运行任务指定测试。
9. 结束前更新 docs/00_当前事实基准/开发日志.md、docs/00_当前事实基准/当前进度交接.md、docs/00_当前事实基准/开发说明.md。
10. 输出修改文件、测试结果、遗留问题和下一步建议。
```

---

## 7. 每轮完成汇报格式

```markdown
## 本次完成任务

任务编号：
任务名称：

## 状态判断

已完成 / 部分完成 / 未完成 / 需人工确认 / 暂缓

## 修改文件

- 文件：

## 新增文件

- 文件：

## 数据库变更

- 无 / 表和字段：

## API 变更

- 无 / METHOD PATH：

## 本地验证

- 命令：
- 结果：

## 验收结果

- [ ] 验收项

## 遗留问题

- 无 / 说明：

## 下一步建议

建议执行：
```

