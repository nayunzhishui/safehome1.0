# 任务十七内容基线审计报告

更新时间：2026-07-11

## 1. 审计范围与复现方式

范围：`content/training_cards.json`、`content/courses.json`、`content/programs.json`、`content/assessment_training_map.json`。

执行：

```powershell
python backend/scripts/audit_task17_content.py
python -m pytest backend/tests/test_task17_content_audit.py -q
```

机器可读结果：`outputs/task17_content_baseline.json`。脚本不修改内容，不批准心理或研究结论；相同输入生成相同核心结果。

## 2. 基线结论

| 内容 | 数量 | 当前判断 |
|---|---:|---|
| 训练卡 | 34 | 34张均启用；需补治理字段和逐卡心理审核 |
| 课程 | 5 | 均为3段式微内容，缺完整学习单元字段 |
| 项目 | 3 | 均为 `pilot_draft`，测量方案待研究审核 |
| 测评训练映射 | 32 | 引用均有效，规则仍为草案治理状态 |

## 3. 自动可修问题

1. 20张训练卡含重复标签。
2. 7张训练卡含三个英文句点形式的残缺文本：`body_scan_before_study`、`cbt_auto_thought_student`、`parent_body_grounding`、`prfq_ic_curiosity`、`prfq_pm_awareness`、`rsca_emotion_regulation`、`sandplay_expression_01`。
3. 3张用户标题含内部缩写：`prfq_cm_tolerance`、`prfq_ic_curiosity`、`prfq_pm_awareness`。
4. 当前不存在无效训练卡引用。
5. 5张卡暂未被课程、项目或测评映射引用：`alternative_behavior`、`erq_reappraisal_student`、`parent_body_signal_check`、`sandplay_expression_01`、`student_exam_body_map`。未引用不等于应删除，需在任务十七中决定映射或保持独立入口。

## 4. 心理与研究审核问题

1. 34张训练卡均缺任务十七定义的完整机制、剂量、禁用、停止、完成、进阶、执行核对、结果关联和证据等级字段。
2. 5门课程均缺学习目标、核心概念、常见误区、正反例、理解检查、引导练习、迁移任务、复盘、巩固和分众字段。
3. 三个项目均缺方案版本、纳排、暂停/退出、最小剂量、完成定义、负面体验、偏离处理和三类签字字段。
4. 自动机制初分仅用于发现覆盖缺口，不作为心理学结论；具体机制必须由心理负责人逐卡核对。

## 5. 下一步

按 `Claude计划模式.md` 依次执行 T17-01 至 T17-09。先修复确定性内容问题，再补结构和前后端能力；研究签字前不得把项目升级为 `pilot_approved`。
