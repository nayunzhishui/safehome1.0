# 任务九数据库字段检查表

<!-- task9_database_field_check -->

生成日期：2026-07-02

来源：`backend/models.py` 的 `SCHEMA_SQL`。

说明：本表用于任务九 T9-14/T9-15 审查。当前 schema 以服务层关联校验为主，未强制补加数据库外键，避免破坏既有 SQLite/MySQL 兼容数据和历史记录。

## 汇总

- 表数量：25
- 字段数量：298

## 字段明细

### users

| 字段 | 定义 | 主键 | 必填 | 关联提示 |
|---|---|---|---|---|
| id | `TEXT PRIMARY KEY` | 是 | 是 |  |
| nickname | `TEXT` | 否 | 否 |  |
| role | `TEXT DEFAULT 'parent'` | 否 | 否 |  |
| source | `TEXT` | 否 | 否 |  |
| wechat_openid | `TEXT` | 否 | 否 |  |
| avatar_url | `TEXT` | 否 | 否 |  |
| created_at | `TEXT NOT NULL` | 否 | 是 |  |
| updated_at | `TEXT NOT NULL` | 否 | 是 |  |

### privacy_requests

| 字段 | 定义 | 主键 | 必填 | 关联提示 |
|---|---|---|---|---|
| id | `TEXT PRIMARY KEY` | 是 | 是 |  |
| user_id | `TEXT NOT NULL` | 否 | 是 | users.id |
| request_type | `TEXT NOT NULL` | 否 | 是 |  |
| reason | `TEXT` | 否 | 否 |  |
| status | `TEXT NOT NULL DEFAULT 'pending'` | 否 | 是 |  |
| handled_by | `TEXT` | 否 | 否 |  |
| handled_note | `TEXT` | 否 | 否 |  |
| created_at | `TEXT NOT NULL` | 否 | 是 |  |
| updated_at | `TEXT NOT NULL` | 否 | 是 |  |

### family_links

| 字段 | 定义 | 主键 | 必填 | 关联提示 |
|---|---|---|---|---|
| id | `TEXT PRIMARY KEY` | 是 | 是 |  |
| parent_user_id | `TEXT NOT NULL` | 否 | 是 | users.id |
| student_user_id | `TEXT` | 否 | 否 | users.id |
| bind_code | `TEXT NOT NULL` | 否 | 是 |  |
| relation_label | `TEXT` | 否 | 否 |  |
| status | `TEXT NOT NULL DEFAULT 'pending'` | 否 | 是 |  |
| expires_at | `TEXT` | 否 | 否 |  |
| attempt_count | `INTEGER NOT NULL DEFAULT 0` | 否 | 是 |  |
| last_attempt_at | `TEXT` | 否 | 否 |  |
| created_at | `TEXT NOT NULL` | 否 | 是 |  |
| updated_at | `TEXT NOT NULL` | 否 | 是 |  |
| confirmed_at | `TEXT` | 否 | 否 |  |
| revoked_at | `TEXT` | 否 | 否 |  |

### schema_migrations

| 字段 | 定义 | 主键 | 必填 | 关联提示 |
|---|---|---|---|---|
| version | `TEXT PRIMARY KEY` | 是 | 是 |  |
| name | `TEXT NOT NULL` | 否 | 是 |  |
| applied_at | `TEXT NOT NULL` | 否 | 是 |  |

### goals

| 字段 | 定义 | 主键 | 必填 | 关联提示 |
|---|---|---|---|---|
| id | `TEXT PRIMARY KEY` | 是 | 是 |  |
| user_id | `TEXT NOT NULL` | 否 | 是 | users.id |
| scene | `TEXT NOT NULL` | 否 | 是 |  |
| smart_goal | `TEXT NOT NULL` | 否 | 是 |  |
| motivation | `TEXT` | 否 | 否 |  |
| start_date | `TEXT` | 否 | 否 |  |
| status | `TEXT NOT NULL DEFAULT 'active'` | 否 | 是 |  |
| created_at | `TEXT NOT NULL` | 否 | 是 |  |
| updated_at | `TEXT NOT NULL` | 否 | 是 |  |

### emotion_diaries

| 字段 | 定义 | 主键 | 必填 | 关联提示 |
|---|---|---|---|---|
| id | `TEXT PRIMARY KEY` | 是 | 是 |  |
| user_id | `TEXT NOT NULL` | 否 | 是 | users.id |
| goal_id | `TEXT` | 否 | 否 | goals.id |
| event_time | `TEXT` | 否 | 否 |  |
| scene | `TEXT NOT NULL` | 否 | 是 |  |
| event_description | `TEXT NOT NULL` | 否 | 是 |  |
| parent_emotion | `TEXT NOT NULL` | 否 | 是 |  |
| parent_emotion_intensity | `INTEGER NOT NULL` | 否 | 是 |  |
| child_emotion | `TEXT` | 否 | 否 |  |
| child_emotion_intensity | `INTEGER` | 否 | 否 |  |
| automatic_thought | `TEXT` | 否 | 否 |  |
| body_sensation | `TEXT` | 否 | 否 |  |
| behavior | `TEXT` | 否 | 否 |  |
| raw_text | `TEXT` | 否 | 否 |  |
| created_at | `TEXT NOT NULL` | 否 | 是 |  |
| updated_at | `TEXT NOT NULL` | 否 | 是 |  |

### emotion_thermometer

| 字段 | 定义 | 主键 | 必填 | 关联提示 |
|---|---|---|---|---|
| id | `TEXT PRIMARY KEY` | 是 | 是 |  |
| user_id | `TEXT NOT NULL` | 否 | 是 | users.id |
| intensity_level | `INTEGER NOT NULL` | 否 | 是 |  |
| brief_text | `TEXT` | 否 | 否 |  |
| created_at | `TEXT NOT NULL` | 否 | 是 |  |
| updated_at | `TEXT NOT NULL` | 否 | 是 |  |

### feedback_results

| 字段 | 定义 | 主键 | 必填 | 关联提示 |
|---|---|---|---|---|
| id | `TEXT PRIMARY KEY` | 是 | 是 |  |
| user_id | `TEXT` | 否 | 否 | users.id |
| diary_id | `TEXT` | 否 | 否 | emotion_diaries.id |
| tags_json | `TEXT NOT NULL` | 否 | 是 |  |
| trigger_summary | `TEXT` | 否 | 否 |  |
| pattern_summary | `TEXT` | 否 | 否 |  |
| supportive_feedback | `TEXT NOT NULL` | 否 | 是 |  |
| alternative_response | `TEXT` | 否 | 否 |  |
| recommended_card_ids_json | `TEXT NOT NULL` | 否 | 是 |  |
| risk_level | `TEXT NOT NULL DEFAULT 'low'` | 否 | 是 |  |
| created_at | `TEXT NOT NULL` | 否 | 是 |  |

### training_cards

| 字段 | 定义 | 主键 | 必填 | 关联提示 |
|---|---|---|---|---|
| id | `TEXT PRIMARY KEY` | 是 | 是 |  |
| type | `TEXT NOT NULL` | 否 | 是 |  |
| title | `TEXT NOT NULL` | 否 | 是 |  |
| purpose | `TEXT` | 否 | 否 |  |
| steps_json | `TEXT NOT NULL` | 否 | 是 |  |
| tags_json | `TEXT NOT NULL` | 否 | 是 |  |
| example | `TEXT` | 否 | 否 |  |
| duration_minutes | `INTEGER` | 否 | 否 |  |
| enabled | `INTEGER NOT NULL DEFAULT 1` | 否 | 是 |  |
| version | `TEXT` | 否 | 否 |  |
| created_at | `TEXT NOT NULL` | 否 | 是 |  |
| updated_at | `TEXT NOT NULL` | 否 | 是 |  |

### checkins

| 字段 | 定义 | 主键 | 必填 | 关联提示 |
|---|---|---|---|---|
| id | `TEXT PRIMARY KEY` | 是 | 是 |  |
| user_id | `TEXT NOT NULL` | 否 | 是 | users.id |
| card_id | `TEXT NOT NULL` | 否 | 是 | training_cards.id |
| diary_id | `TEXT` | 否 | 否 | emotion_diaries.id |
| completed | `INTEGER NOT NULL` | 否 | 是 |  |
| emotion_before | `INTEGER` | 否 | 否 |  |
| emotion_after | `INTEGER` | 否 | 否 |  |
| reflection | `TEXT` | 否 | 否 |  |
| created_at | `TEXT NOT NULL` | 否 | 是 |  |

### assessment_worksheets

| 字段 | 定义 | 主键 | 必填 | 关联提示 |
|---|---|---|---|---|
| id | `TEXT PRIMARY KEY` | 是 | 是 |  |
| display_title | `TEXT NOT NULL` | 否 | 是 |  |
| source_title | `TEXT` | 否 | 否 |  |
| source_file | `TEXT` | 否 | 否 |  |
| category | `TEXT` | 否 | 否 |  |
| audience_class | `TEXT` | 否 | 否 |  |
| reflex_node | `TEXT` | 否 | 否 |  |
| questions_json | `TEXT NOT NULL DEFAULT '[]'` | 否 | 是 |  |
| dimensions_json | `TEXT NOT NULL DEFAULT '[]'` | 否 | 是 |  |
| dimension_score_method | `TEXT NOT NULL DEFAULT 'sum'` | 否 | 是 |  |
| scoring_notes_json | `TEXT NOT NULL DEFAULT '{}'` | 否 | 是 |  |
| search_keywords_json | `TEXT NOT NULL DEFAULT '[]'` | 否 | 是 |  |
| boundary_notice | `TEXT` | 否 | 否 |  |
| result_disclaimer | `TEXT` | 否 | 否 |  |
| instructions | `TEXT` | 否 | 否 |  |
| sensitive_category | `TEXT NOT NULL DEFAULT 'none'` | 否 | 是 |  |
| profile_model_id | `TEXT` | 否 | 否 |  |
| enabled_for_user | `INTEGER NOT NULL DEFAULT 1` | 否 | 是 |  |
| review_status | `TEXT NOT NULL DEFAULT 'approved'` | 否 | 是 |  |
| review_note | `TEXT` | 否 | 否 |  |
| source_version | `TEXT` | 否 | 否 |  |
| source_type | `TEXT` | 否 | 否 |  |
| audience | `TEXT` | 否 | 否 |  |
| audience_class_detail | `TEXT` | 否 | 否 |  |
| recommended_card_ids_json | `TEXT NOT NULL DEFAULT '[]'` | 否 | 是 |  |
| sections_json | `TEXT NOT NULL DEFAULT '[]'` | 否 | 是 |  |
| scoring | `TEXT` | 否 | 否 |  |
| pages | `INTEGER` | 否 | 否 |  |
| _meta_json | `TEXT NOT NULL DEFAULT '{}'` | 否 | 是 |  |
| created_at | `TEXT NOT NULL` | 否 | 是 |  |
| updated_at | `TEXT NOT NULL` | 否 | 是 |  |

### assessment_results

| 字段 | 定义 | 主键 | 必填 | 关联提示 |
|---|---|---|---|---|
| id | `TEXT PRIMARY KEY` | 是 | 是 |  |
| user_id | `TEXT NOT NULL` | 否 | 是 | users.id |
| worksheet_id | `TEXT NOT NULL` | 否 | 是 | assessment_worksheets.id |
| worksheet_title | `TEXT NOT NULL` | 否 | 是 |  |
| category | `TEXT` | 否 | 否 |  |
| answers_json | `TEXT NOT NULL` | 否 | 是 |  |
| scores_json | `TEXT NOT NULL` | 否 | 是 |  |
| total_score | `INTEGER` | 否 | 否 |  |
| result_summary | `TEXT` | 否 | 否 |  |
| profile_model_id | `TEXT` | 否 | 否 |  |
| profile_cluster_id | `INTEGER` | 否 | 否 |  |
| profile_pc1 | `REAL` | 否 | 否 |  |
| profile_pc2 | `REAL` | 否 | 否 |  |
| profile_confidence | `REAL` | 否 | 否 |  |
| created_at | `TEXT NOT NULL` | 否 | 是 |  |

### student_profiles

| 字段 | 定义 | 主键 | 必填 | 关联提示 |
|---|---|---|---|---|
| id | `TEXT PRIMARY KEY` | 是 | 是 |  |
| user_id | `TEXT NOT NULL` | 否 | 是 | users.id |
| anonymous_id | `TEXT NOT NULL` | 否 | 是 |  |
| assessment_result_id | `TEXT` | 否 | 否 | assessment_results.id |
| round | `INTEGER NOT NULL DEFAULT 1` | 否 | 是 |  |
| source | `TEXT` | 否 | 否 |  |
| scores_json | `TEXT NOT NULL` | 否 | 是 |  |
| text_features_json | `TEXT NOT NULL` | 否 | 是 |  |
| profile_code | `TEXT NOT NULL` | 否 | 是 |  |
| profile_name | `TEXT NOT NULL` | 否 | 是 |  |
| confidence | `REAL` | 否 | 否 |  |
| dimensions_json | `TEXT NOT NULL` | 否 | 是 |  |
| recommended_task_ids_json | `TEXT NOT NULL` | 否 | 是 |  |
| risk_level | `TEXT NOT NULL DEFAULT 'low'` | 否 | 是 |  |
| requires_review | `INTEGER NOT NULL DEFAULT 0` | 否 | 是 |  |
| boundary_notice | `TEXT` | 否 | 否 |  |
| rules_version | `TEXT` | 否 | 否 |  |
| model_version | `TEXT` | 否 | 否 |  |
| model_type | `TEXT` | 否 | 否 |  |
| cluster_id | `INTEGER` | 否 | 否 |  |
| pc1 | `REAL` | 否 | 否 |  |
| pc2 | `REAL` | 否 | 否 |  |
| nearest_distance | `REAL` | 否 | 否 |  |
| second_distance | `REAL` | 否 | 否 |  |
| report_json | `TEXT NOT NULL DEFAULT '{}'` | 否 | 是 |  |
| visuals_json | `TEXT NOT NULL DEFAULT '{}'` | 否 | 是 |  |
| legacy_source_id | `TEXT` | 否 | 否 |  |
| legacy_source_table | `TEXT` | 否 | 否 |  |
| export_allowed | `INTEGER NOT NULL DEFAULT 1` | 否 | 是 |  |
| data_quality | `TEXT` | 否 | 否 |  |
| created_at | `TEXT NOT NULL` | 否 | 是 |  |
| updated_at | `TEXT NOT NULL` | 否 | 是 |  |

### student_profile_followups

| 字段 | 定义 | 主键 | 必填 | 关联提示 |
|---|---|---|---|---|
| id | `TEXT PRIMARY KEY` | 是 | 是 |  |
| profile_id | `TEXT NOT NULL` | 否 | 是 | student_profiles.id |
| user_id | `TEXT NOT NULL` | 否 | 是 | users.id |
| round_no | `INTEGER NOT NULL` | 否 | 是 |  |
| fit | `TEXT` | 否 | 否 |  |
| task_done | `TEXT` | 否 | 否 |  |
| state_score | `INTEGER` | 否 | 否 |  |
| text | `TEXT` | 否 | 否 |  |
| keywords_json | `TEXT NOT NULL DEFAULT '[]'` | 否 | 是 |  |
| created_at | `TEXT NOT NULL` | 否 | 是 |  |
| export_allowed | `INTEGER NOT NULL DEFAULT 1` | 否 | 是 |  |

### student_sandplay_entries

| 字段 | 定义 | 主键 | 必填 | 关联提示 |
|---|---|---|---|---|
| id | `TEXT PRIMARY KEY` | 是 | 是 |  |
| profile_id | `TEXT NOT NULL` | 否 | 是 | student_profiles.id |
| user_id | `TEXT NOT NULL` | 否 | 是 | users.id |
| task_title | `TEXT` | 否 | 否 |  |
| scene_json | `TEXT NOT NULL` | 否 | 是 |  |
| reflection_text | `TEXT` | 否 | 否 |  |
| summary_json | `TEXT NOT NULL DEFAULT '{}'` | 否 | 是 |  |
| created_at | `TEXT NOT NULL` | 否 | 是 |  |
| export_allowed | `INTEGER NOT NULL DEFAULT 1` | 否 | 是 |  |

### parent_assessment_submissions

| 字段 | 定义 | 主键 | 必填 | 关联提示 |
|---|---|---|---|---|
| id | `TEXT PRIMARY KEY` | 是 | 是 |  |
| user_id | `TEXT NOT NULL` | 否 | 是 | users.id |
| anonymous_id | `TEXT NOT NULL` | 否 | 是 |  |
| participant_code | `TEXT` | 否 | 否 |  |
| research_consent | `INTEGER NOT NULL DEFAULT 0` | 否 | 是 |  |
| study_batch | `TEXT` | 否 | 否 |  |
| source_channel | `TEXT` | 否 | 否 |  |
| questionnaire_version | `TEXT` | 否 | 否 |  |
| scoring_version | `TEXT` | 否 | 否 |  |
| answers_json | `TEXT NOT NULL` | 否 | 是 |  |
| scores_json | `TEXT NOT NULL` | 否 | 是 |  |
| profile_key | `TEXT` | 否 | 否 |  |
| report_json | `TEXT NOT NULL` | 否 | 是 |  |
| started_at | `TEXT` | 否 | 否 |  |
| completed_at | `TEXT` | 否 | 否 |  |
| duration_seconds | `INTEGER NOT NULL DEFAULT 0` | 否 | 是 |  |
| quality_flags_json | `TEXT NOT NULL DEFAULT '{}'` | 否 | 是 |  |
| legacy_source_id | `TEXT` | 否 | 否 |  |
| legacy_source_table | `TEXT` | 否 | 否 |  |
| export_allowed | `INTEGER NOT NULL DEFAULT 1` | 否 | 是 |  |
| created_at | `TEXT NOT NULL` | 否 | 是 |  |
| updated_at | `TEXT NOT NULL` | 否 | 是 |  |

### parent_report_actions

| 字段 | 定义 | 主键 | 必填 | 关联提示 |
|---|---|---|---|---|
| id | `TEXT PRIMARY KEY` | 是 | 是 |  |
| submission_id | `TEXT NOT NULL` | 否 | 是 |  |
| action_key | `TEXT NOT NULL` | 否 | 是 |  |
| created_at | `TEXT NOT NULL` | 否 | 是 |  |

### profile_reviews

| 字段 | 定义 | 主键 | 必填 | 关联提示 |
|---|---|---|---|---|
| id | `TEXT PRIMARY KEY` | 是 | 是 |  |
| profile_id | `TEXT NOT NULL` | 否 | 是 | student_profiles.id |
| reviewer_id | `TEXT` | 否 | 否 |  |
| review_status | `TEXT NOT NULL DEFAULT 'reviewed'` | 否 | 是 |  |
| review_decision | `TEXT` | 否 | 否 |  |
| note | `TEXT` | 否 | 否 |  |
| action_summary | `TEXT` | 否 | 否 |  |
| visible_to_student | `INTEGER NOT NULL DEFAULT 0` | 否 | 是 |  |
| created_at | `TEXT NOT NULL` | 否 | 是 |  |
| updated_at | `TEXT NOT NULL` | 否 | 是 |  |

### records

| 字段 | 定义 | 主键 | 必填 | 关联提示 |
|---|---|---|---|---|
| id | `TEXT PRIMARY KEY` | 是 | 是 |  |
| user_id | `TEXT NOT NULL` | 否 | 是 | users.id |
| module_type | `TEXT NOT NULL` | 否 | 是 |  |
| source_id | `TEXT` | 否 | 否 | source table by source_type |
| data_json | `TEXT NOT NULL` | 否 | 是 |  |
| created_at | `TEXT NOT NULL` | 否 | 是 |  |
| updated_at | `TEXT NOT NULL` | 否 | 是 |  |
| export_allowed | `INTEGER NOT NULL DEFAULT 1` | 否 | 是 |  |

### audit_logs

| 字段 | 定义 | 主键 | 必填 | 关联提示 |
|---|---|---|---|---|
| id | `TEXT PRIMARY KEY` | 是 | 是 |  |
| actor_id | `TEXT` | 否 | 否 |  |
| action | `TEXT NOT NULL` | 否 | 是 |  |
| target_type | `TEXT` | 否 | 否 |  |
| target_id | `TEXT` | 否 | 否 | target table by target_type |
| metadata_json | `TEXT NOT NULL` | 否 | 是 |  |
| created_at | `TEXT NOT NULL` | 否 | 是 |  |

### consent_records

| 字段 | 定义 | 主键 | 必填 | 关联提示 |
|---|---|---|---|---|
| id | `TEXT PRIMARY KEY` | 是 | 是 |  |
| user_id | `TEXT NOT NULL` | 否 | 是 | users.id |
| consent_type | `TEXT NOT NULL` | 否 | 是 |  |
| consent_version | `TEXT NOT NULL` | 否 | 是 |  |
| agreed | `INTEGER NOT NULL` | 否 | 是 |  |
| agreed_at | `TEXT NOT NULL` | 否 | 是 |  |
| revoked_at | `TEXT` | 否 | 否 |  |
| created_at | `TEXT NOT NULL` | 否 | 是 |  |

### risk_review_records

| 字段 | 定义 | 主键 | 必填 | 关联提示 |
|---|---|---|---|---|
| id | `TEXT PRIMARY KEY` | 是 | 是 |  |
| user_id | `TEXT NOT NULL` | 否 | 是 | users.id |
| source_type | `TEXT NOT NULL` | 否 | 是 |  |
| source_id | `TEXT NOT NULL` | 否 | 是 | source table by source_type |
| risk_level | `TEXT NOT NULL` | 否 | 是 |  |
| matched_categories_json | `TEXT NOT NULL DEFAULT '[]'` | 否 | 是 |  |
| review_status | `TEXT NOT NULL DEFAULT 'pending'` | 否 | 是 |  |
| reviewer_id | `TEXT` | 否 | 否 |  |
| review_note | `TEXT` | 否 | 否 |  |
| action_taken | `TEXT` | 否 | 否 |  |
| closed_reason | `TEXT` | 否 | 否 |  |
| reviewed_at | `TEXT` | 否 | 否 |  |
| created_at | `TEXT NOT NULL` | 否 | 是 |  |
| updated_at | `TEXT NOT NULL` | 否 | 是 |  |

### weekly_reports

| 字段 | 定义 | 主键 | 必填 | 关联提示 |
|---|---|---|---|---|
| id | `TEXT PRIMARY KEY` | 是 | 是 |  |
| user_id | `TEXT NOT NULL` | 否 | 是 | users.id |
| week_start | `TEXT NOT NULL` | 否 | 是 |  |
| week_end | `TEXT NOT NULL` | 否 | 是 |  |
| frequent_scenes_json | `TEXT NOT NULL` | 否 | 是 |  |
| frequent_emotions_json | `TEXT NOT NULL` | 否 | 是 |  |
| common_patterns_json | `TEXT NOT NULL` | 否 | 是 |  |
| completed_cards_json | `TEXT NOT NULL` | 否 | 是 |  |
| next_week_suggestion | `TEXT` | 否 | 否 |  |
| created_at | `TEXT NOT NULL` | 否 | 是 |  |

### supervision_requests

| 字段 | 定义 | 主键 | 必填 | 关联提示 |
|---|---|---|---|---|
| id | `TEXT PRIMARY KEY` | 是 | 是 |  |
| user_id | `TEXT NOT NULL` | 否 | 是 | users.id |
| diary_id | `TEXT` | 否 | 否 | emotion_diaries.id |
| message | `TEXT NOT NULL` | 否 | 是 |  |
| contact | `TEXT` | 否 | 否 |  |
| risk_hint | `TEXT` | 否 | 否 |  |
| risk_level | `TEXT NOT NULL DEFAULT 'low'` | 否 | 是 |  |
| status | `TEXT NOT NULL DEFAULT 'pending'` | 否 | 是 |  |
| supervisor_reply | `TEXT` | 否 | 否 |  |
| created_at | `TEXT NOT NULL` | 否 | 是 |  |
| replied_at | `TEXT` | 否 | 否 |  |

### messages

| 字段 | 定义 | 主键 | 必填 | 关联提示 |
|---|---|---|---|---|
| id | `TEXT PRIMARY KEY` | 是 | 是 |  |
| user_id | `TEXT NOT NULL` | 否 | 是 | users.id |
| message_type | `TEXT NOT NULL` | 否 | 是 |  |
| title | `TEXT NOT NULL` | 否 | 是 |  |
| body | `TEXT` | 否 | 否 |  |
| source_type | `TEXT` | 否 | 否 |  |
| source_id | `TEXT` | 否 | 否 | source table by source_type |
| status | `TEXT NOT NULL DEFAULT 'unread'` | 否 | 是 |  |
| created_at | `TEXT NOT NULL` | 否 | 是 |  |
| read_at | `TEXT` | 否 | 否 |  |

