# Claude计划模式任务九执行记录

执行开始日期：2026-07-02

执行分支：`codex/task9-code-review`

执行范围：`docs/00_当前事实基准/Claude计划模式.md` 中任务九 `T9-01` 至 `T9-31`。

执行原则：

```text
1. 先审查，再修改；只修复有明确证据的问题。
2. 每个小任务都记录审查范围、证据、发现、修改、验证。
3. 不删除小程序 pages/integration-test/index。
4. 不破坏“情绪记录 -> 支持性反馈 -> 推荐训练卡 -> 打卡”核心链路。
5. 涉及代码、配置、内容库或文档改动时，同步当前事实基准三份文档。
```

## 1. 项目结构总览

```text
backend/：Flask 后端、数据库访问、路由、服务、测试。
apps/miniprogram/：微信小程序原生页面、组件、服务层和 CloudBase 调用配置。
apps/web/：React/Vite 网站端和后台。
shared/：前后端共享 API 常量、类型和 mock 数据。
content/：训练卡、测评、画像、项目内容和 JSON schema。
docs/：当前事实基准、专项验收、技术真相、部署联调和历史归档。
scripts/：跨模块检查、文档生成和专项验收脚本。
analysis/：离线研究原型和画像分析脚本，不进入后端运行时。
```

## 2. 任务九状态总表

状态说明：

```text
未开始：尚未审查。
审查中：已开始读取证据，但未完成结论。
已修复：发现并完成明确修复。
无需修改：完成审查，未发现需要修改的问题。
暂缓：需要人工环境、真实云端、真机或外部条件。
```

| 编号 | 主题 | 状态 | 本轮证据/结论 |
|---|---|---|---|
| T9-01 | 小程序页面、组件、配置和请求层分工审查 | 已修复 | `api.js` 已集中接口、身份和环境分支；本轮补强请求层错误对象 |
| T9-02 | 小程序失败态、空态和用户可见错误审查 | 已修复 | 修复 `program-list`、`program-detail`、`personalized-plan`、`thermometer` 加载失败只 toast 的问题 |
| T9-03 | 小程序 page / component / service 职责边界审查 | 已修复 | 页面保留状态与重试；服务层负责错误转换；组件未新增请求逻辑 |
| T9-04 | 小程序服务层和请求封装审查 | 已修复 | `api.js` 统一 `baseURL`、CloudBase、本地分支、token 注入和错误转换 |
| T9-05 | 后端错误到小程序页面错误结构转换审查 | 已修复 | `api.js` 不再把后端原始 message 直接作为用户可见 message |
| T9-06 | 小程序上线后普通用户可打开页面联调审查 | 已修复 | 验收脚本检查 app.json 所有页面三件套存在；已补部分普通页面加载失败持久提示；真机/微信开发者工具仍作为人工验收项 |
| T9-07 | 小程序身份、token、401/403/404/500 处理审查 | 已修复 | `api.js` 对 401 统一清理 `auth_token`、`auth_user` 和 `globalData` |
| T9-08 | 小程序用户提示语义审查 | 已修复 | `api.js` 按 code/status 返回安全用户提示和 `retryable` |
| T9-09 | 小程序 api.js 业务入口命名与封装审查 | 无需修改 | 页面主要调用 `createGoal/listPrograms/getTrainingPlan` 等业务方法，未发现页面自行拼接通用请求 |
| T9-10 | 小程序请求分支和环境定位审查 | 无需修改 | `getDebugConfig` 返回 env/service/transport/path，debug 页可定位本地或云托管 |
| T9-11 | 小程序响应处理统一性审查 | 已修复 | 成功统一返回 `data`，失败统一抛出含 `code/message/retryable/status` 的 error |
| T9-12 | CloudBase 云托管调用链审查 | 无需修改 | `wx.cloud.init` 与 `callContainer` 使用集中配置；健康检查入口为 `/healthz` |
| T9-13 | 数据库持久化边界和数据归属审查 | 无需修改 | 核心数据已落库，`database.py` 暴露连接、schema、查询和审计工具；后续继续补字段表 |
| T9-14 | 数据库字段稳定性和字段说明审查 | 已修复 | 已生成 `任务九数据库字段检查表_20260702.md`，字段来源为 `models.py` 的 `SCHEMA_SQL` |
| T9-15 | 数据库主键、外键和关联关系审查 | 已修复 | 字段检查表包含主键和关联提示；当前多为服务层关联校验，未强制新增外键，避免破坏既有 SQLite/MySQL 兼容数据 |
| T9-16 | 数据库事务安全审查 | 无需修改 | `with get_connection()` 在 MySQL adapter 中自动 commit/rollback；核心写入仍显式 commit |
| T9-17 | SQLite 连接生命周期和参数化查询审查 | 无需修改 | 连接按 `with get_connection()` 使用；动态表/列名已有本地正则或内部白名单 |
| T9-18 | schema 初始化脚本幂等性审查 | 无需修改 | `init_db()` 使用 `CREATE TABLE IF NOT EXISTS`、`ensure_column` 和 schema migration 幂等测试 |
| T9-19 | MySQL、编码、健康检查和备份策略审查 | 暂缓 | 自动项已完成；本地 SQLite 备份恢复演练通过；CloudBase `/healthz/deep` 显示 MySQL 可用；`/readyz` 仍需发布任务九新版本 |
| T9-20 | 审计日志、迁移留痕和端到端编码验证审查 | 已修复 | 新增 `test_text_encoding_roundtrip.py` 验证中文/英文/数字/标点/emoji 写入、DB 读取和 API 返回 |
| T9-21 | Flask 路由拆分、响应结构和中文 JSON 审查 | 已修复 | `SafeHomeJSONProvider.ensure_ascii=False` 已存在；本轮补全全局 JSON 异常响应 |
| T9-22 | Flask 应用工厂和测试配置审查 | 已修复 | `create_app` 新增 `config_overrides` 和 `init_database` 参数，并有测试覆盖 |
| T9-23 | Blueprint、service、database 分层审查 | 无需修改 | `app.py` 注册 blueprint，业务在 routes/services，数据库在 database；未发现需立即迁移的新增 SQL |
| T9-24 | CORS、healthz/readyz 和错误码稳定性审查 | 已修复 | 新增独立 `/readyz`，未知路由和未处理异常返回稳定 JSON code |
| T9-25 | 后端入口装配轻量化审查 | 已修复 | `app.py` 保持装配入口，新增 readiness builder 和 logging 配置，不放业务 SQL |
| T9-26 | argparse 脚本 main 函数边界审查 | 无需修改 | 重点脚本已有 `main()`；复杂业务函数可直接测试，如 `build_worksheets`、`validate_content` |
| T9-27 | 项目结构、依赖方向和内容库组织审查 | 无需修改 | 项目结构清楚；验收脚本检查核心入口和已跟踪运行产物；当前工作区既有 zip/sqlite 产物需提交前人工处理 |
| T9-28 | requirements 和依赖文件审查 | 无需修改 | `backend/requirements.txt` 覆盖 Flask、pytest、gunicorn、PyMySQL；Web 依赖在 package 文件 |
| T9-29 | 环境变量和敏感配置审查 | 已修复 | `.env.example` 补充日志/微信占位；Web 后台令牌改为仅内存保存，不再写入 localStorage，也不保留本地默认管理员 token |
| T9-30 | logging 日志体系审查 | 已修复 | `app.py` 新增入口级 `logging.config.dictConfig`，支持 console 和可选 file handler |
| T9-31 | 异常追踪、错误码和用户可见错误审查 | 已修复 | 未处理异常记录 `logger.exception` 堆栈，用户只看到稳定 `internal_error` |

## 3. 验收工程

新增脚本：

```powershell
scripts/run_task9_review_checks.ps1
scripts/generate_task9_database_field_check.py
docs/02_专项进度与验收/任务九数据库字段检查表_20260702.md
```

基础用法：

```powershell
powershell -ExecutionPolicy Bypass -File scripts/run_task9_review_checks.ps1 -SkipHeavyChecks
```

完整用法：

```powershell
powershell -ExecutionPolicy Bypass -File scripts/run_task9_review_checks.ps1
```

## 4. 执行流水

### 2026-07-02：任务九启动与项目结构总览

已完成：

```text
1. 确认当前分支为 codex/task9-code-review。
2. 确认任务九计划存在 T9-01 至 T9-31 共 31 项。
3. 总览项目目录和关键职责边界。
4. 建立任务九执行记录和验收脚本。
```

验证命令：

```powershell
rg -n "^## T9-[0-9]{2}：" docs/00_当前事实基准/Claude计划模式.md
powershell -ExecutionPolicy Bypass -File scripts/run_task9_review_checks.ps1 -SkipHeavyChecks
```

### 2026-07-02：T9-01 至 T9-12 小程序请求层第一轮审查修复

审查范围：

```text
apps/miniprogram/app.js
apps/miniprogram/app.json
apps/miniprogram/services/api.js
apps/miniprogram/services/cloudConfig.js
apps/miniprogram/pages/program-list/*
apps/miniprogram/pages/program-detail/*
apps/miniprogram/pages/personalized-plan/*
apps/miniprogram/pages/thermometer/*
```

发现问题：

```text
1. api.js 在 HTTP 非 2xx 或 payload.ok=false 时，会把后端原始 error.message 拼进页面 error.message，存在向普通用户暴露内部信息的风险。
2. api.js 未在 401 时统一清理小程序登录态。
3. program-list、program-detail、personalized-plan、thermometer 加载失败时只弹 toast，toast 消失后页面缺少持久错误说明和重试入口。
4. 任务九期间尝试启动的 3 个 explorer 子代理均因连接中断失败，主线程已改为本地证据审查。
```

修改文件：

```text
apps/miniprogram/services/api.js
apps/miniprogram/pages/program-list/index.js
apps/miniprogram/pages/program-list/index.wxml
apps/miniprogram/pages/program-detail/index.js
apps/miniprogram/pages/program-detail/index.wxml
apps/miniprogram/pages/personalized-plan/index.js
apps/miniprogram/pages/personalized-plan/index.wxml
apps/miniprogram/pages/thermometer/index.js
apps/miniprogram/pages/thermometer/index.wxml
scripts/run_task9_review_checks.ps1
```

修复内容：

```text
1. api.js 新增按 status/code 的安全用户提示，不再直接使用后端原始 message 作为用户可见错误。
2. api.js 错误对象补充 retryable、status、debugMessage，页面继续读取 error.message 即可。
3. api.js 对 401 统一清理 auth_token、auth_user 和 app.globalData。
4. 4 个小程序页面新增 errorMessage、错误卡片和重试入口。
5. run_task9_review_checks.ps1 改为 ASCII 匹配和通配符定位中文目录，避免 Windows PowerShell 编码问题。
6. run_task9_review_checks.ps1 补充小程序页面三件套、请求层集中、前端敏感变量名和已跟踪运行产物检查。
```

验证命令：

```powershell
node --check apps/miniprogram/services/api.js
node --check apps/miniprogram/pages/program-list/index.js
node --check apps/miniprogram/pages/program-detail/index.js
node --check apps/miniprogram/pages/personalized-plan/index.js
node --check apps/miniprogram/pages/thermometer/index.js
powershell -ExecutionPolicy Bypass -File scripts/run_task9_review_checks.ps1 -SkipHeavyChecks
```

验证结果：

```text
通过。
```

### 2026-07-02：T9-13 至 T9-31 后端、数据库、脚本配置第一轮审查修复

审查范围：

```text
backend/app.py
backend/database.py
backend/config.py
backend/models.py
backend/routes/*
backend/scripts/*
backend/tests/*
scripts/*
.env.example
backend/requirements.txt
```

发现问题：

```text
1. Flask 只有 /healthz 和 /healthz/deep，缺少更标准的 readiness 入口。
2. create_app 只能读取环境变量和 Config 类，不方便测试中直接传入临时配置，也不能跳过数据库初始化。
3. 未知路由和未处理异常可能返回 Flask 默认错误外形，不利于前端稳定按 code 处理。
4. 后端入口缺少统一 logging 配置，异常堆栈和 handler 输出位置不够明确。
5. 缺少一条自动测试覆盖“中文、英文、数字、标点、emoji”从接口写入数据库再读回接口的编码链路。
6. .env.example 缺少日志和微信登录环境变量占位说明。
```

修改文件：

```text
backend/app.py
backend/tests/test_healthz.py
backend/tests/test_text_encoding_roundtrip.py
.env.example
apps/web/src/services/adminToken.ts
apps/web/src/pages/AdminDashboard.tsx
apps/web/src/services/safehomeApi.ts
shared/constants/api.ts
apps/miniprogram/services/api.js
docs/03_技术真相/API接口文档.md
```

修复内容：

```text
1. create_app 新增 config_overrides 和 init_database 参数。
2. 新增 /readyz，失败时返回 503，和轻量 /healthz 区分。
3. 新增 build_readiness_payload，复用数据库和 content 健康检查。
4. 新增 configure_logging，支持 LOG_LEVEL 和可选 LOG_FILE。
5. 新增全局异常处理：HTTPException 返回稳定 JSON；未处理异常记录堆栈并返回 internal_error。
6. 新增编码 roundtrip 测试，覆盖写入、数据库读取、接口返回。
7. .env.example 补充 LOG_LEVEL、LOG_FILE、WECHAT_APPID、WECHAT_SECRET。
8. 新增数据库字段检查表生成脚本，输出字段、主键、必填和关联提示。
9. Web 后台令牌不再写入 localStorage，也不再内置本地默认管理员 token；刷新后需重新输入。
10. shared 和小程序 API 端点补充 /readyz；API 文档同步健康检查和全局异常外形。
```

验证命令：

```powershell
python scripts/generate_task9_database_field_check.py
python -m py_compile scripts/generate_task9_database_field_check.py
python -m pytest backend/tests/test_healthz.py backend/tests/test_cors.py backend/tests/test_config.py backend/tests/test_text_encoding_roundtrip.py -q
python -m pytest backend/tests/test_text_encoding_roundtrip.py backend/tests/test_schema_migrations.py backend/tests/test_database_indexes.py backend/tests/test_audit_logs.py -q
powershell -ExecutionPolicy Bypass -File scripts/run_task9_review_checks.ps1 -SkipHeavyChecks
npm --prefix apps/web run build
```

验证结果：

```text
16 passed。
4 passed。
任务九轻量验收通过。
Web build 通过，仍有 Vite 大 chunk 警告。
```

仍需后续继续：

```text
1. T9-19 真实 MySQL 连接和 CloudBase readiness 需要部署环境或人工环境配合；本地 SQLite 备份恢复演练已通过。
2. T9-27 当前工作区已有 safehome-deploy zip 和 sqlite 文件状态，提交前需人工决定保留、忽略或清理。
3. T9-06 真机和微信开发者工具逐页打开仍需人工验收；自动侧已检查 app.json 页面三件套存在。
```

### 2026-07-02：任务九自动验收汇总

验收命令：

```powershell
powershell -ExecutionPolicy Bypass -File scripts/run_task9_review_checks.ps1
Get-ChildItem -Path "apps/miniprogram" -Recurse -Filter "*.js" | ForEach-Object { node --check $_.FullName }
Get-ChildItem -Path "apps/miniprogram" -Recurse -Filter "*.json" | ForEach-Object { Get-Content -Raw -LiteralPath $_.FullName | ConvertFrom-Json | Out-Null }
```

验收结果：

```text
任务九计划检查：31 个小任务存在，未出现 T9-32。
任务九执行记录覆盖：T9-01 至 T9-31 均已记录。
数据库字段检查表：存在，字段数量 298。
核心入口文件：存在。
小程序 integration-test：保留。
小程序 app.json 页面三件套：通过。
小程序请求传输集中在 services/api.js：通过。
前端敏感后端环境变量名扫描：通过。
已跟踪本地运行产物扫描：通过；工作区仍提示既有 safehome-deploy zip 状态需提交前人工处理。
关键文件语法烟测：通过。
content validation：通过。
backend tests：143 passed。
web build：通过；仍有 Vite 大 chunk 警告。
小程序 JS 全量语法检查：通过。
小程序 JSON 全量解析检查：通过。
```

子代理情况：

```text
任务九期间曾启动 3 个只读 explorer 子代理，分别审查小程序、后端数据库、脚本配置。
3 个子代理均因连接中断失败，没有产出可用审查结果。
后续审查、修复和验收均由主线程基于本地文件和命令亲自执行。
任务九自动验收完成后，3 个子代理均已关闭。
```

### 2026-07-02：任务九完成审计补充

本轮补查命令：

```powershell
powershell -ExecutionPolicy Bypass -File scripts/run_task9_review_checks.ps1 -SkipHeavyChecks
Invoke-WebRequest https://flask-gh3l-261352-9-1436233118.sh.run.tcloudbase.com/healthz
Invoke-WebRequest https://flask-gh3l-261352-9-1436233118.sh.run.tcloudbase.com/readyz
Invoke-WebRequest https://flask-gh3l-261352-9-1436233118.sh.run.tcloudbase.com/healthz/deep
Get-Command tcb, cloudbase, wxcloud
临时 DATABASE_PATH 下执行 init_db -> backup_sqlite.py -> restore_sqlite.py -> check_database_health
```

补查结果：

```text
1. 任务九轻量验收仍通过。
2. 当前本机没有 DB_PROVIDER、MYSQL_HOST、MYSQL_USER、MYSQL_PASSWORD、MYSQL_DATABASE、WECHAT_APPID、WECHAT_SECRET、ADMIN_EXPORT_TOKEN、SECRET_KEY 环境变量。
3. 当前本机没有 tcb、cloudbase、wxcloud 命令，无法从本机直接发布 CloudBase。
4. CloudBase 公网 URL：
   - /healthz：20 秒超时。
   - /readyz：20 秒超时。
   - /healthz/deep：503 Service Unavailable。
5. 使用临时 SQLite 数据库完成备份和恢复演练，恢复后 check_database_health().ok=True。
6. 临时备份文件和临时数据库目录已清理，未改动项目现有数据库。
```

完成审计结论：

```text
任务九本地自动审查、代码修复、文档记录和验收工程已完成。
真实 MySQL 连接已由 CloudBase `/healthz/deep` 返回 `provider=mysql` 且 `ok=true` 证明。
真实 CloudBase `/readyz` 和小程序真机打开仍缺少通过证据，不能标记为已完成。
下一步需要用户提供或人工完成 CloudBase 控制台发布任务九新版本，以及微信开发者工具/真机验收条件。
```

### 2026-07-02：任务九外部验收脚本补强

新增脚本：

```powershell
scripts/run_task9_external_checks.ps1
```

新增发布验收清单：

```text
docs/02_专项进度与验收/任务九CloudBase发布验收清单_20260702.md
```

脚本用途：

```text
1. 检查本机是否有 tcb、cloudbase、wxcloud 命令。
2. 检查真实环境变量是否存在，但不输出真实值。
3. 检查 CloudBase 公网 /healthz、/readyz、/healthz/deep。
4. 使用临时 SQLite 数据库执行备份恢复烟测。
```

最新运行结果：

```text
powershell -ExecutionPolicy Bypass -File scripts/run_task9_external_checks.ps1 -SkipCloudBase：通过。
powershell -ExecutionPolicy Bypass -File scripts/run_task9_external_checks.ps1 -SkipSqliteBackupRestore：失败，原因是 /readyz 返回 404。

CloudBase 公网端点：
- /healthz：200，env=production，ok=true。
- /readyz：404 Not Found。
- /healthz/deep：200，database.provider=mysql，database.ok=true，content.ok=true。

本机命令和环境：
- tcb/cloudbase/wxcloud：not-found。
- DB_PROVIDER/MYSQL_HOST/MYSQL_USER/MYSQL_PASSWORD/MYSQL_DATABASE/ADMIN_EXPORT_TOKEN/SECRET_KEY/WECHAT_APPID/WECHAT_SECRET：missing。
```

外部验收结论：

```text
云端当前运行的是不包含 /readyz 的旧版本或未发布任务九新代码。
要完成任务九外部闭环，需要发布当前分支代码到 CloudBase 后重新运行 scripts/run_task9_external_checks.ps1。
```

### 2026-07-02：任务九 CloudBase 发布验收清单补齐

本轮新增：

```text
1. docs/02_专项进度与验收/任务九CloudBase发布验收清单_20260702.md。
2. docs/04_部署联调/服务器与数据库部署检查清单.md 补充 /readyz 和任务九外部验收脚本。
3. docs/04_部署联调/小程序云托管调试说明.md 补充 /readyz 调试判断。
```

目的：

```text
当前本机没有 CloudBase CLI，也没有控制台操作条件，不能直接发布。
发布验收清单把剩余外部闭环拆成可执行步骤，后续拿到发布条件后可直接执行。
```

### 2026-07-02：任务九 CloudBase 上传包构建入口补齐

本轮新增：

```text
1. scripts/build_task9_cloudbase_package.ps1。
2. scripts/verify_task9_cloudbase_package.ps1。
3. scripts/run_task9_review_checks.ps1 已把打包脚本和校验脚本纳入存在性、语法和最新上传包校验。
```

打包脚本用途：

```text
1. 复用根目录 Dockerfile。
2. 将 backend、content、shared、Dockerfile 和 .dockerignore 复制到临时目录。
3. 排除 .env、数据库、日志、缓存、虚拟环境、node_modules、dist、backups、exports 等本地运行产物。
4. 生成 .codex_tmp/safehome-cloudbase-task9-*.zip。
5. 只生成上传包，不调用 CloudBase 发布命令，不写入任何密钥。
```

本次验证：

```text
powershell -ExecutionPolicy Bypass -File scripts/build_task9_cloudbase_package.ps1：通过。
powershell -ExecutionPolicy Bypass -File scripts/verify_task9_cloudbase_package.ps1：通过。
生成包：.codex_tmp/safehome-cloudbase-task9-20260702_135817.zip。
zip 内容检查：Dockerfile、.dockerignore、backend/app.py、backend/requirements.txt、content/training_cards.json、content/feedback_rules.json、shared/constants/api.ts、TASK9_PACKAGE_MANIFEST.txt 均存在。
zip 条目反斜杠检查：0 个，路径已使用 / 分隔，便于 Linux 容器解压。
PowerShell 脚本语法解析：通过。
```

当前结论：

```text
CloudBase 自动发布仍不可由本机完成，但已经补齐可上传 zip 包和可重复生成脚本。
下一步在 CloudBase 控制台上传最新 safehome-cloudbase-task9-*.zip 后，重新运行 scripts/run_task9_external_checks.ps1 -SkipSqliteBackupRestore。
```

### 2026-07-02：外部健康检查超时参数补强

本轮调整：

```text
scripts/run_task9_external_checks.ps1 新增 HealthTimeoutSec 参数，默认 60 秒。
原因：CloudBase 健康端点曾在 20 秒脚本超时时误显示三端点都超时；随后 60 秒手动探测确认 /healthz=200、/readyz=404、/healthz/deep=200。
```

当前外部结论仍为：

```text
CloudBase 服务可达，MySQL 可用；/readyz 不存在，说明云端仍未发布任务九新增代码。
```

### 2026-07-02：任务九上传包自动校验补齐

本轮新增：

```text
scripts/verify_task9_cloudbase_package.ps1
```

校验范围：

```text
1. 自动选择 .codex_tmp 下最新 safehome-cloudbase-task9-*.zip。
2. 检查包大小，避免误上传空包或损坏包。
3. 检查 Dockerfile、backend、content、shared 和 TASK9_PACKAGE_MANIFEST.txt 等必需条目。
4. 检查 zip 条目没有 Windows 反斜杠路径。
5. 拒绝 .env、数据库、日志、缓存、虚拟环境、node_modules、dist、backups、exports 等运行产物。
6. 检查 manifest 标识和 included 路径。
```

验证结果：

```text
powershell -ExecutionPolicy Bypass -File scripts/build_task9_cloudbase_package.ps1：通过。
powershell -ExecutionPolicy Bypass -File scripts/verify_task9_cloudbase_package.ps1：通过。
最新上传包：.codex_tmp/safehome-cloudbase-task9-20260702_135817.zip。
```

### 2026-07-02：任务九完整验收复跑

本轮复跑：

```powershell
powershell -ExecutionPolicy Bypass -File scripts/run_task9_review_checks.ps1
powershell -ExecutionPolicy Bypass -File scripts/run_task9_external_checks.ps1 -SkipCloudBase
powershell -ExecutionPolicy Bypass -File scripts/run_task9_external_checks.ps1 -SkipSqliteBackupRestore
```

结果：

```text
run_task9_review_checks.ps1：通过。
content validation：通过。
backend tests：143 passed。
web build：通过，仍有 Vite chunk 大小警告。
verify_task9_cloudbase_package.ps1：通过。
run_task9_external_checks.ps1 -SkipCloudBase：通过，临时 SQLite 备份恢复 health_ok=True。
run_task9_external_checks.ps1 -SkipSqliteBackupRestore：失败，原因仍为 /readyz 404。
CloudBase /healthz：200。
CloudBase /healthz/deep：200，database.provider=mysql，database.ok=true。
CloudBase /readyz：404。
docker：not-found，无法在本机执行 Docker build 验证。
```

完成审计：

```text
任务九本地自动审查、修复、验收工程、上传包构建和上传包校验均已完成。
真实 CloudBase /readyz 和小程序真机验收仍缺少通过证据，不能标记任务九整体完成。
```

### 2026-07-02：任务九严格完成审计脚本补齐

本轮新增：

```text
scripts/audit_task9_completion.ps1
```

审计范围：

```text
1. 检查任务九计划、执行记录、数据库字段检查表、发布验收清单和关键脚本存在。
2. 检查任务九计划为 31 项，执行记录覆盖 T9-01 至 T9-31。
3. 运行任务九本地验收，默认使用轻量模式；可用 -RunHeavyLocalChecks 跑完整本地验收。
4. 运行上传包校验。
5. 默认检查 CloudBase /healthz、/readyz、/healthz/deep。
```

最新运行结果：

```text
powershell -ExecutionPolicy Bypass -File scripts/audit_task9_completion.ps1：失败。
失败点：strict external CloudBase readiness。
本地任务九验收：通过。
上传包校验：通过。
CloudBase /healthz：200。
CloudBase /healthz/deep：200，database.provider=mysql，database.ok=true。
CloudBase /readyz：404。
```

结论：

```text
该脚本会防止把任务九误标为完成。
当前任务九整体仍未完成，唯一外部阻断证据仍是 CloudBase 未发布包含 /readyz 的任务九代码，以及微信开发者工具/真机验收未完成。
```

### 2026-07-03：任务九上传包 latest 与 SHA256 校验补强

本轮新增/调整：

```text
1. scripts/run_task9_external_checks.ps1 增加 HealthRetryCount 和 HealthRetryDelaySec，默认对 CloudBase 健康端点重试，降低 503/冷启动波动误判。
2. scripts/build_task9_cloudbase_package.ps1 生成时间戳包的同时，生成稳定文件名 .codex_tmp/safehome-cloudbase-task9-latest.zip。
3. build 脚本同步生成 .sha256 校验文件。
4. scripts/verify_task9_cloudbase_package.ps1 会校验 sidecar SHA256。
5. verify 脚本默认优先校验 latest 包。
6. 本地 Python 语法检查改为内存 compile，避免并发 py_compile 写 __pycache__ 造成 Windows 拒绝访问。
```

最新上传包：

```text
.codex_tmp/safehome-cloudbase-task9-latest.zip
SHA256=E2FB72F9291CF70074A4D993EE7F71209901A7A6B0E7C5E7EA140B826751E333
```

最新外部探测：

```text
CloudBase /healthz：200。
CloudBase /healthz/deep：200，database.provider=mysql，database.ok=true。
CloudBase /readyz：连续 3 次重试后仍为 404。
```

当前结论：

```text
上传包选择和完整性校验已进一步降低人工发布风险。
任务九仍需把 latest zip 发布到 CloudBase 服务 flask-gh3l 后，再跑严格完成审计。
```

### 2026-07-03：任务九完成审计快照生成

本轮新增：

```text
scripts/write_task9_audit_snapshot.ps1
docs/02_专项进度与验收/task9_completion_audit_snapshot_20260703.json
docs/02_专项进度与验收/task9_completion_audit_snapshot_20260703.md
```

快照内容：

```text
completionStatus=blocked_cloudbase_readyz
planTaskCount=31
recordTaskCount=31
package=.codex_tmp/safehome-cloudbase-task9-latest.zip
SHA256=E2FB72F9291CF70074A4D993EE7F71209901A7A6B0E7C5E7EA140B826751E333
CloudBase /healthz=200
CloudBase /readyz=404
CloudBase /healthz/deep=200
```

用途：

```text
后续发布 CloudBase 后，可以重新运行 write_task9_audit_snapshot.ps1，对比 completionStatus 和 /readyz 状态是否变化。
```

### 2026-07-03：任务九外部人工验收证据表补齐

本轮新增：

```text
docs/02_专项进度与验收/任务九外部人工验收证据表_20260703.md
```

覆盖范围：

```text
1. CloudBase 发布证据：上传包、SHA256、服务名、环境变量、发布版本和回滚点。
2. 发布后命令证据：external checks、strict audit、audit snapshot。
3. 微信开发者工具证据：debug 页、integration-test 页、核心页面失败态。
4. 真机验收证据：核心链路、网络失败、401、风险文案和用户提示。
```

脚本同步：

```text
scripts/audit_task9_completion.ps1 已把该证据表纳入必备交付物检查。
```

### 2026-07-03：任务九外部证据完成门槛补齐

本轮新增：

```text
scripts/verify_task9_external_evidence.ps1
```

校验规则：

```text
1. 定位带有 task9_external_acceptance_evidence 标记的证据表。
2. 所有表格复选项必须从 [ ] 改为 [x]。
3. 证据表中的 allow_complete 必须从 no 改为 yes。
4. 只有 CloudBase readiness 通过后，audit_task9_completion.ps1 才会继续检查该人工证据门槛。
```

当前运行结果：

```text
powershell -ExecutionPolicy Bypass -File scripts/verify_task9_external_evidence.ps1：失败。
失败原因：证据表仍有 27 个未勾选项。
```

结论：

```text
即使后续 CloudBase /readyz 发布成功，任务九也不会在缺少微信开发者工具和真机证据时被误标完成。
```

### 2026-07-04：CloudBase 发布包超长文件名修复

用户提供的 CloudBase 部署日志显示：

```text
部署开始于 2026-07-04 01:22:11
版本名称 flask-gh3l-010
版本状态 构建失败
失败发生在检出 ZIP 包阶段
content/profiles/... File name too long
```

判断：

```text
这次失败不是 Flask 启动失败、端口错误、依赖安装失败或数据库连接失败。
CloudBase 在解压上传 ZIP 包时遇到 content/profiles 下超长中文文件名，导致 Docker 构建尚未开始。
```

本轮修复：

```text
1. scripts/build_task9_cloudbase_package.ps1 新增 Rename-ProfileModelsForCloudBase。
2. 该函数只在发布包 staging 目录中把 content/profiles/*.json 改成 profile_001_<hash>.json。
3. 源目录 content/profiles 文件名不改，JSON 内 model_id 不改，避免覆盖用户已人工调整的画像模型。
4. scripts/verify_task9_cloudbase_package.ps1 新增 ZIP 路径片段长度校验。
5. verify 脚本要求发布包内 content/profiles 文件名必须是 profile_001_<hash>.json 这类短名。
```

最新包：

```text
.codex_tmp/safehome-cloudbase-task9-latest.zip
SHA256=4821AEA9C7E1C3422C541A7B0C50B9A7E190A9352B6BEAFD7860BDBBC0301F74
SizeBytes=360169
```

验证命令：

```powershell
powershell -ExecutionPolicy Bypass -File scripts/build_task9_cloudbase_package.ps1
powershell -ExecutionPolicy Bypass -File scripts/verify_task9_cloudbase_package.ps1
powershell -ExecutionPolicy Bypass -File scripts/run_task9_review_checks.ps1 -SkipHeavyChecks
```

验证结果：

```text
三条命令均通过。
ZIP 内 content/profiles 文件名已变为 profile_001_85aea5e3aa28.json 等短文件名。
```

下一步：

```text
重新上传 latest 包到 CloudBase 服务 flask-gh3l。
确认构建日志不再出现 File name too long。
发布成功后继续检查 /readyz，并补齐外部人工验收证据表。
```

### 2026-07-04：CloudBase 发布成功后复测

用户已完成 CloudBase 发布：

```text
服务：flask-gh3l
线上版本：flask-gh3l-012
发布时间：2026-07-04 01:36:51
服务状态：正常
实例数量：1
```

复测命令：

```powershell
powershell -ExecutionPolicy Bypass -File scripts/run_task9_external_checks.ps1 -SkipSqliteBackupRestore
powershell -ExecutionPolicy Bypass -File scripts/write_task9_audit_snapshot.ps1
powershell -ExecutionPolicy Bypass -File scripts/audit_task9_completion.ps1
```

复测结果：

```text
run_task9_external_checks.ps1：通过。
/healthz=200。
/readyz=200。
/healthz/deep=200。
write_task9_audit_snapshot.ps1：通过。
completionStatus=ready_for_manual_miniprogram_acceptance。
audit_task9_completion.ps1：未通过。
```

严格审计未通过原因：

```text
CloudBase readiness 已通过。
当前失败点是 docs/02_专项进度与验收/任务九外部人工验收证据表_20260703.md 仍有未勾选项。
这些未勾选项主要是微信开发者工具和真机验收。
```

证据表更新：

```text
已勾选可确认的 CloudBase 发布项和命令验收项。
allow_complete 仍为 no。
任务九状态从 blocked_cloudbase_readyz 推进为 ready_for_manual_miniprogram_acceptance。
```
