# 任务十二执行记录（2026-07-10）

任务：补录量表验收 · 数据1题项级亲密关系画像 · 治疗性评估赋能闭环。

## 一、完成结论

T12-01至T12-30的自动可执行项已完成。T12-05为“部分完成/待人工”：12份补录量表中8份已有题项草稿和小程序链路，4份因题项、版本或授权未确认继续隐藏，没有复制受限题项或臆造内容。微信开发者工具、真机、正式研究者账号策略、模型命名、报告下载体验和正式开放仍是人工验收项。

## 二、S0-S3：清单、来源与全量量表审核

1. 读取 `D:\桌面\Desktop\补录12量表人工验收表.xlsx`，确认唯一清单为12份。
2. 新增 `backend/scripts/audit_task12_scale_sources.py`，自动核对验收表、catalog、draft、worksheet和本地源文件。
3. 产出补录12量表审核矩阵和本地来源抽取表：8份已有完整结构，4份继续隐藏。
4. 对四个缺口只检索正式来源与授权线索；不从网页复制题目。
5. 扩展 `audit_assessment_content.py`，全量审核30份worksheet的题项数、选项、反向、维度、计分、来源、边界和画像绑定；结构缺口为0。

明确缺陷：

```text
sleep_isi_psqi：ISI/PSQI被合并为一个catalog ID，且授权、中文版本未确认。
cognitive_curiosity_student：本地10题变量与作者中文版本未完成逐题映射和用途授权。
family_cohesion_adaptability：FACES 20题/30题版本冲突未解决。
parental_autonomy_support：本地9题无法确认父亲版/母亲版及中文验证来源。
```

处理：四项全部保持 `metadata_only` 和隐藏，不绕过人工验收。

## 三、S4-S5：数据1映射、计分、分量表画像

1. 新增 `build_task12_relationship_dataset.py`，建立69列缩写映射；输出425×18、425×18、425×31三个独立题项矩阵。
2. 逐行私有矩阵写入已忽略的 `outputs/task12_relationship_profiles/private/`，仓库只保留哈希、映射、维度校验和聚合结果，不含开放文本。
3. 三份量表接入content、数据库、API、小程序和Web：
   - `regulatory_focus_relationship_18`：18题，PROM/PREV，产品1-7，既往数据1-5；画像匹配显式线性换算。
   - `micro_ysq_relationship_18`：18题，1-6，只作关系担心线索。
   - `relationship_initiation_intention_action`：31题，1-5，按冻结乘积/均值公式计算，不生成总分。
4. 扩展通用计分引擎，支持逐题选项、乘积、乘积均值、反向项均值、派生维度和无总分。
5. 新增GMM/KMeans方法比较脚本。最终候选：调节聚焦GMM k=3、Micro YSQ GMM k=4、关系主动性GMM k=2；PCA只用于二维位置。
6. 三个聚合模型写入 `content/profiles/task12_*.json`，不含训练样本点；画像名称和解释标记为研究者待复核。
7. 交叉验证报告明确425题项矩阵与469清洗分析样本口径不同，所有一致性只作探索性解释。

已修复缺陷：

```text
1. 产品调节聚焦1-7与训练数据1-5不一致：模型特征增加linear_range输入换算。
2. 关系乘积公式无法由旧通用计分表达：新增声明式计算，并保留审计原始选择。
3. GMM只按BIC会偏向过多簇：增加最小簇比例、稳定性和silhouette最低门槛。
4. 全量测试中scripts模块导入受顺序影响：测试改为按文件路径加载。
```

## 四、S6-S7：报名、报告、研究者和线上评估工具

1. 新增第二阶段报名表和接口，必须学生角色、明确研究授权、已有三份关系测评之一。
2. 报名快照关联测评结果、画像模型、簇、维度、雷达、支持性描述和用户ID。
3. 新增关系健康初筛报告；报告包含维度、雷达、画像、个性化解读、评估问题、项目任务、四层画像、版本和边界。
4. 报告必须研究者确认后才可发送；消息只携带摘要和报告ID，收件人从报告所有者读取，不能由请求体替换。
5. 研究者登录复用正式账号接口；小程序“我的”新增研究者入口，普通用户不能访问仪表盘。
6. 小程序和Web研究者平台可查看报名、画像、报告、任务材料、备注、风险/复核状态；敏感查看和操作写入审计。
7. 关系绘画使用本地画布笔画数据并保存画布尺寸；研究者预览按比例缩放，不自动解释象征或潜意识。
8. 句子补全限制为七个配置情境，原文默认不导出；开放文本进入风险预检和risk review。
9. 探索手记先生成草稿，研究者确认后发送消息，用户只能查看确认版本。
10. 训练页新增大学生关系探索入口；Web新增三份关系量表选择、填写、提交和画像结果页。

已修复缺陷：

```text
1. 报告可能在未确认时发送：后端强制confirmed状态。
2. 研究者查看报名材料缺审计：补relationship_enrollment_viewed。
3. 开放叙事只标状态未进风险队列：同步写risk_review_records。
4. 绘画在不同设备预览可能变形：保存源画布宽高并按比例重放。
5. 测一测入口传query/audience参数但页面忽略：onLoad读取并应用筛选参数。
6. Web只有后台查看、没有三份量表提交页：新增RelationshipAssessmentPage。
```

## 五、S8：连续施测、成长仪表盘与替代方案

1. 新增每周补充测量：主动社交次数、真实表达次数、挫折应对、靠近意愿、担忧强度。
2. 新增成就感、挫败和关键事件叙事；绑定 `event_at`，风险文本进入人工复核。
3. 小程序“我的”新增关系探索成长仪表盘，展示曲线数据、测评/任务/事件/报告/研究者反馈时间轴和成长报告。
4. 画像拆为基础、矛盾、机制、动态四层；机制层始终写成待核对假设，动态层明确不构成疗效证明。
5. 完成知识星球站内替代方案，复用课程、项目、打卡、消息、报告、督导和研究者平台，不传敏感数据到外部平台。
6. 完成任务十二后前端优化计划，按P0/P1/P2列出试点前体验、信息架构、成长图表、研究者效率和Web拆包路径。

## 六、自动化验证

```text
python backend/scripts/validate_content.py：通过。
python backend/scripts/build_worksheets.py：通过，生成28份draft worksheet，总worksheet 30份。
python backend/scripts/audit_assessment_content.py：通过，30份，structural_gap_count=0。
python analysis/profiling/build_task12_relationship_dataset.py：通过，69列映射；425×18、425×18、425×31；raw_text_included=false。
python analysis/profiling/build_task12_relationship_profiles.py：通过，3个模型；GMM k=3/4/2。
python -m pytest backend/tests -q：171 passed。
小程序50个JS文件 node --check：通过。
小程序47个JSON文件解析：通过。
apps/web npm run build：通过；620 modules；主包1,633.29kB，gzip489.89kB，有大包告警。
git check-ignore private/item_matrices.npz：命中.gitignore；git ls-files无私有矩阵。
```

## 七、人工验收与已知缺陷

1. 四份量表缺口必须由量表负责人确认版本、授权和逐字题项，不能自动完成。
2. 三个画像模型的k、名称、解释和训练建议需研究者复核；Micro YSQ相邻低分簇可考虑人工合并。
3. 微信开发者工具和iOS/Android真机尚未运行；画布触控、弱网、本地报告保存需真机验收。
4. 当前用户下载为结构化JSON本地保存，不是排版PDF/长图；可用但体验仍需优化。
5. Web主包超过500kB告警，功能和构建通过，但应按路由拆分ECharts和后台页面。
6. 正式研究者账号轮换、停用、密码策略、数据保留期和备份恢复需试点负责人确认。
7. 未实际使用Claude或Claude Code，因此没有更新 `docs/10Claude协作/Claude使用记录.md`。

## 八、下一轮启动提示词

```text
请继续 safehome1.0，先阅读 docs/02_专项进度与验收/任务十二执行记录_20260710.md、任务十二代码审查.md 和 docs/07_UI设计/任务十二后前端优化计划.md。任务十二自动可执行项已完成，后续先做试点前人工验收：四份量表授权/版本、三个画像命名、微信开发者工具与真机、研究者账号策略、报告下载体验。发现问题只做最小修复，不重写任务十二，不删除integration-test页。
```

## 九、六份量表试点开放补充记录

2026-07-10 项目负责人确认先开放以下六份现有worksheet：

| 量表ID | 题数 | 开放状态 | 保留的人工复核重点 |
| --- | ---: | --- | --- |
| `acceptance_action_aaq2` | 7 | `pilot_review_required` / enabled | 分数解释与正式用途边界 |
| `academic_buoyancy_4` | 4 | `pilot_review_required` / enabled | 总分解释与学生端文案 |
| `afq_y8_avoidance_fusion` | 8 | `pilot_review_required` / enabled | 避免“心理僵化”标签化解释 |
| `cfi2_cognitive_flexibility` | 12 | `pilot_review_required` / enabled | 4个反向题和3个维度 |
| `fmi_12_mindfulness` | 12 | `pilot_review_required` / enabled | 觉知/接受精确映射 |
| `swls_life_satisfaction` | 5 | `pilot_review_required` / enabled | 选项方向和非诊断等级解释 |

实施结果：catalog、draft、worksheet三层一致；API列表/详情/提交均通过测试；用户端worksheet由19份开放增至25份，5份继续隐藏。构建脚本不再把worksheet更新时间回写成旧日期。

验证：`.\scripts\check_all.ps1` 全部通过；后端173 passed；Web构建、小程序JS/JSON、内容校验通过。审核CSV已重新生成，30份worksheet结构缺口仍为0。

边界：本次是“试点开放”而不是“正式验收通过”。六份 `scoring_status` 仍为 `rule_available_pending_review`；仓库尚未部署到云托管，也未完成微信开发者工具和真机验收。

## 十、登录与后前端优化补充记录

已完成微信/手机号/账号密码登录代码、P0敏感材料恢复、P1产品体验第1至3项、关系服务拆分和约束、共享API/UI契约、Web路由拆包、非敏感事件、图表五态及全站小程序视觉/组件审查。P1产品体验第4项研究者三栏工作台按用户要求排除，没有重复实现。

最终自动验证：`scripts/check_all.ps1`全部通过，后端187项；Playwright真实后端E2E 2项；Web首屏JS gzip 64.38KB；小程序37页、42个组件引用、7个Canvas，问题0；`npm audit`漏洞0。

人工事项：配置微信开发者工具CLI，CloudBase加入`/wxa/business/getuserphonenumber`开放接口并部署，iOS/Android验证登录、画布、弱网草稿和报告长图。未使用ImageGen，因为审查未发现需要图案才能解释的信息；未执行Git提交或推送。

## 十一、全部聚类画像审查补充记录

已审查学生KMeans、8个既往调研KMeans、3个任务十二GMM及仓库外6个内部补充模型；新增聚合审计脚本、审计JSON和全链路优化报告。审查发现的问题不改变任务十二功能链路已完成的事实，但会阻止“正式智能画像”口径：必须先修运行时准入、服务端答案验证、标签化名称、隐私撤回/数据范围和逐行训练点，再处理GMM线上一致性、稳定性及个体化规则。
