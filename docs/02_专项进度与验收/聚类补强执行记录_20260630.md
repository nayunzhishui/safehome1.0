# 聚类补强执行记录

日期：2026-06-30

## 1. 目标

只补齐现有聚类画像进入小程序试点前必须具备的质量控制、稳定性判断和用户端保护。不重做整套系统，不重建网页版，不新增复杂算法体系。

## 2. 完成内容

```text
1. 新增聚类补强脚本：analysis/profiling/build_cluster_strengthening_reports.py
2. 生成 06A_聚类文件索引与状态表.csv
3. 生成 06B_模型质量审计报告.csv
4. 生成 06C_模型稳定性检验报告.csv
5. 生成 06D_模型准入清单.md
6. 生成 06E_画像_训练中心最小映射表.csv
7. 增强画像匹配接口低置信度和离群保护
8. 小程序结果页增加低置信/离群提示
9. shared 类型同步新增画像解释可靠性字段
```

## 3. 输出位置

```text
D:\codex\workspace\safehome1.0其他内容\画像系统设计_Claude_20260628
```

核心文件：

```text
06A_聚类文件索引与状态表.csv
06B_模型质量审计报告.csv
06C_模型稳定性检验报告.csv
06D_模型准入清单.md
06E_画像_训练中心最小映射表.csv
```

## 4. 当前准入结论

可试点接入：

```text
青少年情绪弹性问卷
反思功能问卷8题（RFQ-8）
青少年心理韧性量表（RSCA）
健康促进生活方式量表（HPLP）- 牛至旭数据
健康促进生活方式量表（HPLP）- 高鸣聪数据
一般调节聚焦问卷
```

仅内部审核：

```text
父母反思功能问卷（PRFQ）
自我关怀量表（SCS）
6 个未映射补充聚类模型
```

## 5. 已验证

```powershell
python analysis/profiling/build_cluster_strengthening_reports.py
python backend/scripts/validate_content.py
python -m pytest backend/tests/test_assessments_route.py -q
Get-ChildItem apps/miniprogram -Recurse -Filter *.js | ForEach-Object { node --check $_.FullName }
Get-ChildItem apps/miniprogram -Recurse -Filter *.json | ForEach-Object { Get-Content -Raw -LiteralPath $_.FullName | ConvertFrom-Json | Out-Null }
cd apps/web; npm run build
```

结果：

```text
聚类补强报告生成成功，覆盖 14 个模型。
内容校验通过。
测评路由测试 14 passed。
小程序 JS/JSON 静态检查通过。
Web build 通过。
```

## 6. 已知问题

```text
1. PRFQ 和 SCS 暂列仅内部审核，不建议当前直接展示给用户端。
2. 6 个补充聚类模型默认不开放，除非后续人工确认题项来源、计分方向和产品映射。
3. 稳定性检验使用脚本内置 numpy KMeans，仅用于审核报告，不改写正式模型 JSON。
```

## 7. 下一步建议

```text
1. 先人工查看 06D_模型准入清单.md。
2. 只处理“可试点接入”的模型与小程序量表关联。
3. 检查 06E_画像_训练中心最小映射表.csv 中训练卡推荐是否符合两个暑期测试项目。
4. 不要开放 PRFQ、SCS 和补充聚类模型给用户端。
```
