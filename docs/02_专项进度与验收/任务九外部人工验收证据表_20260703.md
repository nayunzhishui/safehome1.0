# 任务九外部人工验收证据表

<!-- task9_external_acceptance_evidence -->

创建日期：2026-07-03

适用分支：`codex/task9-code-review`

适用上传包：

```text
.codex_tmp/safehome-cloudbase-task9-latest.zip
SHA256=4821AEA9C7E1C3422C541A7B0C50B9A7E190A9352B6BEAFD7860BDBBC0301F74
```

用途：记录任务九无法由本机自动完成的外部证据。只有 CloudBase 发布、`/readyz`、微信开发者工具和真机验收都有证据后，任务九才可以进入完成审计。

## 1. CloudBase 发布证据

| 状态 | 验收项 | 证据要求 | 记录 |
|---|---|---|---|
| [x] | 上传包 | 已上传 `safehome-cloudbase-task9-latest.zip` | 2026-07-04 用户截图显示线上版本 `flask-gh3l-012`，发布方式为本地上传 |
| [x] | SHA256 | 上传前本地 SHA256 与本表一致 | 本地校验 `SHA256=4821AEA9C7E1C3422C541A7B0C50B9A7E190A9352B6BEAFD7860BDBBC0301F74` |
| [x] | 服务名 | 发布到 CloudBase 服务 `flask-gh3l` | 用户截图显示服务 `flask-gh3l` 线上版本正常 |
| [ ] | 环境变量 | `DB_PROVIDER=mysql`，MySQL、`SECRET_KEY`、`ADMIN_EXPORT_TOKEN` 等只在 CloudBase 环境变量中配置 |  |
| [x] | 发布版本 | 记录 CloudBase 新版本号或发布时间 | `flask-gh3l-012`，发布时间 2026-07-04 01:36:51，服务正常 |
| [x] | 回滚点 | 记录发布前可回滚版本 | 历史版本包含 `flask-gh3l-011`，创建时间 2026-07-04 01:30:40 |

## 2. 发布后命令证据

发布后在项目根目录运行：

```powershell
powershell -ExecutionPolicy Bypass -File scripts/run_task9_external_checks.ps1 -SkipSqliteBackupRestore
powershell -ExecutionPolicy Bypass -File scripts/audit_task9_completion.ps1
powershell -ExecutionPolicy Bypass -File scripts/write_task9_audit_snapshot.ps1
```

| 状态 | 命令 | 通过标准 | 结果摘要 |
|---|---|---|---|
| [x] | `run_task9_external_checks.ps1 -SkipSqliteBackupRestore` | `/healthz=200`、`/readyz=200`、`/healthz/deep=200` | 2026-07-04 复跑通过，三项 CloudBase endpoint 均为 200 |
| [ ] | `audit_task9_completion.ps1` | 严格完成审计通过 |  |
| [x] | `write_task9_audit_snapshot.ps1` | 快照 `completionStatus` 不再是 `blocked_cloudbase_readyz` | 2026-07-04 快照状态为 `ready_for_manual_miniprogram_acceptance` |

## 3. 微信开发者工具证据

验收环境：

```text
项目路径：apps/miniprogram
CloudBase 环境：prod-d3gl35otiaa7c8d24
云托管服务：flask-gh3l
```

| 状态 | 页面/链路 | 通过标准 | 截图或说明 |
|---|---|---|---|
| [ ] | 编译 | 微信开发者工具可成功编译，无阻断错误 |  |
| [ ] | `pages/debug/index` | 可切换云托管并访问 healthz/assessments/risk/check/profile 最小请求 |  |
| [ ] | `pages/integration-test/index` | 长期联调页仍存在且可打开 |  |
| [ ] | 首页 | 普通用户可打开，不是空白页 |  |
| [ ] | 测一测 | 测评入口可打开，失败时有用户可见提示 |  |
| [ ] | 情绪记录 | 可提交明显测试记录，失败时有用户可见提示 |  |
| [ ] | 反馈结果 | 可展示非诊断反馈，失败时不暴露 SQL、堆栈或密钥 |  |
| [ ] | 训练卡/训练中心 | 可查看训练卡和推荐入口，失败时可重试 |  |
| [ ] | 情绪温度计 | 页面可打开，接口失败时不空白 |  |
| [ ] | 个性化训练方案 | 页面可打开，接口失败时不空白 |  |
| [ ] | 项目测试/项目详情 | 页面可打开，接口失败时不空白 |  |
| [ ] | 消息/督导/我的 | 页面可打开，权限不足时提示合理 |  |

## 4. 真机验收证据

| 状态 | 验收项 | 通过标准 | 截图或说明 |
|---|---|---|---|
| [ ] | 真机预览 | 可扫码打开当前小程序版本 |  |
| [ ] | 核心链路 | 首页 -> 情绪记录 -> 反馈 -> 训练卡可走通 |  |
| [ ] | 网络失败 | 断网或接口失败时不是空白页 |  |
| [ ] | 401/登录态失效 | 清理登录态并提示重新授权或登录 |  |
| [ ] | 高风险文案 | 高风险内容不生成普通自动训练建议 |  |
| [ ] | 用户提示 | 错误提示不暴露数据库、SQL、堆栈、密钥或内部服务细节 |  |

## 5. 验收结论

完成判定标记：

```text
allow_complete: no
```

填写规则：只有上方所有复选项都改为 `[x]`，且没有剩余阻断问题时，才能把 `allow_complete` 改为 `yes`。

```text
验收日期：
验收人：
CloudBase 发布版本：flask-gh3l-012，2026-07-04 01:36:51，服务正常
run_task9_external_checks 结果：通过；/healthz=200，/readyz=200，/healthz/deep=200
audit_task9_completion 结果：未通过；剩余阻断为外部人工验收证据表仍有未勾选项
write_task9_audit_snapshot completionStatus：ready_for_manual_miniprogram_acceptance
微信开发者工具结论：
真机结论：
剩余问题：微信开发者工具和真机验收尚未补齐；环境变量“只在 CloudBase 配置”的人工确认项尚未勾选
是否允许将任务九标记完成：否
```
