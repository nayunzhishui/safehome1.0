# 任务十五、任务十六代码审查报告

审查日期：2026-07-11

## 1. 审查范围

审查任务十三/十四合并工作区与任务十五/十六新增改动，覆盖测评计分、画像准入、离线分析、训练节奏、项目测量计划、身份认证、隐私所有权、数据库索引、请求追踪、Web身份页面、小程序入口、CI和文档一致性。

## 2. 已发现并修复

| 等级 | 问题 | 修复与证据 |
|---|---|---|
| P0 | CloudBase身份头默认可信，普通公网存在伪造风险 | 增加默认关闭的显式信任开关、固定来源和OpenID格式校验；安全专项通过 |
| P0 | 旧helper在生产环境接受body/query user_id | 生产环境改为signed actor优先，开发匿名试用保留；冒充测试通过 |
| P0 | 隐私路由统一helper后把“其他用户ID”替换成本人，越权请求未返回403 | 改为privacy专用的先比对后解析流程；缺登录401、身份不符403；14项专项通过 |
| P1 | `users`三个身份字段缺数据库唯一性 | 增加匿名重复组预检和条件建唯一索引；旧库不自动合并；22项schema/health/auth测试通过 |
| P1 | Web可由localStorage伪造研究者角色 | 启动时调用`/api/auth/me`，受保护导航只使用服务端角色；双视口E2E通过 |
| P1 | `/family`同时渲染家庭页和暂缓占位页 | 修正deferred路由排除条件并加入回归断言 |
| P1 | 登录/注册/家庭绑定使用未定义`pill`且缺焦点/减少动效 | 统一按钮、状态live region、skip link、44px触控和focus-visible；桌面/移动截图通过 |
| P1 | API缺请求关联线索，readiness暴露内部路径 | 增加请求ID、耗时日志、响应头和错误体ID；移除路径及MySQL连接细节 |
| P1 | CI未覆盖Web类型与小程序资源 | 增加typecheck、npm ci、37页结构和53 JS/49 JSON跨平台门禁 |

## 3. 验证结果

```text
内容校验：通过
后端：215 passed，9个第三方弃用警告
Web：TypeScript通过，Vite build通过
小程序：37页、42组件引用、7 Canvas；53 JS、49 JSON通过
Playwright：12 passed，桌面Chrome与Pixel 7视口
git diff --check：通过
```

## 4. 剩余风险

1. CloudBase真实网关、微信手机号白名单、正式研究者账号、iOS/Android真机未验收。
2. MySQL唯一索引、迁移、事务、备份恢复尚未在真实实例演练；历史重复账号不得自动处理。
3. 量表题项/选项/计分/授权、项目测量计划、画像命名、外部效标和伦理仍需人工签字。
4. ECharts产物仍有约1.1MB大包告警，属于P2性能优化，不影响本轮正确性。
5. 全业务幂等键、登录限流、独立迁移runner、外部日志告警平台仍是后续工程项。
6. jieba依赖链存在`pkg_resources`弃用警告，当前不影响测试结果，后续升级依赖时处理。

结论：未发现未处理的P0代码问题；自动可执行范围通过全量门禁。剩余事项均已进入`docs/00_当前事实基准/待人工审查内容合集.md`或明确列为P2后续工程工作。
