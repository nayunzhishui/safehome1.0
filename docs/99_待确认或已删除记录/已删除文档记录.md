# 已删除文档记录

更新时间：2026-06-27

本文记录 `docs` 整理时确认不恢复、不迁移的已删除或当前缺失文档。后续如果需要恢复其中任何文件，必须由用户单独确认。

## 1. 已删除且不恢复

| 原路径 | 处理口径 | 说明 |
|---|---|---|
| `docs/6-9月微信小程序自动化开发总计划.md` | 不恢复 | 旧总计划已按用户要求删除，内容已对齐到 `docs/01_当前执行入口/6-9月自动化开发第一轮总计划修订版.md`。 |
| `docs/美术资产最小接入方案.md` | 不恢复 | 当前工作区显示已删除；UI 相关当前保留在 `docs/07_UI设计/`。 |
| `docs/P3暂缓事项.md` | 不恢复 | 当前工作区显示已删除；P3 状态以总计划和 `docs/02_专项进度与验收/P3量表录入进度表.md` 为准。 |
| `docs/代码审查.md` | 不恢复 | 当前工作区显示已删除；若后续需要审查历史，需用户单独确认恢复来源。 |

## 2. 当前未发现且不补建

| 原路径 | 处理口径 | 说明 |
|---|---|---|
| `docs/小样本用户研究试用任务脚本.md` | 不补建 | 历史索引曾提到，但当前 docs 列表未发现。 |
| `UI设计与Codex分步开发指令.md` | 不补建 | 历史索引曾提到，但本轮不在 docs 中恢复。 |
| `安心陪伴_零基础并行开发执行手册.md` | 不补建 | 历史索引曾提到，但本轮不恢复。 |

## 3. 后续规则

1. 不因旧文档名出现在历史日志中就自动恢复文件。
2. 不把已删除文档重新加入当前执行入口。
3. 如果未来确实需要恢复，先说明恢复原因、来源和目标分类，再由用户确认。
