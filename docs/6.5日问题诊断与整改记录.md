# 6.5日问题诊断与整改方案

更新时间：2026-06-05

## P0-01：数据导出字段白名单与后端脱敏

状态：部分完成 -> 已完成  
处理：补普通表白名单转换、records 不再导出完整 `data_json`、raw_wide/long 输出 `participant_code_hash`、Web 导出提示更新。  
修改文件：`backend/routes/admin.py`、`backend/tests/test_admin_export_privacy.py`、`apps/web/src/pages/ExportManagement.tsx`、`docs/数据字典.md`。  
测试：`python -m pytest tests -q` -> 69 passed；`npm run build` -> 通过。

## P0-02：后端敏感读取接口权限补齐

状态：部分完成 -> 已完成  
处理：新增 `require_admin_or_owner()`；画像详情、visuals、followups、sandplay、家长测评详情/action 要求后台令牌或匹配 user_id；家长测评列表要求后台令牌。  
修改文件：`backend/routes/utils.py`、`backend/routes/profile.py`、`backend/routes/parent_assessments.py`、`backend/tests/test_sensitive_owner_auth.py`、相关旧测试、Web/小程序 API client。  
测试：`python -m pytest tests -q` -> 61 passed；`npm run build` -> 通过；小程序 JS 检查 -> 通过。

## P0-03：Web 后台 Admin Token 统一传递

状态：部分完成 -> 已完成  
处理：新增 `adminToken.ts`，后台页面统一读取/保存 `safehome_admin_token`，profile/risk review 接口通过 header 传 token。  
修改文件：`apps/web/src/services/adminToken.ts`、`apps/web/src/services/safehomeApi.ts`、后台相关页面。  
测试：`npm run build` -> 通过。

## P0-04：高风险阻断一致性与测试增强

状态：部分完成 -> 已完成  
处理：补一致性测试，覆盖 low 不进复核、medium/high 进复核、high 反馈/画像不推荐普通训练卡、家长测评和督导 high 边界、多字段取最高风险。  
修改文件：`backend/tests/test_risk_routing_consistency.py`。  
测试：`python -m pytest tests/test_risk_routing_consistency.py ... -q` -> 16 passed；`python -m pytest tests -q` -> 65 passed。

## P0-05：content 伦理边界自动校验增强

状态：部分完成 -> 已完成  
处理：增强 `validate_content.py`，加入重复 ID、推荐卡存在性、高风险推荐阻断、禁用语、not_suitable_for 边界、risk_keywords high 处理和边界说明检查。  
修改文件：`backend/scripts/validate_content.py`、`backend/tests/test_content_validation.py`、`docs/content伦理边界校验说明.md`。  
测试：`python backend/scripts/validate_content.py` -> 通过；`python -m pytest tests/test_content_validation.py -q` -> 8 passed；后端全量 -> 69 passed。

## P0-06：仓库污染与敏感文件扫描

状态：未完成 -> 已完成  
处理：删除并从 Git 索引移除 1582 个 `outputs/**/Default/Cache/**`、`Default/Code Cache/**`、`Default/GPUCache/**` 浏览器缓存文件；补 `.gitignore`。  
修改文件：`.gitignore`、`docs/仓库污染与敏感文件扫描记录.md`，以及缓存文件删除。  
测试：缓存路径 `git ls-files` -> 0；敏感运行产物扫描 -> 无命中。

## P1-01：/healthz/deep 诊断增强

状态：部分完成 -> 已完成  
处理：补 schema 版本、训练卡同步、数据库路径父目录/文件状态字段。  
修改文件：`backend/database.py`、`backend/tests/test_healthz.py`、`docs/服务器与数据库部署检查清单.md`。  
测试：`python -m pytest tests/test_healthz.py -q` -> 2 passed；后端全量 -> 69 passed。

## P1-02：SQLite 运维检查脚本

状态：未完成 -> 已完成  
处理：新增只读检查脚本 `check_sqlite_ops.py`。  
修改文件：`backend/scripts/check_sqlite_ops.py`、`docs/数据库备份与恢复说明.md`、`docs/服务器与数据库部署检查清单.md`。  
测试：`python backend/scripts/check_sqlite_ops.py` -> 0，1 条 warning：未显式配置 `DATABASE_PATH`，使用默认路径。

## P1-03：统一全量检查脚本

状态：未完成 -> 已完成  
处理：新增 `scripts/check_all.ps1`，顺序执行 content、pytest、Web build、小程序 JS/JSON 检查。  
测试：`.\scripts\check_all.ps1` -> All checks passed。

## P1-04：API 错误格式和前端错误处理一致性

状态：部分完成 -> 已完成  
处理：Web `requestRaw` 在 `payload.ok=false` 时按真实 HTTP status 抛错；小程序错误对象补 `path/method/status/code/message`。  
修改文件：`apps/web/src/services/safehomeApi.ts`、`apps/miniprogram/services/api.js`。  
测试：后端全量 -> 69 passed；Web build -> 通过；小程序 JS -> 通过。

## P1-05：content 候选库

状态：未完成 -> 已完成  
处理：新增 enrichment 候选库：家长训练卡 16 条、学生训练卡 12 条、反馈规则 12 条、覆盖矩阵 1 份。正式 content 未导入候选。  
修改文件：`content/enrichment/**`、`docs/content候选库导入流程.md`。  
测试：`python -m json.tool` 检查 enrichment JSON -> 通过；`python backend/scripts/validate_content.py` -> 通过。

## P1-06：content 覆盖矩阵与缺口清单

状态：未完成 -> 已完成  
处理：新增 `docs/content覆盖矩阵与缺口清单.md`。  
测试：`python backend/scripts/validate_content.py` -> 通过。

## P2-01：GitHub Actions 自动检查

状态：未完成 -> 已完成  
处理：新增 `.github/workflows/check.yml`，只做 checkout、Python 依赖、content 校验、后端测试、Node 安装和 Web build。  
测试：本地未运行 GitHub Actions；最终 `scripts/check_all.ps1` 覆盖同类本地检查。

## P2-02：小程序和 Web 文案低 AI 味检查

状态：部分完成 -> 已完成  
处理：替换 Web 用户可见“固定人格/心理疾病诊断”等表述；记录量表原文、内部规则和否定式边界说明保留原因。  
修改文件：`apps/web/src/pages/ReadFeedbackIntegrationPages.tsx`、`content/enrichment/parent_training_cards_candidates.json`、`docs/文案低AI味与伦理表达检查.md`。  
测试：`npm run build` -> 通过；小程序 JS -> 通过。

## P2-03：项目架构边界文档

状态：未完成 -> 已完成  
处理：新增 `docs/项目架构边界与后续开发规则.md`。  
测试：文档任务，无代码测试；最终全量检查覆盖项目可运行性。

## P3：暂缓事项

状态：需暂缓  
处理：只记录，不开发。  
修改文件：`docs/P3暂缓事项.md`。
