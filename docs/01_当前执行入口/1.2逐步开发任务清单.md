# 1.2 逐步开发任务清单

创建日期：2026-06-01
最近更新：2026-06-02
文档定位：执行任务书。本文档从 `docs/06_产品规划/0版网页整合与GitHub参考优化路线.md` 拆分而来，用于后续一步一步开发、验收、交接。2026-06-02 已更名为 1.2 清单，重点跟踪 P1-P4 第一版后的未完成补齐项。

## 0. 执行总规则

所有任务必须遵守：

- 不把“0版网页”作为用户端入口名称，对用户统一称为“支持性测评”或“阶段性画像”。
- 不使用“人格”作为学生端画像文案。
- 不输出医学诊断、危机干预承诺或 AI 自由心理咨询。
- 高风险文本不进入普通自动反馈，必须进入人工复核。
- 自由文本原文默认不导出。
- 新增 API 必须同步 `docs/03_技术真相/API接口文档.md`。
- 新增字段必须同步 `docs/03_技术真相/数据库字段说明.md`。
- 新增心理内容必须同步 `docs/02_专项进度与验收/UI与伦理边界验收清单.md`。
- 每轮任务结束必须更新 `docs/00_当前事实基准/开发日志.md`、`docs/00_当前事实基准/当前进度交接.md`、`docs/00_当前事实基准/开发说明.md`。

## 1. 当前状态

已完成：

- [x] 新增 `content/student_profile_rules.json`。
- [x] 新增 `content/risk_keywords.json`。
- [x] 扩展 `content/training_cards.json`，新增学生端 CBT、情绪命名、自我支持、沙盘表达训练卡。
- [x] 扩写 `docs/06_产品规划/0版网页整合与GitHub参考优化路线.md` 为执行级总纲。
- [x] 扩写 `D:\codex\workspace\safehome1.0其他内容\GitHub参考项目细节分析.docx`。
- [x] 同步 `docs/02_专项进度与验收/UI与伦理边界验收清单.md` 中的画像、风险、导出、沙盘边界检查项。
- [x] 同步 `docs/06_产品规划/量表与工作表接入方案.md` 中的学生支持性画像测评规划。
- [x] 确认 `docs/03_技术真相/API接口文档.md` 与 `docs/03_技术真相/数据库字段说明.md` 已更新为：画像基础接口、`student_profiles`、`records`、`audit_logs` 已实现。
- [x] 新增 `docs/00_当前事实基准/项目进度统一口径.md`。
- [x] 新增 `docs/06_产品规划/后续上线措施.md`。
- [x] 在 `content/assessment_worksheets.json` 增加 `student_profile_v1` 学生支持性画像测评条目。
- [x] 完成 P1-0 代码阅读。
- [x] 新增 `backend/services/content_loader.py` 内容加载器。
- [x] 新增 `backend/services/risk_service.py` 风险检查服务。
- [x] 新增 `backend/services/profile_service.py` 画像规则服务。
- [x] 新增 `backend/routes/profile.py`，实现 `POST /api/profile`、`POST /api/risk/check`、`GET /api/model/info`。
- [x] `POST /api/profile` 复用 `assessment_results` 保存画像结果，默认不保存自由文本原文。
- [x] 同步 `shared` 类型、Web API client、小程序 API client 和 `docs/03_技术真相/API接口文档.md`。
- [x] P2-1 小程序“测一测”入口高亮 `student_profile_v1` 并补充非诊断说明。
- [x] P2-2 小程序学生画像测评提交调用 `POST /api/profile`。
- [x] P2-3 小程序结果页最小展示画像名称、置信度、风险状态和推荐训练卡。
- [x] P2-4 小程序画像结果页可按推荐训练卡 ID 进入训练卡页，高风险状态不显示普通训练卡入口。
- [x] 小程序画像结果页已补充维度解释、支持性解释、优势提示、小步行动和高风险现实支持提示。
- [x] P2-Web-1 Web 后台新增 `/profiles` 学生画像列表。
- [x] P2-Web-2 Web 后台支持 `/profiles/<id>` 画像详情 URL。
- [x] P2-Web-3 `GET /api/admin/export?type=profile` 脱敏导出。
- [x] P3-2 Web 后台新增 `/reviews` 人工复核列表。
- [x] P3-1 Web 后台周报页新增画像复测、需复核、高风险和练习积累趋势。
- [x] P4-1 新增 `student_profiles`，生成画像时同步写入。
- [x] P4-2 新增 `records`，生成画像时同步写入研究摘要。
- [x] P4-3 新增 `audit_logs`，记录画像详情查看和后台导出。
- [x] 在 `docs/03_技术真相/数据库字段说明.md` 补充画像导出、`records.data_json` 和 `audit_logs` 数据字典。

当前仍未完成：

- [ ] 1.2-5 试点前人工验收，需在本地浏览器和微信开发者工具中人工确认页面体验。

后续能力，不属于当前 1.2 开发闭环：

- [ ] 规则编辑后台和规则修改审计。当前后台只做规则只读查看，后续如新增编辑入口，必须同步审计日志。

## 2. P0：内容库与文档准备

P0 目标：不改后端、不改数据库、不改页面逻辑，先把内容规则、风险边界、测评入口和文档同步准备好。

### P0-1 学生画像规则库

状态：已完成。

文件：

- `content/student_profile_rules.json`

已完成内容：

- `pressure_alert_profile`
- `profile_code`: `pressure_alert`
- 展示名：`压力警觉型画像`
- 推荐训练卡：
  - `student_emotion_naming`
  - `cbt_auto_thought_student`
  - `self_support_statement`
  - `sandplay_expression_01`

验收：

- [x] JSON 合法。
- [x] 推荐训练卡 ID 均存在。
- [x] 用户端边界文案不含“人格”。
- [x] 包含 `review_status`、`version`、`risk_level`、`requires_review`。

### P0-2 风险关键词库

状态：已完成。

文件：

- `content/risk_keywords.json`

已完成内容：

- 自伤或自杀风险。
- 伤害他人风险。
- 被侵害或现实安全威胁。
- 极端痛苦或明显功能受损。

验收：

- [x] JSON 合法。
- [x] high 风险设置 `requires_human_review`。
- [x] high 风险不允许普通自动反馈。
- [x] high 风险不允许普通训练卡推荐。
- [x] 自由文本原文默认不导出。

### P0-3 学生端训练卡扩展

状态：已完成。

文件：

- `content/training_cards.json`

已新增训练卡：

- `cbt_auto_thought_student`
- `student_emotion_naming`
- `self_support_statement`
- `sandplay_expression_01`

验收：

- [x] JSON 合法。
- [x] 训练卡 ID 无重复。
- [x] 每张卡包含 `id`、`type`、`title`、`purpose`、`tags`、`steps`、`example`、`duration_minutes`、`enabled`。
- [x] 训练卡文案不承诺治疗，不做诊断。

### P0-4 学生支持性画像测评条目

状态：已完成。

文件：

- `content/assessment_worksheets.json`

目标：

- 在“测一测”内容库中增加学生画像入口。
- 该条目用于后续小程序显示“学生支持性画像测评”。
- 第一版只做内容配置，不接 API。

建议新增条目：

```json
{
  "id": "student_profile_v1",
  "source_file": "夏老师项目计划书与学生画像规则",
  "source_title": "学生支持性画像测评",
  "display_title": "学生支持性画像测评",
  "category": "学生画像",
  "instructions": "本测评用于了解近期压力反应和支持资源，不用于诊断。",
  "sections": [],
  "questions": [],
  "scoring": "第一版仅保存分数和文本，后续由画像规则生成阶段性支持反馈。",
  "recommended_card_ids": ["student_emotion_naming", "cbt_auto_thought_student"],
  "source_version": "2026.06-student-profile-v1-draft"
}
```

必须包含的问题维度：

- 考试焦虑。
- 不确定性不耐受 `iu_score`。
- 恐惧或压力警觉倾向 `fear_score`。
- 自我支持或自我同情 `self_compassion`。
- 自由文本，可选。

验收：

- [x] JSON 合法。
- [x] `id` 不重复。
- [x] `category` 使用 `学生画像`。
- [x] `display_title` 不含“诊断”“人格”。
- [x] `recommended_card_ids` 均存在。
- [x] `boundary_notice` 或 `instructions` 明确非诊断。

### P0-5 UI 与伦理边界清单同步

状态：已完成。

文件：

- `docs/02_专项进度与验收/UI与伦理边界验收清单.md`

已同步：

- 学生画像结果页检查项。
- 高风险文本检查项。
- 画像导出检查项。
- 沙盘表达检查项。
- 禁用词检查项。

必须写入的检查标准：

- 不使用“人格”。
- 不使用“系统判断你有……”。
- 不使用“障碍、异常、病态”。
- 每个报告页显示“不是诊断”。
- 高风险文本不生成普通训练建议。
- 导出默认隐藏自由文本原文。

验收：

- [x] 清单中出现“学生画像结果页”。
- [x] 清单中出现“高风险复核”。
- [x] 清单中出现“导出脱敏”。
- [x] 清单中出现“禁用词”。

### P0-6 量表与工作表接入方案同步

状态：已完成。

文件：

- `docs/06_产品规划/量表与工作表接入方案.md`

已同步：

- 学生支持性画像测评说明。
- 输入字段说明。
- 输出结果说明。
- 与训练卡的连接。
- 与后续 `POST /api/profile` 的连接。

验收：

- [x] 文档说明学生画像测评不是诊断量表。
- [x] 文档写明输入包含量表分数和可选自由文本。
- [x] 文档写明输出包含画像、置信度、维度、推荐任务、边界声明。

### P0-7 API 与数据库规划文档确认

状态：已完成。

文件：

- `docs/03_技术真相/API接口文档.md`
- `docs/03_技术真相/数据库字段说明.md`

任务：

- 确认 `POST /api/profile`、`POST /api/risk/check`、`GET /api/model/info` 已从规划更新为基础已实现接口。
- 确认短期保存策略已执行：第一版复用 `assessment_results`。
- 确认已新增 `student_profiles`，不再使用旧规划名 `profile_results`。
- 确认已新增 `records` 和 `audit_logs` 第一版。

验收：

- [x] API 文档有画像基础接口说明。
- [x] 数据库文档已更新为 `assessment_results` 兼容保存、`student_profiles` 专用保存、`records` 研究摘要、`audit_logs` 审计记录。

### P0-8 开发记录同步

状态：已完成。

文件：

- `docs/00_当前事实基准/项目进度统一口径.md`
- `docs/00_当前事实基准/开发日志.md`
- `docs/00_当前事实基准/当前进度交接.md`
- `docs/00_当前事实基准/开发说明.md`

任务：

- 记录本轮 P0 内容库准备。
- 写明已完成和下一步，并在后续 P1/P2 完成后持续更新。
- 写明当轮是否改后端、数据库或页面。

验收：

- [x] 开发日志有本轮条目。
- [x] 当前进度交接已更新到当前下一步。
- [x] 开发说明用零基础语言解释新增内容库、画像 API、小程序链路和后台导出的作用。

## 3. P1：画像 API 最小闭环

P1 目标：实现规则版 `POST /api/profile`，先不接复杂模型。

### P1-0 代码阅读

必须先读：

- `backend` 入口文件。
- 当前 route 注册方式。
- 当前数据库工具。
- 当前 `assessment_results` 保存逻辑。
- 当前导出接口。
- `shared/types/api.ts`。
- 小程序 `services/api.js`。

验收：

- [x] 确认后端路由组织方式。
- [x] 确认内容库 JSON 读取方式。
- [x] 确认是否已有通用响应格式。

### P1-1 内容加载器

状态：已完成。

建议文件：

- `backend/services/content_loader.py`

职责：

- 读取 `content/student_profile_rules.json`。
- 读取 `content/risk_keywords.json`。
- 读取 `content/training_cards.json`。
- JSON 文件缺失时返回明确错误。

验收：

- [x] 能读取画像规则。
- [x] 能读取风险关键词。
- [x] 能读取训练卡。
- [x] 文件缺失时不让后端崩溃。

### P1-2 风险检查服务

状态：已完成。

建议文件：

- `backend/services/risk_service.py`

输入：

```json
{
  "text": "我最近很想消失",
  "source": "student_profile"
}
```

输出：

```json
{
  "risk_level": "high",
  "matched_categories": ["self_harm"],
  "requires_review": true,
  "allow_auto_feedback": false,
  "safe_response": "..."
}
```

验收：

- [x] 命中 high 风险返回 `requires_review=true`。
- [x] high 风险返回 `allow_auto_feedback=false`。
- [x] 空文本返回 low。
- [x] 不输出诊断结论。

### P1-3 画像规则服务

状态：已完成。

建议文件：

- `backend/services/profile_service.py`

职责：

- 校验分数字段。
- 计算或模拟 `profile_code`。
- 计算 `confidence`。
- 匹配 `student_profile_rules.json`。
- 返回画像名称、维度、优势提示、推荐任务、边界声明。

第一版规则：

- `test_anxiety >= 3.5` 且 `iu_score >= 3.5`，输出 `pressure_alert`。
- `confidence` 默认可按规则强度计算，也可第一版固定为 `0.76`。
- 如果缺少必要分数，返回 400。
- 如果风险检查为 high，不返回普通画像内容。

验收：

- [x] 正常分数返回 `pressure_alert`。
- [x] 低置信度样本不显示强标签。
- [x] high 风险不返回普通训练卡。
- [x] 推荐训练卡 ID 都存在。

### P1-4 后端路由

状态：已完成。

建议文件：

- `backend/routes/profile.py`

接口：

- `POST /api/profile`
- `POST /api/risk/check`
- `GET /api/model/info`

验收：

- [x] `POST /api/profile` 可本地调用。
- [x] `POST /api/risk/check` 可本地调用。
- [x] `GET /api/model/info` 返回规则版本。
- [x] 错误响应格式与现有 API 风格一致。

### P1-5 保存策略

状态：已完成。

第一版默认选择：

- 复用 `assessment_results`，不新增数据库表。

保存建议：

- `worksheet_id`: `student_profile_v1`
- `worksheet_title`: `学生支持性画像测评`
- `category`: `学生画像`
- `answers_json`: 原始分数和可选文本摘要
- `scores_json`: 画像结果、置信度、维度、风险等级、推荐任务、规则版本

验收：

- [x] 画像结果能保存。
- [x] 不保存不必要敏感字段。
- [x] 后续能按 `worksheet_id=student_profile_v1` 查询。

### P1-6 shared 类型同步

状态：已完成。

文件：

- `shared/types/api.ts`

新增类型：

- `ProfileResult`
- `ProfileDimension`
- `RiskCheckResult`

验收：

- [x] 类型字段和 API 响应一致。
- [x] 小程序和网页端能复用。

### P1-7 P1 测试场景

必须测试：

| 场景 | 输入 | 期望 |
|---|---|---|
| 正常画像 | 焦虑/IU 偏高 | 返回 `pressure_alert` |
| 空自由文本 | 只有分数 | 正常返回 |
| 高风险文本 | 含自伤关键词 | `requires_review=true` |
| 低置信度 | 分数接近中间 | 不强推画像 |
| 缺字段 | 缺 `iu_score` | 400 |
| JSON 文件缺失 | 临时移动规则文件 | 返回系统错误，不崩溃 |

当前验证状态：

- [x] 正常画像。
- [x] 空自由文本。
- [x] 高风险文本。
- [x] 低置信度。
- [x] 缺字段。
- [x] JSON 文件缺失错误路径已用临时内容目录自动测试，确认返回 `content_load_error`，不崩溃。

## 4. P2：小程序学生画像页面

P2 小程序目标：

```text
测一测 -> 学生支持性画像测评 -> 画像报告 -> 推荐训练卡 -> 打卡
```

### P2-1 测一测入口

状态：已完成。

文件：

- `apps/miniprogram/pages/assessment`

任务：

- 显示 `student_profile_v1`。
- 展示标题“学生支持性画像测评”。
- 展示非诊断说明。

验收：

- [x] 用户能看到学生画像测评入口。
- [x] 页面不出现“诊断”“人格”。

### P2-2 API 封装

状态：已完成。

文件：

- `apps/miniprogram/services/api.js`

任务：

- 新增 `submitProfileAssessment`。
- 新增 `riskCheck` 可选封装。

验收：

- [x] 小程序可调用 `POST /api/profile`。
- [x] 失败时不白屏。

### P2-3 画像结果页

状态：已完成。

文件：

- 可复用 `apps/miniprogram/pages/assessment-result`
- 或新增 `apps/miniprogram/pages/profile-result`

页面内容：

- 阶段性画像标题。
- 画像名称。
- 置信度。
- 维度解释。
- 优势提示。
- 推荐任务。
- 非诊断边界。

高风险状态：

- 不展示普通画像。
- 展示安全提示。
- 提供人工支持入口。

验收：

- [x] 正常画像页可显示。
- [x] 高风险页不显示普通训练建议。
- [x] 用户可跳转推荐训练卡。
- [x] 用户可稍后再练习。

### P2-4 训练卡跳转与打卡

状态：已完成最小版。

文件：

- `apps/miniprogram/pages/training`
- `apps/miniprogram/pages/checkin`

任务：

- 从画像结果页带 `card_id` 进入训练卡。
- 训练完成后进入打卡。
- 打卡记录练习前后评分。

验收：

- [x] 推荐训练卡可打开。
- [x] 打卡不显示断签失败。
- [x] 打卡数据可进入周报。

## 5. P2：网页后台画像列表与导出

### P2-Web-1 后台画像列表

状态：已完成。

文件：

- `apps/web` 对应路由和页面。

列表字段：

- 匿名 ID。
- 画像名称。
- 置信度。
- 风险等级。
- 是否需复核。
- 推荐任务。
- 提交时间。

验收：

- [x] 后台能查看画像列表。
- [x] 默认不展示自由文本原文。
- [x] 默认不展示联系方式。

### P2-Web-2 画像详情页

状态：已完成。

内容：

- 基础信息。
- 画像结果。
- 维度说明。
- 文本摘要。
- 推荐任务。
- 复核状态。
- 人工备注。

验收：

- [x] 能查看单条画像详情。
- [x] 敏感原文默认隐藏。
- [x] 人工备注不自动同步到学生端。

### P2-Web-3 `type=profile` 导出

状态：已完成。

文件：

- 后端导出接口。
- 网页导出页。

导出字段：

- `anonymous_id`
- `profile_code`
- `profile_name`
- `confidence`
- `risk_level`
- `requires_review`
- `dimension_summary`
- `recommended_task_ids`
- `created_at`

默认不导出：

- `user_id`
- 联系方式。
- 微信标识。
- 自由文本原文。

验收：

- [x] `type=profile` 可导出。
- [x] 默认匿名化。
- [x] 默认不含自由文本原文。
- [x] 导出文档或数据字典说明字段含义。

## 6. P3：周报趋势与人工督导

### P3-1 周报趋势增强

状态：已完成。Web 后台已展示画像复测、需复核、高风险和练习积累趋势；后端 `GET /api/weekly-report` 已直接接入 `student_profiles` 多轮画像趋势。

数据来源：

- 情绪记录标签。
- 训练卡完成情况。
- 打卡前后评分。
- 学生画像复测结果。

周报模块：

- 本周高频标签。
- 本周重复模式。
- 本周练习积累。
- 下周一个建议。

验收：

- [x] 周报至少展示一个趋势。
- [x] 周报不评价用户好坏。
- [x] 周报给一个小任务。
- [x] 后端周报结果直接返回画像复测次数、主要画像变化、需复核数量和高风险数量。

### P3-2 人工复核列表

状态：已完成。

条件：

- `risk_level=high`
- `requires_review=true`
- `confidence` 低于阈值。
- 用户主动质疑结果。

验收：

- [x] 后台能筛选需复核个案。
- [x] 高风险个案不生成普通自动建议。
- [x] 人工备注不覆盖系统报告。
- [x] 人工备注、复核结论和处置状态可单独保存到 `profile_reviews`。
- [x] 保存复核会写入 `audit_logs` 的 `review_profile` 操作。

## 7. P4：研究平台化

### P4-1 新增 `student_profiles`

状态：已完成。

适用条件：

- P1/P2 已稳定。
- 画像结果查询和导出需求明显增加。

字段：

- `id`
- `user_id`
- `anonymous_id`
- `round`
- `source`
- `scores_json`
- `text_features_json`
- `profile_code`
- `profile_name`
- `confidence`
- `dimensions_json`
- `recommended_task_ids_json`
- `risk_level`
- `requires_review`
- `boundary_notice`
- `rules_version`
- `export_allowed`
- `data_quality`
- `created_at`
- `updated_at`

验收：

- [x] 不破坏 `assessment_results`。
- [x] 画像结果可按轮次查询。
- [x] 导出可按匿名 ID 串联。

### P4-2 新增 `records`

状态：已完成。已写入学生画像研究摘要；`type=records` 统一研究导出入口已补齐。

定位：

- 统一研究导出入口。
- 不替代业务表。

验收：

- [x] `module_type` 可区分 `student_profile`。
- [x] `data_json` 只存研究摘要。
- [x] 学生画像数据字典已写入数据库字段说明。
- [x] 新增 `records` 统一导出或查询入口，默认只导出摘要和匿名 ID。

### P4-3 新增 `audit_logs`

状态：已完成第一版，已接入画像查看、导出、人工复核保存和高风险导出确认审计。

记录：

- 查看敏感详情。
- 导出数据。
- 修改规则，待后续有规则编辑后台后接入。
- 人工复核。

验收：

- [x] 导出有日志。
- [x] 敏感查看有日志。
- [x] 人工复核保存有日志。
- [x] 当前无规则编辑入口，已在文档中明确后续新增规则编辑后台时必须补审计。
- [x] 高风险画像导出二次确认写入审计元数据。

## 8. 1.2 未完成开发清单

本节只记录当前代码核对后确认仍未完成或只完成第一版的工作。优先级按试点风险排序。

### 1.2-1 后台画像规则查看

状态：已完成。

现状：

- Web 后台已有 `/content/rules`。
- 当前页面读取的是 `content/feedback_rules.json`，用于反馈规则查看。
- `content/student_profile_rules.json` 画像规则库尚未在后台形成独立只读查看。

目标：

- 在后台提供画像规则查看入口，可复用 `/content/rules` 的 tab，也可新增 `/content/profile-rules`。
- 展示画像规则版本、启用状态、画像编码、画像名称、触发条件摘要、推荐训练卡和安全说明。
- 不展示可诱导复制的高风险触发词细节；必要时只展示“高风险由风险关键词库处理”。

验收：

- [x] 后台能查看 `student_profile_rules.json` 的规则版本和规则列表。
- [x] 能看到 `pressure_alert` 的画像名、维度摘要、推荐训练卡和边界声明。
- [x] 页面不提供画像规则编辑入口。
- [x] 不提供编辑入口，除非同步实现规则修改审计。

### 1.2-2 后端周报接入多轮画像趋势

状态：已完成。

现状：

- Web `/reports` 页面会额外读取测评结果和打卡，展示画像复测、需复核、高风险和练习积累。
- 后端 `GET /api/weekly-report` 仍主要基于 `emotion_diaries`、`feedback_results`、`checkins` 生成周报。
- `student_profiles` 多轮画像未直接进入后端周报结果。

目标：

- `build_weekly_report` 读取本周 `student_profiles`。
- 返回画像复测次数、主要画像类型、需复核数量、高风险数量。
- 保留现有高频场景、情绪、标签频次、训练卡完成情况。
- 下周建议综合画像风险和训练卡完成情况；高风险优先提示人工关注，不进入普通训练建议。

验收：

- [x] `GET /api/weekly-report` 响应中包含画像趋势字段。
- [x] 多轮画像可按 `round` 或创建时间参与趋势统计。
- [x] 高风险或需复核画像会影响 `next_week_suggestion`。
- [x] API 文档和 shared 类型同步。

### 1.2-3 `records` 统一研究导出

状态：已完成。

现状：

- `records` 表已创建。
- `POST /api/profile` 会写入 `records.module_type=student_profile`。
- 后台导出还没有独立 `type=records`。

目标：

- 新增 `GET /api/admin/export?type=records` 或等价统一研究摘要导出。
- 支持按 `user_id`、`module_type` 筛选。
- 默认只导出匿名 ID、模块类型、来源 ID、摘要 JSON、创建时间和授权标记。
- 不导出自由文本原文、高风险文本原文、联系方式。

验收：

- [x] `type=records` 可导出。
- [x] 支持 `module_type=student_profile` 筛选。
- [x] 导出写入 `audit_logs.action=export_records`。
- [x] 数据库字段说明和 API 文档同步。

### 1.2-4 高风险导出二次确认

状态：已完成。

现状：

- `GET /api/admin/export` 已要求 `X-Admin-Token`。
- `type=profile` 已默认匿名化，不导出自由文本原文。
- 当前没有“本次导出包含高风险记录，需二次确认”的后端参数和前端确认流程。

目标：

- 后端识别导出结果是否包含 `risk_level=high` 或 `requires_review=1`。
- 如果包含高风险记录，默认要求额外确认参数，例如 `confirm_high_risk=true`。
- Web 导出页在保存 CSV 前弹出或显示明确确认提示。
- 审计日志记录是否包含高风险记录、是否完成确认和导出行数。

验收：

- [x] 未确认时，高风险画像导出返回明确错误。
- [x] 确认后可导出，但仍不包含自由文本原文。
- [x] Web 导出页有高风险确认提示。
- [x] `audit_logs.metadata_json` 记录 `contains_high_risk` 和 `confirmed_high_risk_export`。

### 1.2-5 试点前人工验收

状态：自动预检已完成，人工验收未完成。

范围：

- 小程序：测一测 -> 学生画像测评 -> 画像报告 -> 推荐训练卡跳转。
- Web：`/profiles`、`/profiles/<id>`、`/reviews`、`/reports`、`/export`、画像规则查看入口。
- 数据：导出匿名化、自由文本不导出、高风险不进入普通训练建议。

验收：

- [x] 自动预检已完成：Web 页面可达、后端关键接口可达、小程序页面文件和 app.json 注册存在。
- [ ] 浏览器逐页检查完成。
- [ ] 微信开发者工具链路检查完成。
- [x] `docs/02_专项进度与验收/UI与伦理边界验收清单.md` 已记录自动预检结果和待人工确认问题。
- [ ] 验收问题按 P0/P1/P2 排序后再开发。

## 9. 每轮任务结束检查

每轮必须执行：

```powershell
git status --short
```

内容库 JSON 校验：

```powershell
$code = @'
import json
from pathlib import Path
root = Path(r"D:\codex\workspace\safehome1.0")
for rel in [
    "content/student_profile_rules.json",
    "content/risk_keywords.json",
    "content/training_cards.json",
    "content/assessment_worksheets.json"
]:
    if (root / rel).exists():
        json.loads((root / rel).read_text(encoding="utf-8"))
        print(rel, "OK")
'@
$code | python -
```

Markdown 格式检查：

```powershell
git diff --check -- docs
```

如果修改后端：

- 运行已有后端测试或最小 API smoke test。
- 至少手动调用新增接口一次。

如果修改小程序：

- 检查页面能打开。
- 检查 API 失败状态。
- 检查高风险状态。

如果修改 Word：

- 不做渲染检查，除非用户重新要求。
- 颜色只允许黑色和红色。

## 10. 最推荐的下一步

下一步执行：

```text
进入 1.2-5 试点前人工验收。
```

原因：

- P1 画像 API、风险检查和模型信息接口已完成。
- `POST /api/profile` 已能生成并保存画像结果。
- shared、Web API client、小程序 API client 已同步接口。
- 学生画像已经从“后端可用”进入“小程序可见、可提交、可展示”的最小状态。
- 小程序画像测评、结果页、训练卡跳转、打卡已形成最小闭环。
- 研究者/教师端已经能查看画像结果和风险状态。
- Web 周报趋势和人工复核列表已完成第一版。
- P4 研究平台化表已完成第一版，`records` 统一导出和高风险导出二次确认已补齐。
- 后续进入 1.2-5 人工验收；规则修改审计待后续有规则编辑后台后接入。

下一步按 `docs/02_专项进度与验收/UI与伦理边界验收清单.md` 检查小程序画像页、Web 画像页、复核页、周报页、规则页和导出页。

