# 数据库字段说明

最后更新时间：2026-07-24

本文档定义安心陪伴 MVP 1.0 的 SQLite 第一版数据表。字段命名采用英文 snake_case，展示文案使用中文。

进度口径：当前真实数据库表以前文已实现表和第 11 节研究平台化表为准；学生画像同时写入 `assessment_results` 和 `student_profiles`，研究摘要写入 `records`，导出/查看/人工复核写入 `audit_logs`。`records` 统一导出和高风险导出确认审计元数据已补齐。当前总进度见 `docs/00_当前事实基准/项目进度统一口径.md`。

阅读方式：先按表名查字段；如历史规划文档提到 `profile_results` 或尚未创建的研究表，以本文第 11 节和 `docs/00_当前事实基准/项目进度统一口径.md` 的当前状态为准。导出 CSV 字段含义、脱敏和敏感性说明见 `docs/03_技术真相/数据字典.md`。

## 0. 导出数据字典

2026-06-04 已新增 `docs/03_技术真相/数据字典.md`，用于说明 `GET /api/admin/export` 各类 CSV 导出的字段名、中文含义、数据类型、是否脱敏、是否敏感、研究用途和缺失说明。

导出前必须确认：

```text
1. 用户授权和研究授权；
2. 是否包含高风险或需复核记录；
3. 是否需要进一步去除可识别身份信息；
4. 不把导出数据用于诊断、治疗、危机干预或法律判断。
```

## 0A. 常用索引

2026-06-04 已在 `backend/models.py` 中新增 `INDEX_SQL`，`init_db()` 会幂等创建以下索引：

| 索引 | 字段 | 主要服务的查询 |
|---|---|---|
| `idx_emotion_diaries_user_created` | `emotion_diaries(user_id, created_at)` | 情绪记录按用户查询 |
| `idx_emotion_thermometer_user_created` | `emotion_thermometer(user_id, created_at)` | 情绪温度计按用户和日期查询 |
| `idx_feedback_results_user_created` | `feedback_results(user_id, created_at)` | 反馈结果按用户查询 |
| `idx_student_profiles_user_created` | `student_profiles(user_id, created_at)` | 画像历史按用户查询 |
| `idx_student_profiles_risk_created` | `student_profiles(risk_level, created_at)` | 高风险画像筛选 |
| `idx_risk_review_status_created` | `risk_review_records(review_status, created_at)` | 风险复核队列 |
| `idx_audit_logs_action_created` | `audit_logs(action, created_at)` | 审计日志按动作查询 |
| `idx_records_module_created` | `records(module_type, created_at)` | records 研究摘要按模块导出 |
| `idx_assessment_worksheets_audience_enabled` | `assessment_worksheets(audience_class, enabled_for_user)` | 测一测列表按对象和开放状态筛选 |

## 1. `users`

保存家长弱身份信息或测试用户信息。第一版不采集强身份信息。

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | TEXT PRIMARY KEY | 用户 ID |
| `nickname` | TEXT | 昵称或测试编号 |
| `role` | TEXT | `parent`、`student`、`admin` |
| `source` | TEXT | 来源渠道 |
| `created_at` | TEXT | 创建时间 |
| `updated_at` | TEXT | 更新时间 |

## 1A. `consent_records`

保存用户使用说明、隐私说明、非诊断边界说明和匿名研究授权等同意记录。第一版只保存匿名 `user_id`，不采集真实姓名、手机号或身份证号。

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | TEXT PRIMARY KEY | 同意记录 ID |
| `user_id` | TEXT | 匿名用户 ID |
| `consent_type` | TEXT | 同意类型，例如 `user_agreement`、`privacy_policy`、`non_diagnostic_notice`、`research_authorization` |
| `consent_version` | TEXT | 同意文本版本 |
| `agreed` | INTEGER | 1 表示同意，0 表示不同意或撤回 |
| `agreed_at` | TEXT | 记录该选择的时间 |
| `revoked_at` | TEXT | 不同意或撤回时间，可为空 |
| `created_at` | TEXT | 创建时间 |

边界：

- `research_authorization` 可为 0，不应影响基础使用；
- 正式试点前如需撤回流程，可以继续复用同表新增一条 `agreed=0` 记录；
- 该表不是登录权限表，不保存强身份信息。

## 1B. `risk_review_records`

保存 high/medium 风险初筛后的人工关注队列。该表用于记录处理状态，不保存完整高风险自由文本原文。

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | TEXT PRIMARY KEY | 风险复核记录 ID |
| `user_id` | TEXT | 匿名用户 ID |
| `source_type` | TEXT | 来源类型，例如 `feedback`、`student_profile` |
| `source_id` | TEXT | 来源记录 ID |
| `risk_level` | TEXT | `medium` 或 `high` 为主要复核对象 |
| `matched_categories_json` | TEXT | 命中的风险类别和关键词摘要，不保存完整原文 |
| `review_status` | TEXT | `pending`、`reviewed`、`follow_up_needed`、`transferred`、`closed` |
| `reviewer_id` | TEXT | 复核人匿名 ID |
| `review_note` | TEXT | 后台复核备注 |
| `action_taken` | TEXT | 已采取的最小处置动作 |
| `closed_reason` | TEXT | 关闭或暂不继续处理的原因 |
| `reviewed_at` | TEXT | 复核时间 |
| `created_at` | TEXT | 创建时间 |
| `updated_at` | TEXT | 更新时间 |

边界：

- `POST /api/feedback/generate` 和 `POST /api/profile` 命中 medium/high 风险时会自动创建记录；
- 复核状态更新会写入 `audit_logs.action=review_risk`；
- 该表不代表系统提供实时危机干预，只用于人工关注流转。

## 1C. `schema_migrations`

保存数据库 schema 初始化版本，避免后续不知道当前库是否升级过。

| 字段 | 类型 | 说明 |
|---|---|---|
| `version` | TEXT PRIMARY KEY | 迁移版本，例如 `2026_07_01_001` |
| `name` | TEXT | 迁移名称，例如 `assessment_worksheets_and_profile_positions` |
| `applied_at` | TEXT | 记录时间 |

当前不使用 Alembic，不迁移 PostgreSQL；该表只做 SQLite 试用阶段的最小版本留痕。

## 1D. `emotion_thermometer`

保存用户当天可多次记录的情绪温度计。该表只用于自我观察、日曲线和练习提示，不保存诊断、筛查或人格结论。

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | TEXT PRIMARY KEY | 温度计记录 ID |
| `user_id` | TEXT | 匿名用户 ID |
| `intensity_level` | INTEGER | 1-10 的情绪强度 |
| `valence_level` | INTEGER | 1-10 的愉悦度/不愉悦度观察，可为空 |
| `arousal_level` | INTEGER | 1-10 的身体唤起水平观察，可为空 |
| `control_level` | INTEGER | 1-10 的可控感观察，可为空 |
| `emotion_label` | TEXT | 情绪命名，可为空，不作为诊断标签 |
| `brief_text` | TEXT | 200 字内简短备注，可为空 |
| `created_at` | TEXT | 创建时间 |
| `updated_at` | TEXT | 更新时间 |

查询约定：

```text
GET /api/emotion-thermometer/day 先按`user_id`使用索引读取，再把`created_at`转换为`Asia/Shanghai`本地日期筛选当天记录。
索引：idx_emotion_thermometer_user_created。
```

2026-07-05 任务十补充说明：`valence_level`、`arousal_level`、`control_level` 和 `emotion_label` 是轻量观察字段，用于周报和阶段性反馈中的过程复盘；不能用于诊断、筛查、人格定性或风险预测。

2026-07-27 补充：记录回执是接口计算字段，不写入数据库，不新增表或列。回执只依据该用户自己的强度记录生成描述性文字。

## 1E. `assessment_worksheets`

保存小程序“测一测”的结构化测评量表。2026-06-30 起，`init_db()` 会把 `content/assessment_worksheets.json` 幂等同步到该表，`/api/assessments` 优先从数据库读取，数据库不可用时回退 content JSON。

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | TEXT PRIMARY KEY | 量表 ID |
| `display_title` | TEXT | 用户端展示标题 |
| `source_title` | TEXT | 来源标题 |
| `source_file` | TEXT | 来源文件名 |
| `category` | TEXT | 分类 |
| `audience_class` | TEXT | 对象大类，例如 student、parent、adult |
| `reflex_node` | TEXT | 情绪反射弧或主题节点 |
| `questions_json` | TEXT | 题项数组 JSON |
| `dimensions_json` | TEXT | 维度数组 JSON |
| `dimension_score_method` | TEXT | 维度计分方式，例如 sum、mean |
| `scoring_notes_json` | TEXT | 计分备注 JSON |
| `search_keywords_json` | TEXT | 搜索关键词 JSON |
| `boundary_notice` | TEXT | 条目边界说明 |
| `result_disclaimer` | TEXT | 结果页免责声明 |
| `instructions` | TEXT | 填写说明 |
| `sensitive_category` | TEXT | 敏感语义类别 |
| `profile_model_id` | TEXT | 默认关联画像模型 ID |
| `enabled_for_user` | INTEGER | 是否允许用户端填写，1/0 |
| `review_status` | TEXT | 审核状态 |
| `review_note` | TEXT | 审核备注 |
| `source_version` | TEXT | 来源版本 |
| `source_type` | TEXT | 来源类型 |
| `audience` | TEXT | 目标对象说明 |
| `audience_class_detail` | TEXT | 对象细分说明 |
| `recommended_card_ids_json` | TEXT | 推荐训练卡 ID JSON |
| `sections_json` | TEXT | 分区结构 JSON |
| `scoring` | TEXT | 计分说明 |
| `pages` | INTEGER | 来源页数 |
| `_meta_json` | TEXT | 扩展元数据 JSON |
| `created_at` | TEXT | 创建时间 |
| `updated_at` | TEXT | 更新时间 |

边界：

- 该表是内容库入库后的管理表，不代表所有量表都已完成正式开放审核；
- `enabled_for_user=1` 也不能替代人工对题项、来源、计分和伦理边界的最终复核；
- 不在该表保存用户作答结果，用户结果仍保存到 `assessment_results`。

## 2. `goals`

保存目标设定。

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | TEXT PRIMARY KEY | 目标 ID |
| `user_id` | TEXT | 用户 ID |
| `scene` | TEXT | 高频亲子冲突场景 |
| `smart_goal` | TEXT | 7 天 SMART 小目标 |
| `motivation` | TEXT | 改变动机 |
| `start_date` | TEXT | 开始日期 |
| `status` | TEXT | `active`、`done`、`paused` |
| `created_at` | TEXT | 创建时间 |
| `updated_at` | TEXT | 更新时间 |

## 3. `emotion_diaries`

保存情绪事件记录。

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | TEXT PRIMARY KEY | 记录 ID |
| `user_id` | TEXT | 用户 ID |
| `goal_id` | TEXT | 关联目标 ID |
| `event_time` | TEXT | 事件发生时间 |
| `scene` | TEXT | 亲子互动或冲突场景 |
| `event_description` | TEXT | 具体事件 |
| `parent_emotion` | TEXT | 家长主要情绪 |
| `parent_emotion_intensity` | INTEGER | 家长情绪强度，1-10 |
| `child_emotion` | TEXT | 孩子主要情绪 |
| `child_emotion_intensity` | INTEGER | 孩子情绪强度，1-10 |
| `automatic_thought` | TEXT | 自动想法 |
| `body_sensation` | TEXT | 身体感觉 |
| `behavior` | TEXT | 当时行为或回应 |
| `raw_text` | TEXT | 原始记录文本 |
| `created_at` | TEXT | 创建时间 |
| `updated_at` | TEXT | 更新时间 |

## 4. `feedback_results`

保存即时反馈结果。

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | TEXT PRIMARY KEY | 反馈 ID |
| `user_id` | TEXT | 用户 ID |
| `diary_id` | TEXT | 情绪事件记录 ID |
| `tags_json` | TEXT | 标签 JSON |
| `trigger_summary` | TEXT | 触发点摘要 |
| `pattern_summary` | TEXT | 互动模式解释 |
| `supportive_feedback` | TEXT | 支持性反馈 |
| `alternative_response` | TEXT | 替代回应建议 |
| `recommended_card_ids_json` | TEXT | 推荐训练卡 ID JSON |
| `risk_level` | TEXT | `low`、`medium`、`high` |
| `created_at` | TEXT | 创建时间 |

## 5. `training_cards`

保存训练卡内容。第一版也可以从 `content/training_cards.json` 读取并同步到数据库。

2026-06-03 补充：`content/training_cards.json` 已增加理论来源、目标技能、适用/禁用情境、复盘问题、审核状态和审核备注等元数据。当前 SQLite `training_cards` 表仍只同步基础展示字段；新增元数据先保留在内容库中，不强制改表。

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | TEXT PRIMARY KEY | 训练卡 ID |
| `type` | TEXT | 卡片类型 |
| `title` | TEXT | 卡片标题 |
| `purpose` | TEXT | 使用目的 |
| `steps_json` | TEXT | 操作步骤 JSON |
| `tags_json` | TEXT | 适用标签 JSON |
| `example` | TEXT | 示例话术 |
| `enabled` | INTEGER | 是否启用，1/0 |
| `version` | TEXT | 内容版本 |
| `created_at` | TEXT | 创建时间 |
| `updated_at` | TEXT | 更新时间 |

## 6. `checkins`

保存练习打卡。

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | TEXT PRIMARY KEY | 打卡 ID |
| `user_id` | TEXT | 用户 ID |
| `card_id` | TEXT | 训练卡 ID |
| `diary_id` | TEXT | 关联情绪记录 |
| `completed` | INTEGER | 是否完成，1/0 |
| `emotion_before` | INTEGER | 练习前情绪强度，1-10 |
| `emotion_after` | INTEGER | 练习后情绪强度，1-10 |
| `reflection` | TEXT | 简短复盘 |
| `created_at` | TEXT | 创建时间 |

## 7. `assessment_results`

保存小程序“测一测”中量表和工作表的填写结果。

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | TEXT PRIMARY KEY | 测一测结果 ID |
| `user_id` | TEXT | 用户 ID |
| `worksheet_id` | TEXT | 工作表 ID |
| `worksheet_title` | TEXT | 工作表标题 |
| `category` | TEXT | 分类 |
| `answers_json` | TEXT | 用户答案 JSON |
| `scores_json` | TEXT | 分数或维度结果 JSON |
| `total_score` | INTEGER | 数值题合计，记录型工作表可为空 |
| `result_summary` | TEXT | 支持性结果摘要 |
| `profile_model_id` | TEXT | 本次结果缓存的画像模型 ID |
| `profile_cluster_id` | TEXT | 本次结果缓存的最接近画像簇 ID |
| `profile_pc1` | REAL | 本次结果在画像模型 PCA 二维图上的横坐标 |
| `profile_pc2` | REAL | 本次结果在画像模型 PCA 二维图上的纵坐标 |
| `profile_confidence` | REAL | 本次画像落点置信度 |
| `created_at` | TEXT | 创建时间 |

说明：

- 第一版不采集姓名、手机号、身份证等强身份信息。
- 结果用于自我观察和练习记录，不构成诊断。
- 旧版自建 UP 工作表和附录示例已从用户端测评内容库下线，不再通过 `/api/assessments`、`/api/assessment-results` 或 `type=assessments` 导出暴露。
- 学生画像第一版使用 `worksheet_id=student_profile_v1`、`category=学生画像` 写入本表。
- 学生画像的 `answers_json` 默认只保存量表分数、支持资源和自由文本脱敏摘要，不保存自由文本原文。
- 2026-06-30 起，通用测评结果也会在有可用画像模型时缓存 `profile_model_id`、`profile_cluster_id`、`profile_pc1`、`profile_pc2` 和 `profile_confidence`，用于 Web 研究看板和结果页展示；这些字段不是诊断结论。

## 8. `weekly_reports`

保存周度报告。

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | TEXT PRIMARY KEY | 周报 ID |
| `user_id` | TEXT | 用户 ID |
| `week_start` | TEXT | 周开始日期 |
| `week_end` | TEXT | 周结束日期 |
| `frequent_scenes_json` | TEXT | 高频场景 JSON |
| `frequent_emotions_json` | TEXT | 高频情绪 JSON |
| `common_patterns_json` | TEXT | 常见互动模式 JSON |
| `completed_cards_json` | TEXT | 已完成训练卡 JSON |
| `next_week_suggestion` | TEXT | 下周建议 |
| `created_at` | TEXT | 创建时间 |

任务十一后，`GET /api/weekly-report` 会即时汇总普通测评结果、情绪温度计趋势和训练效果，并把以下三类摘要写入 `weekly_reports`：

| 字段 | 类型 | 说明 |
|---|---|---|
| `assessment_summary_json` | TEXT / JSON string | 本周相关测评的维度变化、推荐训练卡和边界说明 |
| `thermometer_summary_json` | TEXT / JSON string | 情绪温度计强度、效价、唤醒度、可控感等摘要 |
| `training_effectiveness_summary_json` | TEXT / JSON string | 训练打卡前后变化和按训练卡聚合的过程效果摘要 |

兼容说明：

- 接口仍返回历史字段 `assessment_trend`、`thermometer_trend`；
- 新增返回 `assessment_summary`、`thermometer_summary`、`training_effectiveness_summary`；
- 三个 JSON 字段只保存过程复盘摘要，不保存用户原始文本；
- SQLite 迁移版本为 `2026_07_06_001`，旧库启动时由 `ensure_schema_columns` 自动补列。

## 9. `supervision_requests`

保存人工督导请求。

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | TEXT PRIMARY KEY | 督导请求 ID |
| `user_id` | TEXT | 用户 ID |
| `diary_id` | TEXT | 兼容旧版的关联情绪日记 ID |
| `source_type` | TEXT | 可空；`diary`或`assessment` |
| `source_id` | TEXT | 可空；关联记录 ID，服务端校验属于当前用户 |
| `source_title` | TEXT | 可空；提交时保存的用户可读记录标题 |
| `message` | TEXT | 用户提交内容 |
| `contact` | TEXT | 可选联系方式 |
| `risk_hint` | TEXT | 用户自述风险提示 |
| `risk_level` | TEXT | 系统或人工风险等级 |
| `status` | TEXT | `pending`、`replied`、`closed` |
| `supervisor_reply` | TEXT | 人工回复 |
| `created_at` | TEXT | 创建时间 |
| `replied_at` | TEXT | 回复时间 |

## 10. 数据边界

- 第一版不采集姓名、手机号、身份证等强身份字段。
- 开发环境可使用 `demo-parent` 做本地测试；`APP_ENV=production` 时写入型接口必须传匿名 `user_id`，不得缺省写入 `demo-parent`。
- 任何联系方式字段都应明确告知用途。
- 高风险文本只用于提示人工复核或转介，不由 AI 自动处理。
- 导出数据应尽量使用匿名 ID。

## 11. 0版网页评估画像整合保存策略

本节根据夏老师“学生画像”“records 表结构”“数据导出分析”思维导图整理。短期复用 `assessment_results` 已保留；中长期研究平台化表 `student_profiles`、`records` 和 `audit_logs` 已创建。

### 11.1 短期策略：复用 `assessment_results`

在最小实现阶段，当前已经复用 `assessment_results` 保存学生画像结果：

| 字段 | 用法 |
|---|---|
| `worksheet_id` | 使用画像测评条目 ID，例如 `student_profile_v1` |
| `worksheet_title` | 保存展示标题，例如 `学生支持性画像测评` |
| `category` | 使用 `学生画像` |
| `answers_json` | 保存量表分数、支持资源和自由文本脱敏摘要，不默认保存自由文本原文 |
| `scores_json` | 保存输入分数、画像编码、画像名称、置信度、风险等级、推荐训练卡和规则版本 |
| `result_summary` | 保存支持性解释和非诊断提示 |

优点：无需第一步改表，能快速打通“测评 -> 画像结果 -> 训练卡推荐 -> 历史查询”闭环。

风险：画像结果、普通量表结果和工作表结果混在一张表中，后续查询和导出会变复杂。

### 11.2 `student_profiles`

保存学生画像结果，支持轮次追踪、研究导出和后台复核。

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | TEXT PRIMARY KEY | 画像结果 ID |
| `user_id` | TEXT | 用户 ID 或匿名学生 ID |
| `anonymous_id` | TEXT | 匿名 ID，用于导出 |
| `assessment_result_id` | TEXT | 关联测一测结果，可为空 |
| `round` | INTEGER | 第几轮测评，默认 1 |
| `source` | TEXT | 来源模块，当前为 `student_profile_v1` |
| `scores_json` | TEXT | 输入分数 JSON |
| `text_features_json` | TEXT | 文本特征摘要，不含自由文本原文 |
| `profile_code` | TEXT | 画像编码，例如 `pressure_alert` |
| `profile_name` | TEXT | 画像名称，例如 `压力警觉型画像` |
| `confidence` | REAL | 画像置信度，0-1 |
| `dimensions_json` | TEXT | 维度结果 JSON |
| `recommended_task_ids_json` | TEXT | 推荐训练卡 ID JSON |
| `risk_level` | TEXT | `low`、`medium`、`high` |
| `requires_review` | INTEGER | 是否需要人工复核，1/0 |
| `boundary_notice` | TEXT | 非诊断边界说明 |
| `rules_version` | TEXT | 规则版本 |
| `export_allowed` | INTEGER | 是否允许研究导出，1/0 |
| `data_quality` | TEXT | 数据质量标记，当前默认 `valid` |
| `created_at` | TEXT | 创建时间 |
| `updated_at` | TEXT | 更新时间 |

### 11.3 `records`

统一研究导出入口，适合承载学生画像、家长记录、沙盘任务、打卡等多源数据摘要。该表不建议第一步替换现有业务表。

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | TEXT PRIMARY KEY | 记录 ID |
| `user_id` | TEXT NOT NULL | 用户 ID 或匿名 ID |
| `module_type` | TEXT | 模块类型 |
| `source_id` | TEXT | 来源业务表 ID，例如画像结果 ID 或打卡 ID |
| `data_json` | TEXT | 模块摘要数据 JSON |
| `created_at` | TEXT | 创建时间 |
| `updated_at` | TEXT | 更新时间 |
| `export_allowed` | INTEGER | 是否允许导出，1/0 |

`module_type` 建议值：

- `student_profile`
- `parent_log`
- `sandplay_task`
- `checkin`
- `weekly_report`
- `supervision`
- `training_plan_assignment`（用户训练节奏设置，`export_allowed=0`，不进入研究导出）

推荐策略：

1. 业务页面继续读写专用表；
2. 研究导出优先从 `records` 查询；
3. 新增模块时只需增加 `module_type`，避免频繁改大宽表；
4. 自由文本原文不默认进入 `records.data_json`，只保存脱敏摘要或关键词。

导出说明：

- `GET /api/admin/export?type=records` 可导出统一研究摘要；
- 可用 `module_type=student_profile` 筛选学生画像摘要；
- 导出默认使用 `anonymous_id`，不导出自由文本原文；
- 如果摘要包含 `risk_level=high` 或 `requires_review=true`，需要 `confirm_high_risk=true`。

### 11.4 `audit_logs`

用于记录后台导出、风险复核、规则变更等敏感操作。

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | TEXT PRIMARY KEY | 日志 ID |
| `actor_id` | TEXT | 操作者 ID，第一版可为管理员令牌标识 |
| `action` | TEXT | 操作类型，例如 `export_profile`、`review_risk` |
| `target_type` | TEXT | 操作对象类型 |
| `target_id` | TEXT | 操作对象 ID |
| `metadata_json` | TEXT | 筛选条件、导出行数、脱敏状态等 |
| `created_at` | TEXT | 创建时间 |

### 11.5 `profile_reviews`

用于保存学生画像的人工复核备注、复核结论和处置状态。该表不覆盖 `student_profiles` 的原始画像结果。

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | TEXT PRIMARY KEY | 复核记录 ID |
| `profile_id` | TEXT | 关联 `student_profiles.id` |
| `reviewer_id` | TEXT | 复核人标识 |
| `review_status` | TEXT | `pending`、`in_progress`、`reviewed`、`escalated`、`closed` |
| `review_decision` | TEXT | 复核结论 |
| `note` | TEXT | 后台人工备注，不自动同步学生端 |
| `action_summary` | TEXT | 处置摘要 |
| `visible_to_student` | INTEGER | 是否允许学生端可见，当前默认 0 |
| `created_at` | TEXT | 创建时间 |
| `updated_at` | TEXT | 更新时间 |

边界：

- 人工备注不修改原始画像结果；
- 人工复核保存会写入 `audit_logs.action=review_profile`；
- 如后续要向学生端展示人工回复，应先增加审核和展示开关，不直接公开后台备注。

### 11.6 数据边界补充

- 学生画像前端不使用“人格”字样，使用“阶段性画像”或“压力反应画像”。
- `confidence` 只能表示规则/模型匹配置信度，不代表临床判断确定性。
- `free_text`、高风险文本、联系方式均属于敏感字段，导出默认屏蔽。
- `export_allowed=false` 的数据不得进入研究导出。
- 沙盘表达只作为表达媒介和访谈线索，不自动解释潜意识。

### 11.7 学生画像导出数据字典

`GET /api/admin/export?type=profile` 当前从 `student_profiles` 读取，并只导出脱敏摘要。

| CSV 字段 | 来源字段 | 说明 | 是否敏感 |
|---|---|---|---|
| `anonymous_id` | `student_profiles.anonymous_id` | 匿名 ID，用于跨轮次串联 | 低 |
| `assessment_result_id` | `student_profiles.assessment_result_id` | 关联测一测记录 ID | 中 |
| `profile_code` | `student_profiles.profile_code` | 画像编码 | 低 |
| `profile_name` | `student_profiles.profile_name` | 展示画像名称，非诊断标签 | 低 |
| `confidence` | `student_profiles.confidence` | 规则匹配置信度，0-1 | 低 |
| `risk_level` | `student_profiles.risk_level` | 风险初筛等级 | 中 |
| `requires_review` | `student_profiles.requires_review` | 是否需要人工复核 | 中 |
| `dimension_summary` | `student_profiles.dimensions_json` 摘要 | 维度摘要，不含原始自由文本 | 中 |
| `recommended_card_ids` | `student_profiles.recommended_task_ids_json` | 推荐训练卡 ID | 低 |
| `rules_version` | `student_profiles.rules_version` | 规则版本 | 低 |
| `export_allowed` | `student_profiles.export_allowed` | 是否允许导出 | 低 |
| `data_quality` | `student_profiles.data_quality` | 数据质量标记 | 低 |
| `created_at` | `student_profiles.created_at` | 创建时间 | 中 |

默认不导出：

1. `student_profiles.user_id`；
2. 自由文本原文；
3. 联系方式；
4. 微信 openid 或手机号等强身份字段；
5. 高风险文本原文。

### 11.8 `records.data_json` 学生画像摘要字典

学生画像写入 `records` 时，`module_type=student_profile`。

| JSON 字段 | 说明 | 是否包含原文 |
|---|---|---|
| `anonymous_id` | 匿名 ID | 否 |
| `assessment_result_id` | 关联测一测记录 ID | 否 |
| `profile_code` | 画像编码 | 否 |
| `profile_name` | 画像名称 | 否 |
| `confidence` | 规则匹配置信度 | 否 |
| `risk_level` | 风险初筛等级 | 否 |
| `requires_review` | 是否需要人工复核 | 否 |
| `recommended_card_ids` | 推荐训练卡 ID 列表 | 否 |
| `rules_version` | 画像规则版本 | 否 |

设计边界：

- `records` 用于研究摘要，不是完整业务详情表；
- 不在 `records.data_json` 保存自由文本原文；
- 后续接入 `checkin`、`sandplay_task`、`parent_log` 时，也应优先保存摘要字段。

### 11.9 `audit_logs` 操作字典

当前已记录的操作：

| `action` | 触发场景 | `target_type` | 说明 |
|---|---|---|---|
| `view_profile` | 查看 `GET /api/profile-results/<profile_id>` | `student_profile` | 记录画像详情查看 |
| `review_profile` | 保存人工复核记录 | `student_profile` | 记录复核状态、结论和展示边界 |
| `export_profile` | 导出 `type=profile` | `export` | 记录画像导出 |
| `export_records` | 导出 `type=records` | `export` | 记录统一研究摘要导出 |
| `export_diaries` 等 | 导出其他类型 | `export` | 记录后台导出 |

审计边界：

- 第一版不记录管理员令牌原文，只记录 `admin-token` 这类标识；
- `metadata_json` 可记录导出类型、筛选条件、导出行数、`contains_high_risk` 和 `confirmed_high_risk_export`；
- 后端已提供 `backend.database.write_audit_log()` 统一写入工具，后续敏感操作应优先复用该函数，不再手写插入 SQL；
- 后续新增规则编辑后台时，必须增加 `update_rule` 或类似操作日志；
- 人工复核处置已记录 `review_profile`，但不代表完成危机干预流程。

## 2026-07-01：任务六/任务七字段补充

### `users` 补充字段

| 字段 | 类型 | 说明 |
|---|---|---|
| `wechat_openid` | TEXT | 微信登录 openid；生产环境不得导出 |
| `avatar_url` | TEXT | 用户头像地址，可为空 |

索引：

```text
idx_users_wechat_openid
```

## 0B. 任务二十七内容治理表

数据库版本：`2026_07_20_015 / content_governance_lifecycle`。迁移通过`init_db()`或`python backend/scripts/migrate_task27_content_governance.py --apply`幂等执行。

### `content_governance_versions`

保存不可覆盖的内容草稿和生命周期状态。`content_type + item_id + version`唯一；`payload_hash`为规范JSON的SHA-256；`metadata_json`保存来源、来源版本、版权、适龄、人群和变更摘要。状态为`registered/draft/pending_review/rejected/approved/published/paused/retired`。

### `content_governance_reviews`

保存研究、心理、伦理和内容审核。字段包括`version_id`、`discipline`、`decision`、`reviewer_id`、`reviewer_role`、`evidence_path`、`note`和`created_at`。审核记录不覆盖内容正文。

### `content_governance_releases`

保存不可变发布包、内容哈希、前一发布ID、发布原因、发布人和状态。恢复时先校验包哈希与版本哈希，再原子替换运行内容；失败会恢复原文件。

回滚原则：先关闭`CONTENT_GOVERNANCE_PUBLISH_ENABLED`，再按`previous_release_id`恢复已校验包；不自动删除治理表或审核轨迹。人工、伦理、真机和生产门禁只能附证据，不能由迁移脚本自动签字。

边界：

- `wechat_openid` 属于强身份字段，研究导出默认不得包含；
- 开发环境允许 `POST /api/auth/wechat-login` 使用兜底 openid，生产环境必须使用微信接口换取。

### `messages`

用于保存系统消息和人工督导补充反馈。

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | TEXT PRIMARY KEY | 消息 ID |
| `user_id` | TEXT | 关联用户 |
| `message_type` | TEXT | `system`、`supervision_feedback`、`researcher_message`、`relationship_report`、`relationship_stage_feedback` 等 |
| `title` | TEXT | 消息标题 |
| `body` | TEXT | 消息正文 |
| `source_type` | TEXT | 来源类型，如 `supervision_request` |
| `source_id` | TEXT | 来源记录 ID |
| `sender_id` | TEXT | 发送者用户 ID；系统消息可为空 |
| `sender_role` | TEXT | `researcher`、`supervisor`、`admin` 等发送角色 |
| `idempotency_key` | TEXT | 同一发送者范围内的防重复发送标识 |
| `status` | TEXT | `unread` 或 `read` |
| `created_at` | TEXT | 创建时间 |
| `read_at` | TEXT | 阅读时间 |

索引：

```text
idx_messages_user_status_created
idx_messages_sender_idempotency（`sender_id,idempotency_key` 唯一）
```

边界：

- 消息是补充反馈和提醒，不承担实时危机干预；
- 消息正文不得写入真实 token、联系方式、未脱敏研究敏感原文。
- 研究者消息的收件人由关系试点报名记录反查，不能由前端直接指定任意用户；阶段性反馈和普通提醒使用不同 `message_type`。

### `assessment_results` 画像落点字段

| 字段 | 类型 | 说明 |
|---|---|---|
| `profile_model_id` | TEXT | 使用的画像模型 ID |
| `profile_cluster_id` | INTEGER | 聚类簇 ID；允许为 0 |
| `profile_pc1` | REAL / DOUBLE | PCA 第一维位置 |
| `profile_pc2` | REAL / DOUBLE | PCA 第二维位置 |
| `profile_confidence` | REAL / DOUBLE | 当前落点置信度 |

索引：

```text
idx_assessment_results_user_created
idx_assessment_results_profile_model
```

规则：

- `profile_cluster_id=0` 是有效簇编号，不能当作空值；
- SQLite 使用 `REAL`，MySQL 建表时转换为 `DOUBLE`；
- `GET /api/assessment-results/<id>/profile-position` 会回填上述字段；
- 低置信度或离群结果不应生成强解释。

### `assessment_worksheets` 后台管理规则

后台新增：

```text
GET /api/admin/worksheets
POST /api/admin/worksheets
PUT /api/admin/worksheets/<worksheet_id>
DELETE /api/admin/worksheets/<worksheet_id>
```

主要用途：

- 管理小程序实际可见测评入口；
- 设置 `enabled_for_user`；
- 设置 `review_status`；
- 绑定 `profile_model_id`；
- 保留 `boundary_notice` 和 `result_disclaimer`。

边界：

- 后台隐藏入口只设置 `enabled_for_user=0`，不删除题项、内容库或历史测评结果；
- 题项文字、选项、题序、计分和反向题仍以人工验收为准。

## 2026-07-05：任务十字段补充

### `users` 手机号绑定状态字段

| 字段 | 类型 | 说明 |
|---|---|---|
| `phone_hash` | TEXT | 已验证手机号的不可逆 HMAC-SHA256 摘要，用于手机号快捷登录；不保存完整手机号 |
| `phone_verified_at` | TEXT | 手机号授权绑定成功时间 |
| `phone_source` | TEXT | 手机号来源，例如 `wechat_phone` |

边界：

- 系统不保存微信返回的完整手机号，`phone_or_email` 也不会因手机号快捷登录被写入；
- `phone_hash` 使用后端 `SECRET_KEY` 计算，生产环境必须保持密钥稳定，轮换前需设计账号迁移；
- 研究导出、日志和前端用户对象不得包含 `phone_hash`；
- 索引 `idx_users_phone_hash` 只用于登录查找，不改变公开注册的联系方式字段语义。

### `checkins` 训练效果字段

| 字段 | 类型 | 说明 |
|---|---|---|
| `helpfulness_rating` | TEXT | 用户主观有帮助程度，可存数字或标签 |
| `skip_reason` | TEXT | 暂停、跳过或未完成原因 |
| `source_recommendation_id` | TEXT | 来源推荐项 ID |
| `before_thermometer_id` | TEXT | 练习前情绪温度计记录 ID |
| `after_thermometer_id` | TEXT | 练习后情绪温度计记录 ID |

用途：

- 仅用于训练过程复盘和聚合分析；
- 不代表训练效果承诺；
- 不用于诊断、筛查或人格判断。

### `records` 项目练习记录

项目练习提交保存到 `records`：

| 字段 | 写入方式 | 说明 |
|---|---|---|
| `module_type` | `program_entry` | 表示项目练习过程记录 |
| `source_id` | `program_id` | 来源项目 ID |
| `data_json.program_id` | string | 项目 ID |
| `data_json.program_title` | string | 项目标题 |
| `data_json.session_no` | number/string | 项目 session 序号 |
| `data_json.answers` | object | 用户填写的草稿字段 |
| `data_json.reflection` | string | 练习后反思 |
| `data_json.analysis_consent` | boolean | 是否允许进入脱敏聚合分析 |
| `data_json.protocol_version` | string | 提交时绑定的项目方案版本 |
| `data_json.program_review_status` | string | 提交时项目审核状态 |
| `data_json.participation_status` | string | 完成、跳过、暂停或退出 |
| `data_json.recommendation_source` | string | 项目默认、用户选择或研究者调整 |
| `data_json.distress_before/after` | number/null | 练习前后0至10分不适 |
| `data_json.adverse_response` | boolean | 是否报告明显不适或负面体验 |
| `data_json.risk` | object | 透明关键词分流结果，不是临床风险结论 |
| `export_allowed` | 0/1 | 跟随 `analysis_consent` |

边界：

- 项目练习文本属于用户过程记录，后续导出默认应脱敏；
- 若用户未授权分析，`export_allowed=0`；
- 不把该记录解释成诊断、筛查或治疗结果。

### `records` 课程进度记录

课程进度复用 `records`，不新建第二套学习表：

| 字段 | 写入方式 | 说明 |
|---|---|---|
| `module_type` | `course_progress` | 课程进度记录 |
| `source_id` | `course_id` | 课程 ID |
| `data_json.course_version` | string | 保存时内容版本 |
| `data_json.status` | string | `in_progress/completed/skipped` |
| `data_json.completed_section_count` | number | 完成章节数 |
| `data_json.knowledge_check_completed_ids` | string[] | 已尝试的理解检查，不保存能力标签 |
| `data_json.transfer_task_status` | string | 迁移任务状态 |
| `data_json.linked_card_id` | string/null | 关联训练卡 |
| `export_allowed` | `0` | 默认不进入研究导出 |

页面浏览不会自动创建完成记录；同一课程可保留多个版本化历史记录，API默认返回最新一条。

## 2026-07-10：任务十二关系探索试点表

### `relationship_pilot_enrollments`

第二阶段报名快照。保存 `assessment_result_id`、`worksheet_id`、`profile_model_id`、`profile_cluster_id`、`dimensions_json`、`radar_features_json`、`profile_json`、`consent_scope`、`status` 和 `review_status`。`assigned_researcher_id` 记录研究者首次写操作形成的负责人归属；督导和管理员不受该归属限制。该字段不向普通参与者返回。不保存训练样本逐行数据。

### `relationship_screening_reports`

关系健康初筛报告。`report_json` 包含维度、雷达、阶段性画像、评估问题、项目任务、四层画像和边界说明；初始报告按 `pending_review`/`ready`/`confirmed`/`sent` 流转。研究者阶段性反馈以同一 `enrollment_id` 下的新 `id`、新 `version` 行保存，状态从 `updated` 到 `sent`，不覆盖旧报告。`confirmed_by`、`confirmed_at`记录人工确认。

### `relationship_pilot_tasks`

关系绘画和句子补全材料。`drawing_data_json`保存简易画布笔画，`narration`保存画外音，`answers_json`保存句子补全，`material_consent=1`才允许写入。`risk_level`和`review_status`用于人工复核。该表属于高敏感材料，默认不进入研究导出。

### `relationship_research_notes`

研究者私有备注。备注不覆盖用户报告，不直接展示给用户；禁止诊断化、人格化或病理化定性。

### `relationship_narratives`

治疗性反馈会议的探索手记。`draft_json`为系统汇总草稿，`status=draft`时用户不可见；研究者确认后写入 `confirmed_by`、`confirmed_at`并允许本人查看。

### `relationship_longitudinal_entries`

连续施测补充记录和关键事件。`entry_type`为 `weekly_supplement`或`key_event`；`measures_json`保存次数、应对、意愿和担忧强度；`narratives_json`保存开放叙事；`event_at`用于成长时间轴。开放文本执行风险预检，medium/high同步进入 `risk_review_records`。

当前数据库版本：`2026_07_10_001 / t12_relationship_pilot`。

任务十九消息交付字段版本为：`2026_07_15_006 / researcher_message_delivery_fields`，研究者归属版本为：`2026_07_15_007 / relationship_researcher_assignment`。任务二十当前数据库版本为：`2026_07_17_008 / supervision_source_link`，为`supervision_requests`补充可空`source_type/source_id/source_title`；旧`diary_id`继续兼容，不删除历史请求。关联记录提交时服务端按Bearer token校验所有权。

### `emotion_thermometer` 轻量多维字段

| 字段 | 类型 | 说明 |
|---|---|---|
| `valence_level` | INTEGER | 愉悦度或正负向感受观察 |
| `arousal_level` | INTEGER | 身体唤起程度 |
| `control_level` | INTEGER | 主观可控感 |
| `emotion_label` | TEXT | 用户自己命名的情绪词 |

用途：用于周报和阶段性反馈的过程复盘，不用于风险诊断。

## 2026-07-10：关系体验加固迁移

当前数据库版本：`2026_07_10_003 / relationship_experience_hardening`，覆盖并取代本文件前文的`2026_07_10_001`旧口径。

### `relationship_hypothesis_feedback`

保存用户对报告机制假设的共同核对：`report_id`、`enrollment_id`、`user_id`、`hypothesis_index`、`response`及时间戳。`response`只允许符合/不符合/不确定对应枚举；唯一约束为`(report_id,user_id,hypothesis_index)`，再次核对执行更新而不是新增重复行。

### 幂等和唯一约束

| 表/索引 | 约束 | 目的 |
|---|---|---|
| `relationship_pilot_enrollments` | `assessment_result_id`唯一 | 一次测评不重复报名 |
| `relationship_screening_reports` | `(enrollment_id,version)`唯一 | 同一报名下报告版本不重复 |
| `relationship_pilot_tasks` | 新增`idempotency_key`；`(user_id,idempotency_key)`唯一 | 弱网重试不重复保存敏感任务 |
| `relationship_longitudinal_entries` | 新增`idempotency_key`；`(user_id,idempotency_key)`唯一 | 周记录和关键事件不重复写入 |

`relationship_screening_reports.status`现使用`pending_review`、`ready`、`confirmed`、`sent`、`updated`。发送时间不新增冗余列，由与报告关联的`messages.created_at`计算为接口字段`sent_at`。非敏感产品事件不新增业务表，按严格枚举写入既有`audit_logs`，不保存用户原文。

## 2026-07-11：身份唯一性与 records 查询索引

当前数据库版本：`2026_07_11_005 / assessment_source_file_text_capacity`。该迁移会在量表内容同步前，将 MySQL 旧表 `assessment_worksheets.source_file` 从 `VARCHAR(255)` 扩容为可空 `TEXT`，避免多来源文件路径超过 255 字符导致容器启动失败；SQLite 字段保持 `TEXT`。

1. `users.username`、`users.wechat_openid`、`users.phone_hash` 增加数据库唯一索引；空值可由多个未绑定账号保留。
2. 初始化前检查三个字段的非空重复组。无重复时幂等建索引；有重复时不自动合并或删除账号，数据库可启动，但 `/readyz` 的 `database.ok=false`。
3. 健康信息只返回各字段重复组数量，不返回用户名、OpenID 或手机号摘要值。
4. `records` 增加 `idx_records_user_module_source_created(user_id,module_type,source_id,created_at)`，服务训练计划、项目记录和研究摘要按本人及模块读取。
5. SQLite 已自动测试；MySQL 生产迁移、历史重复账号处理和回滚仍需按 `待人工审查内容合集.md` 留证。

## 2026-07-18：订阅提醒表

当前数据库版本：`2026_07_17_009 / wechat_subscription_notifications`。

### `notification_preferences`

| 字段 | 说明 |
|---|---|
| `user_id` | 授权参与者；接口只允许本人写入 |
| `channel` | 当前固定为 `wechat_subscribe` |
| `notification_type` | 当前为练习到期提醒 `training_due` |
| `template_id` | 微信审核通过的模板 ID |
| `subscription_mode` | `once` 或受平台能力约束的 `long_term`；默认 `once` |
| `consent_status` | `unknown/accepted/rejected/banned/consumed` |
| `consented_at/last_prompted_at/revoked_at` | 授权、最近询问和拒绝/撤回时间 |

唯一约束：`(user_id, notification_type, template_id)`；当前渠道固定为微信订阅消息。

### `notification_deliveries`

| 字段 | 说明 |
|---|---|
| `idempotency_key` | 同一用户、同一练习日、同一模板的唯一发送键 |
| `status` | `sending/sent/failed` |
| `attempt_count` | 发送尝试次数，普通失败最多 3 次 |
| `retry_category` | `retryable/reauthorization_required/template_error/permanent_failure`，不保存提供方原文 |
| `next_attempt_at/max_attempts` | 指数退避下次时间和最大尝试次数 |
| `dead_lettered_at/last_attempt_at` | 进入死信和最近实际发送时间 |
| `scheduled_for/sent_at` | 计划日期和成功发送时间 |
| `provider_message_id` | 微信返回的消息标识；不向参与者端返回 |
| `error_code/error_message` | 受控失败摘要；不得写入密钥或用户原文 |

唯一约束：`idempotency_key`。OpenID 继续只保存在 `users.wechat_openid`，不会复制进投递表。

## 2026-07-18：匿名数据认领流水

当前数据库版本：`2026_07_18_010 / anonymous_data_claims`。

### `data_claims`

| 字段 | 说明 |
|---|---|
| `id` | 前端确认时使用的不可猜测认领流水 ID |
| `anonymous_id` | 登录设备匿名标识，仅后端内部匹配，唯一约束 |
| `target_user_id` | 候选绑定的正式参与者账号；不能由前端改写 |
| `status` | `available` 或 `claimed` |
| `counts_json` | 各参与者模块的认领数量，不保存原文 |
| `claimed_at` | 明确确认并完成事务的时间 |
| `created_at/updated_at` | 候选创建和刷新时间 |

认领迁移只更新参与者归属字段和派生匿名研究 ID；`audit_logs`、研究者备注和人工复核记录保持不可变。匿名用户行不删除，仅在没有任何登录凭证时标记为 `merged`。

## 2026-07-19：参与者反馈账本

历史版本：`2026_07_19_011 / participant_feedback_ledger`。

### `feedback_ledger`

| 字段 | 说明 |
|---|---|
| `id/user_id` | 评价记录与参与者本人；写入时按Bearer token校验所有权 |
| `source_type/source_id` | `instant_feedback/stage_report/training_recommendation/message`及对应来源ID |
| `content_version` | 被评价内容版本；新版本不覆盖历史 |
| `evaluation` | `matches/partly_matches/does_not_match/uncomfortable` |
| `reason_code/reason_text` | 可选结构化原因和本人补充；研究聚合接口不返回原文 |
| `review_status` | `recorded/pending_review/reviewed`，不适评价进入待人工复核 |
| `status` | `active/superseded`，同一来源当前只保留一个有效评价 |
| `idempotency_key` | 本人范围内唯一，防止弱网重复提交 |
| `created_at/updated_at` | 创建与最近更新时间 |

索引覆盖`(user_id,created_at)`、来源查询和`(user_id,idempotency_key)`唯一约束。该表是通用协作式纠错记录，不是治疗性评估专用表；旧`checkins.helpfulness_rating`和关系报告核对表继续兼容，不迁移、不删除。

## 2026-07-20：隐私生命周期执行与恢复证据

历史数据库版本：`2026_07_20_013 / privacy_lifecycle_execution`。SQLite由`ensure_schema_columns`幂等补列；MySQL由同一`SCHEMA_SQL`经适配器创建表和索引。迁移不自动执行任何参与者删除。

### `privacy_requests`新增字段

| 字段 | 说明 |
|---|---|
| `participant_notice` | 可向参与者展示的固定安全说明；不复制内部处理备注 |
| `policy_version` | 完成执行所采用的数据保存策略版本 |
| `execution_proof_hash` | 范围、计数、策略和完成时间的SHA-256证明 |

### 新增证据表

| 表 | 关键字段与约束 |
|---|---|
| `privacy_request_approvals` | 申请、人员、角色、范围哈希、策略版本；同一人员/范围/版本唯一 |
| `privacy_request_executions` | `dry_run/execute`、环境、预览、结果、证明、状态和幂等键；`(actor_id,idempotency_key)`唯一 |
| `privacy_deletion_tombstones` | 主体HMAC、替换标识、策略版本、范围和证明哈希；每个申请唯一，不保存原始参与者ID |

### 迁移、恢复与回滚

1. 上线前先备份并在副本运行`init_db`；当前健康检查目标版本见下方任务二十五迁移说明。该迁移只建表/补列，可重复执行。
2. 代码回滚可以停用三个执行开关并回退服务；数据库新增表/列保持兼容，不自动`DROP`，避免丢失审计与证明。
3. 业务执行在单事务中完成；异常时业务删除、执行流水、墓碑和完成状态全部回滚。
4. SQLite恢复后运行`python backend/scripts/verify_privacy_restore.py --db <恢复库> --secret <墓碑密钥>`；输出只含计数。发现已删除主体重新出现时，必须保持服务/研究导出关闭并走人工恢复流程。
5. 旧备份保留期、法律依据和正式生产双重批准仍待负责人/伦理/法律确认；工程实现不代替这些签字。

## 2026-07-20：研究运营工作项、租约与恢复

当前数据库版本：`2026_07_20_014 / research_operations_work_items`。

### `research_work_items`

| 字段 | 说明 |
|---|---|
| `queue_type/source_type/source_id` | 运营队列与原始来源引用；三字段唯一，不复制来源原文 |
| `user_id` | 对象权限和研究者分配范围校验 |
| `priority` | `routine/attention/urgent` |
| `status` | `open/claimed/processing/waiting/completed/closed/dead_letter` |
| `assignee_id/lease_expires_at` | 当前负责人和15分钟租约 |
| `due_at` | SLA观察到期时间；不作为心理质量考核 |
| `version` | 乐观锁版本；每次动作递增 |
| `resolution_code/closed_at` | 完成/关闭原因和关闭时间 |
| `last_action_at/created_at/updated_at` | 操作与审计时间 |

### `research_work_item_notes`与`research_work_item_actions`

- `research_work_item_notes`只保存授权人员的`internal/handling`说明；参与者可见消息不存这里，继续写入`messages`。
- `research_work_item_actions`保存前后状态、动作、角色、非原文元数据和幂等键；`(actor_id,idempotency_key)`唯一。
- 来源记录保持只读；来源在其原流程完成后，系统以`system/source_resolved`动作把工作项同步为已完成。

### 迁移、备份与回滚

1. 空库、旧`notification_deliveries`表和重复`init_db`均由自动测试覆盖；SQLite/MySQL共用同一schema定义。
2. 上线前备份并在副本执行`init_db`，确认`/healthz/deep`版本为`2026_07_20_014`。
3. 进程中断时工作项、动作、备注、消息和审计处于同一事务，异常整体回滚；SQLite备份恢复测试验证状态和版本一致。
4. 代码回滚先设`RESEARCH_OPERATIONS_WRITE_ENABLED=0`恢复只读；新增表列不自动删除，保留审计和幂等证据。
5. 测试云MySQL、真实双账号并发和生产备份恢复仍需人工执行，不由本地测试代签。

## 2026-07-20：受控AI合成研究沙盒

当前数据库版本：`2026_07_20_016 / controlled_ai_qa_sandbox`。该版本只承载研究者合成案例，不代表参与者AI功能已开放。

| 表 | 用途与关键约束 |
|---|---|
| `ai_qa_sessions` | 会话所有者、`research_sandbox`模式、合成标记、仅当前会话上下文、研究使用默认关闭和软删除状态 |
| `ai_qa_messages` | 合成输入或受控回答、批准来源、模型/安全元数据、提示/知识版本、token与成本；按会话和用户隔离 |
| `ai_qa_feedback` | `helpful/neutral/does_not_match/uncomfortable`纠错；同一用户/回答唯一，`research_use_allowed=0` |
| `ai_qa_safety_events` | 请求HMAC、安全类别、严重度、路由和是否调用供应商；不保存原始提示词 |
| `ai_qa_provider_events` | fake provider版本、成功/失败、时延、token、成本和错误代码；不保存请求/回答原文 |
| `ai_qa_evaluation_runs` | 固定合成集版本、知识快照哈希、阈值、指标和逐案例路由结果 |
| `ai_qa_evaluation_reviews` | supervisor/admin人工复核结论和证据路径；结论只推进内部评审，不改变参与者开关 |
| `ai_qa_runtime_control` | 全局只允许停用的kill switch；接口不能自行重新开启 |

会话、消息、反馈、安全事件和供应商事件已纳入隐私申请`messages_and_notifications`白名单及个人摘要；删除会话会立即移除消息/评价原文，完整隐私请求按既有预览、dry-run、批准和事务执行链处理。迁移脚本`migrate_task28_ai_qa.py`只幂等建表；回滚关闭两个AI开关并停用沙盒，八表不自动删除。测试云MySQL、真实备份恢复和数据保存/出境决定仍待人工。

## 2026-07-20：离线基准治理与盲标证据

当前数据库版本：`2026_07_20_017 / offline_benchmark_governance`。该版本只保存内部离线工程证据，不表示外部数据已获准下载，也不允许替换生产规则。

| 表 | 用途与关键约束 |
|---|---|
| `offline_dataset_cards` | 数据集名称、来源/版本、语言/平台/人群/情境、许可、内容权利、敏感性、允许/禁止用途、工件哈希、本地路径、接入状态和删除方式；许可不清时路径/哈希为空 |
| `offline_benchmark_runs` | 基准类型、数据集卡、证据等级、算法版本、参数、指标、工件哈希、创建人和状态；`raw_text_included=0`、`production_replacement_allowed=0` |
| `offline_benchmark_annotations` | 合成案例的标注者、盲标轮次、情绪、效价、唤醒、情境、反射弧节点和不确定标记；同一标注者/案例/轮次唯一 |
| `offline_benchmark_reviews` | supervisor/admin对运行的工程复核决定、证据路径和备注；不写发布批准 |
| `offline_benchmark_runtime_control` | 全局只允许停用的运行控制；没有自动重新开启动作 |

迁移脚本`migrate_task29_offline_benchmarks.py --apply`在空SQLite库幂等建表并登记017版本，统一schema继续覆盖MySQL字段与索引；`--rollback-plan`只输出关闭`OFFLINE_BENCHMARK_ENABLED`、保持外部接入/生产替换关闭、停用运行并保留证据表的非破坏步骤。若未来经人工批准接入外部工件，回滚还必须按数据集卡`deletion_method`删除源工件和派生运行；此步骤不得由当前迁移自动执行。

## 2026-07-20：研究方法冻结前证据与计分溯源

当前数据库版本：`2026_07_20_018 / research_methodology_freeze_evidence`。该版本表示本地工程证据可生成，不表示研究方案已正式冻结或伦理/生产已批准。

`assessment_results`新增字段：

| 字段 | 说明 |
|---|---|
| `scoring_version` | 工作表来源版本和服务端计分版本 |
| `raw_scale_json` | 工作表原始量尺范围，不含解释结论 |
| `raw_scores_json` | 原始量尺题项、维度和总分；九点量表保持1—9 |
| `transformed_scores_json` | 仅模型兼容需要的转换空间；九点问卷为1—5，其他量表默认空对象 |
| `transformation_version` | 当前九点转换为`linear_9_to_5_v1`，无转换为NULL |

研究方法证据表：

| 表 | 用途与不可越过边界 |
|---|---|
| `research_methodology_versions` | 不可变注册表JSON、版本和哈希；`formal_freeze_allowed=0`、`real_outcome_data_accessed=0` |
| `research_methodology_checks` | 机器结构检查、结果哈希和创建人；不保存真实结局 |
| `research_methodology_simulation_runs` | 合成参数/指标/哈希；`contains_real_data=0`、`confirmatory_power_claim=0` |
| `research_methodology_evidence_packages` | 待真人签字证据包；`formal_freeze_recorded=0` |
| `research_methodology_runtime_control` | 只允许停用的运行状态和安全原因摘要 |

迁移只为缺少溯源的历史测评回填字段，已有非空溯源不覆盖，未知工作表跳过并计数。回滚关闭工作台和分析开关、保留原分/证据/审计；删表或销毁证据需要另行人工批准。

## 2026-07-20：安全、隐私与滥用防护

当前数据库版本：`2026_07_20_019 / security_privacy_abuse_controls`。该版本表示本地工程控制可运行，不表示正式安全、隐私、伦理或生产批准。

`users`新增字段：

| 字段 | 说明 |
|---|---|
| `auth_epoch` | 令牌世代；登出、凭据轮换或账号停用时递增，使旧令牌失效 |
| `status_reason` | 账号停用/恢复的结构化原因；不保存参与者原文 |

新增证据表：

| 表 | 用途与边界 |
|---|---|
| `security_control_runs` | 保存脱敏扫描模式、状态、摘要、工件哈希和创建者；摘要不含密钥值、令牌或参与者文本 |
| `security_events` | 保存登录失败、账号状态和安全控制动作的类别、严重度、状态、目标类型/HMAC及白名单元数据 |
| `privacy_deletion_verifications` | 保存隐私申请的预期计数、删除计数、删除后核验、证明哈希和状态；对外响应去除主体哈希 |

`migrate_task31_security_controls.py --apply`幂等补列、建表和登记019版本，既有账号默认`auth_epoch=0`，旧令牌在主动轮换前保持兼容。`--rollback-plan`只关闭扫描、隐藏入口并保留令牌世代、CSV保护、安全响应头、删除证明和审计；破坏性删表需要人工批准。

## 2026-07-20：可靠性、可观测性与发布证据

当前数据库版本：`2026_07_20_020 / reliability_observability_release_engineering`。该版本表示本地可靠性机制可运行，不表示测试云SLO已冻结、值班责任已指定或允许上线。

| 表 | 用途与关键边界 |
|---|---|
| `observability_events` | 保存请求ID、方法、路径、角色范围、模块、旅程、结果、错误码、状态、时延、重试和恢复；不保存令牌、Cookie、请求/响应正文或参与者文本 |
| `reliable_jobs` | 保存四类可靠任务的业务来源引用、幂等键、租约、尝试、退避、死信和内容哈希；不复制业务payload |
| `reliable_job_actions` | 保存领取、完成、失败、重试和人工恢复的前后状态、原因与白名单元数据 |
| `feature_flag_versions` | 追加保存功能开关版本、角色范围、比例、原因、前版本和操作者；用于原子回滚 |
| `reliability_slo_snapshots` | 保存本地/测试云待补的观察窗口和脱敏指标；`production_slo_frozen=0` |
| `reliability_drill_runs` | 保存六类固定合成故障演练的场景、结果、哈希和创建者；不含真实参与者数据 |
| `reliability_evidence_packages` | 保存外部门禁待补的草稿证据包；`production_release_approved=0`且不推断人工决定 |

`migrate_task32_reliability_release.py`幂等建立020结构；SQLite可生成带完整性检查、七表行数和SHA-256的备份证明。MySQL共用schema定义，但测试云隔离备份/恢复仍须外部执行。回滚默认只关闭工作台、任务执行、故障注入和渐进发布，保留七表、审计和证据；破坏性删表或生产恢复需人工批准。

## 2026-07-22：账号一次性凭据生命周期

当前本地工程数据库版本：`2026_07_22_025 / researcher_capability_scope`。生产MySQL仍须由受控迁移门禁执行，本文不表示已迁移生产库。

### research_scope_assignments

研究者对象范围分配表。关键字段：`enrollment_id`、`actor_id`、`assignment_role`、`status`、`version`、`idempotency_key`、`assigned_by`、`revoked_at`及时间戳。`version`用于撤销/转交的乐观并发，幂等索引防重复提交。旧`relationship_pilot_enrollments.assigned_researcher_id`不删除，迁移期双读双写；`migrate_task36_research_access.py rollback`只撤销回填记录并保留旧字段，支持非破坏性回退。

### research_scope_assignment_actions

研究分配变更的持久化幂等回执表。关键字段：`assignment_id`、`actor_id`、`action`、`idempotency_key`、`request_hash`、`result_json`和`created_at`。同一操作者重复提交相同键和相同载荷时返回第一次结果；相同键对应不同载荷时返回409，避免弱网重试重复转交或撤销。回滚保留该表和审计历史，不恢复已作废的访问范围。

`users`新增字段：

| 字段 | 说明 |
|---|---|
| `must_change_password` | 是否必须先完成首次改密；默认0 |
| `credential_receipt_id` | 一次性凭据唯一回执ID；只用于幂等和防复用，不对普通接口返回 |
| `credential_expires_at` | 一次性凭据过期时间，必须含时区且不超过24小时 |
| `password_changed_at` | 最近一次成功改密时间 |
| `failed_login_count` | 连续密码失败次数；成功登录或解锁后清零 |
| `last_failed_login_at` | 最近一次密码失败时间 |
| `locked_until` | 临时锁定截止时间；当前5次失败后锁定15分钟 |

唯一索引`idx_users_credential_receipt_unique`阻止同一receipt跨账号复用；SQLite允许多个NULL，MySQL将该列映射为可索引的`VARCHAR(191)`。回滚采用停用新账号、递增`auth_epoch`和撤销receipt的非破坏方式，不恢复旧密码、不删除安全审计。

## 2026-07-20：体验、无障碍与表单恢复治理

当前数据库版本：`2026_07_20_021 / ux_accessibility_resilience_governance`。该版本表示本地体验工程和自动门禁可运行，不代表大字体、读屏、微信环境、Android/iOS、真人访谈或发布验收已批准。

| 表/字段 | 用途与关键边界 |
|---|---|
| `ux_audit_runs` | 保存八类自动门禁的环境、平台、视口、注册表版本、状态、脱敏结果与摘要；不保存参与者文本 |
| `ux_evidence_packages` | 保存待人工核验的体验证据包；真人研究、设备和发布批准固定为false，签名数组为空 |
| 六类表的`client_submission_id` | `goals/emotion_diaries/supervision_requests/checkins/assessment_results/parent_assessment_submissions`的客户端提交标识；与`user_id`组成唯一索引，支持安全重试 |

相同用户和提交标识的相同内容返回原记录；变更内容复用同一标识返回409。迁移幂等补列和建表；回滚关闭工作台/草稿挂钩并保留提交标识、唯一索引、审计和证据，不自动删除本机草稿或数据库列。

## 2026-07-21：内容、数据与模型运营治理

当前数据库版本：`2026_07_21_023 / participant_journey_feedback_recommendation`。该版本表示T23参与者主旅程、反馈修订/撤回与推荐回放本地工程可运行，不表示人工、伦理、云、真机或生产发布已批准。

### 2026-07-21 T23追加表与字段

- `feedback_ledger`追加`supersedes_id`、`participant_status`、`withdrawn_at`，保留原评价版本，不覆盖历史。
- MySQL 5.7部署时，`participant_status`、`priority`等带默认值的短状态字段，以及`request_id`、`case_id`、`job_id`、`verified_at`、`available_at`等参与键或索引的字段，由SQLite定义的`TEXT`转换为`VARCHAR(191)`。长度191兼容utf8mb4四列复合索引的3072字节限制；`*_json`长文本转换为`LONGTEXT`且移除默认值，避免错误1101、1170和后续键过长。SQLite字段定义保持不变。
- `feedback_ledger_actions`保存参与者撤回/纠错动作、前后状态、替代版本、幂等键和时间；不保存新的参与者原文副本。
- `recommendation_snapshots`保存参与者本人的测评来源、策略版本、候选卡ID、排序原因结构、幂等键和时间；用于解释、重放和旧策略回滚，不用于诊断或疗效判断。
- 两张新表均进入`feedback_and_training`隐私处理范围。迁移脚本为`backend/scripts/migrate_task23_participant_journey.py`；行为回滚关闭`training_feedback_adaptive_ranking`并恢复`legacy_rule_order_v1`，不自动删除追加表或审计历史。

| 表 | 用途与关键边界 |
|---|---|
| `operations_release_packages` | 保存唯一版本、能力范围、不可变清单/总哈希、状态、提出与发布人；生产批准默认0 |
| `operations_package_reviews` | 保存独立审核和研究/心理/安全领域批准证据，支持人员与领域分离 |
| `operations_replay_runs` | 保存固定合成回放、行为/文案差异、严重回归计数和快照哈希；不含真实数据 |
| `operations_runtime_controls` | 每项能力的活动/前一发布包、运行状态、版本和变更原因，用于原子切换与回滚 |
| `operations_monitor_snapshots` | 保存七类聚合运营指标与漂移信号；只触发复核，不评价参与者或家庭 |
| `operations_incidents` | 保存结构化严重事件、证据保全哈希和停用状态；不保存参与者原文 |
| `operations_incident_notifications` | 保存角色通知队列、幂等键、尝试、失败码和投递状态；无消息正文 |
| `operations_evidence_packages` | 保存人工/伦理/云/设备/生产待审草稿；批准均为0且签名为空 |

`migrate_task34_operations_governance.py`幂等建立022结构。回滚默认关闭T34工作台、本地发布和生产发布开关，暂停活动包并按哈希恢复旧包；保留发布包、审核、回放、事件、通知、证据和审计，不自动DROP表。

## 2026-07-22：任务36 F04移动工作台数据口径

F04没有新增表、字段或数据库迁移，当前schema仍为`2026_07_22_025 / researcher_capability_scope`。移动工作台只聚合既有`research_work_items`、`relationship_pilot_enrollments`、`messages`及相关来源表的数量和最小队列元数据；参与者检索继续读取`users`与各业务表计数，不复制开放文本。列表与搜索审计继续写入`audit_logs`。代码回滚不需要数据库回滚，也不得删除既有工作项、消息、报名、分配或审计记录。
## T36-F05 数据库说明（2026-07-22）

本任务无新增表、无新增列、无数据迁移。参与者档案按需读取复用`users`、`assessment_results`、`emotion_thermometer`、`emotion_diaries`、`checkins`、`relationship_screening_reports`、`relationship_pilot_tasks`、`records`、`messages`、`supervision_requests`、`relationship_pilot_enrollments`、`research_scope_assignments`和`audit_logs`。后端仅从白名单列读取，旧表缺少非关键列时跳过该列；缺失模块返回空分页。回滚不删除或改写业务数据。
# T36-F06 研究交付版本（schema 2026_07_23_026）

- `research_delivery_workflows`：保存报名、收件人、交付类型、四段状态、乐观锁版本、当前预览版本、消息/报告回执和撤回时间。
- `research_delivery_versions`：不可变内容版本；`workflow_id + version_no` 唯一，保存内容哈希、风险级别和创建者。
- `research_delivery_events`：追加式状态事件；按执行者和幂等键唯一，记录草稿、预览、确认、发送和撤回。
- `messages.delivery_id`、`messages.delivery_version`：把参与者消息与交付版本对应。
- `messages.withdrawn_at`：撤回时间。撤回只改变状态，不删除消息、报告或版本。

迁移脚本：`python backend/scripts/migrate_task36_research_delivery.py plan|apply|verify|rollback`。生产 `apply/rollback` 默认阻断；rollback采用加法迁移兼容策略，只回退应用版本，不DROP表列，不删除交付历史。

# T36-F12 身份与认领生命周期（schema 2026_07_24_027）

当前本地工程数据库版本为`2026_07_24_027 / identity_claim_lifecycle`，不表示CloudBase/MySQL生产库已迁移。

| 表/字段 | 用途与关键边界 |
|---|---|
| `users.merged_into_user_id`、`users.merged_at` | 记录参与者账号受控合并去向与执行时间；不改变目标账号原角色 |
| `data_claims.idempotency_key`、`data_claims.version` | 支持匿名记录认领的幂等重放和乐观并发；状态按available、processing、claimed推进 |
| `identity_merge_workflows` | 保存候选、确认、执行、核对、撤销状态、操作者、版本、幂等键和24小时撤销窗口 |
| `identity_merge_record_links` | 逐记录保存表、记录ID、归属字段及原值/目标值；用于精确核对和非破坏性撤销 |

`migrate_task36_identity_claims.py plan|apply|verify|rollback`默认阻断生产apply；加法迁移可重复执行。rollback仅声明旧应用兼容并保留结构、业务记录、认领历史和审计，不自动DROP表列。正式账号合并限定参与者角色；合并后记录或认领状态再次变化时拒绝自动撤销，转人工核对。

# T36-F13 研究者在线分析任务框架（schema 2026_07_25_028）

当前本地工程数据库版本为`2026_07_25_028 / research_analysis_job_framework`，不表示CloudBase/MySQL生产库已迁移。

| 表 | 用途与关键边界 |
|---|---|
| `research_analysis_snapshots` | 保存参与者、报名、研究用途、授权版本、来源数量、整体SHA256和有效期；不保存来源正文 |
| `research_analysis_snapshot_links` | 只保存来源类型、来源ID、版本和SHA256，用于可追溯快照 |
| `research_analysis_jobs` | 保存分析类型/版本/资源SHA256、最小聚合参数、幂等键、租约、退避、死信和七态状态机 |
| `research_analysis_artifacts` | 保存覆盖率、未知率、样本量、质量状态和聚合结果；固定`researcher_only`影子模式，不保存诊断标签 |
| `research_analysis_events` | 追加保存排队、领取、失败、恢复、取消、冻结和完成的前后状态及白名单元数据 |

授权撤回、版本变化、快照过期或模型停用时，任务与派生结果同步冻结。派生结果删除只清空`metrics_json`并留下删除原因、时间和审计，不删除业务来源。`migrate_task36_research_analysis.py plan|apply|verify|rollback`支持SQLite/MySQL共用加法schema，生产apply/rollback默认阻断；rollback先停执行器并回退应用，不自动DROP表或删除审计。
## 任务36 F14：在线分析呈现复用说明

F14 不新增表或字段，继续复用 F13 的 `research_analysis_snapshots`、`research_analysis_snapshot_links`、`research_analysis_jobs`、`research_analysis_artifacts` 和 `research_analysis_events`。合成数据通过 `research_analysis_snapshot_links.source_type=synthetic_fixture` 记录不可变引用和 SHA256，不把合成文本或参与者原文写入队列。

回滚不删除数据：关闭 `RESEARCH_ANALYSIS_JOB_EXECUTION_ENABLED` 后停止新领取，保留任务、工件、事件和审计；如需撤回派生结果，使用既有受控删除接口并保留 tombstone。
## T36-F17 数据库说明

本任务不新增表或字段，继续使用 `2026_07_25_028 / research_analysis_job_framework`。可靠性、安全和隐私闭环由机器注册表描述，并复用 `reliable_jobs`、`observability_events`、`security_events`、`privacy_requests`、`privacy_deletion_verifications`、`research_analysis_jobs` 和 `research_analysis_artifacts`。回滚仅移除展示接入或关闭功能开关，不删除业务表、审计或历史版本。
## T36-F15 数据库说明

本任务复用既有 `ai_qa_sessions/messages/feedback/safety_events/provider_events/evaluation_*` 表，无schema升级。7天是合成沙盒工程默认值；清理会删除过期合成会话的消息、评价及供应商/安全事件内容并把会话墓碑为deleted，审计日志保留且不含原始提示词。生产保留期限待隐私/法律人工批准。
## 2026-07-26：任务36 F16 治疗性评估协作（schema 029）

- 当前版本：`2026_07_26_029 / therapeutic_assessment_collaboration`。
- `therapeutic_assessment_cases`：参与者问题、共享范围、同意/撤回、风险、复杂范围、L0—L2人工门禁证据、对象分配和乐观版本。
- `therapeutic_assessment_feedback_versions`：结构化人工/AI草稿、版本号、人工复核与发送时间；AI来源不能绕过人工复核。
- `therapeutic_assessment_actions`：参与者选择、完成或拒绝的一小步及随访说明。
- `therapeutic_assessment_events`：幂等动作、前后版本和最小元数据；与`audit_logs`共同保留可追溯证据。
- 迁移为加法迁移；回滚默认关闭入口并回退应用，不DROP表、不删除历史版本。生产迁移、恢复和回滚仍需人工批准。

## 2026-07-26：任务36 F18 迁移、备份恢复与回滚验证

当前目标仍为`2026_07_26_029 / therapeutic_assessment_collaboration`，F18不新增业务表或字段。机器矩阵覆盖F03/F12/F13/F16相关结构，并在隔离SQLite库验证空库、旧库、重复执行、部分建表、事务中断和备份恢复；MySQL 5.7通过类型、默认值、占位符与索引宽度静态契约。备份证据只记录SHA-256、schema hash、表行数和版本。恢复后必须执行隐私墓碑复核；生产迁移、恢复和回滚需要人工批准，默认不DROP表、不删除历史与审计。

## 2026-07-27：任务37 P03 数据血缘与隐私生命周期（schema 030）

当前本地工程目标为`2026_07_27_030 / computation_lineage_privacy_lifecycle`，不表示CloudBase/MySQL生产库已经迁移。

| 表 | 用途与关键边界 |
|---|---|
| `computation_datasets` | 注册数据集用途、分层、版本、来源、授权状态和校验和；禁止写入参与者原始文本 |
| `computation_authorization_snapshots` | 保存HMAC化对象标识、用途、同意版本、来源版本、去标识状态和授权时间 |
| `computation_lineage_edges` | 以资源类型和ID记录父子派生关系，用于递归定位派生产物 |
| `computation_deletion_tombstones` | 追加记录撤回根对象、受影响资源、原因和处理时间；不物理删除墓碑 |
| `computation_legal_holds` | 保存法定保留范围、原因、有效期和解除时间；有效保留阻断自动删除但保留审计 |

迁移脚本为`python backend/scripts/migrate_task37_p03_lineage.py plan|apply|verify|rollback`。SQLite和MySQL均采用加法迁移；生产`apply`要求精确确认短语，`rollback`不DROP表、不删除历史。恢复核验器`verify_task37_p03_lineage_restore.py`只比较schema哈希、行数、版本和墓碑哈希，不输出业务原文。本轮未执行生产迁移、恢复或回滚。

## 2026-07-27：schema 031治疗性评估三轨状态

`therapeutic_assessment_cases`加法新增`workflow_state`、`hypothesis_state`、`safety_state`。它们分别表示服务流程、共同理解成熟度和安全事件；旧`status`字段继续保留兼容。创建case会进入`submitted / observations_only`，安全轨道按风险检查进入`low_risk`或`needs_human_review`。

迁移脚本为`backend/scripts/migrate_task38_f02_state_machine.py`。旧case按原状态非破坏映射；生产apply要求`--allow-production --confirmation APPLY_TASK38_F02_STATE_MACHINE`，rollback不自动删除字段、历史状态和审计事件。

## 2026-07-27：schema 032 O/P/H/U证据账本

新增`therapeutic_assessment_evidence_items`，保存证据种类、内容、来源、提供者、时间、情境、可见范围、适用范围、例外、时间窗、支持证据、反证、替代解释、推翻条件、保护功能、代价、参与者识别状态、作者、人工复核状态、版本和幂等键。迁移脚本为`backend/scripts/migrate_task38_f03_evidence_ledger.py`，只加表且回滚不自动删除证据或审计。

## 2026-07-27：schema 033问题质量字段

`therapeutic_assessment_cases`加法新增`working_question`、`question_candidates_json`、`question_quality_json`、`best_guess`、`question_status`、`candidate_decision`和`question_version`。原`assessment_question`保持不可覆盖。迁移脚本为`backend/scripts/migrate_task38_f04_question_quality.py`，回滚不删除原话、版本或审计。

## 2026-07-27：schema 034动态同意

新增`therapeutic_assessment_data_items`和`therapeutic_assessment_data_consents`。前者保存资料主体/提供者/涉及者/控制者、可见范围、查看者、用途、有效期、状态、受控引用和摘要；后者追加保存每次批准、修改、撤回及同意版本。迁移脚本为`backend/scripts/migrate_task38_f05_dynamic_consent.py`，回滚不自动删除授权历史或审计。

## 2026-07-27：schema 035参与者流程草稿

新增`therapeutic_assessment_participant_drafts`与`therapeutic_assessment_participant_draft_events`。草稿按case、参与者和step唯一，保存受控JSON、客户端更新时间、状态和版本；事件表持续保存幂等键和结果版本，防止重试造成重复写入。迁移脚本为`backend/scripts/migrate_task38_f06_participant_flow.py`，回滚只停止新功能，不自动删除草稿或审计。

## 2026-07-27：schema 036人工安全责任链

| 表 | 用途与关键边界 |
|---|---|
| `therapeutic_assessment_responsibility_chains` | 按case保存责任人、督导、支持通道、证据引用、状态、队列时限和乐观锁版本；case唯一 |
| `therapeutic_assessment_safety_events` | 追加保存“需要真人了解”事件、受控来源引用、处置状态和人工解除证据；操作者与幂等键唯一 |
| `therapeutic_assessment_runtime_control` | 保存全局普通流程kill switch、原因、变更者、时间和恢复证据；不保存参与者原文 |

迁移脚本为`backend/scripts/migrate_task38_f07_safety_chain.py`。生产apply要求单独允许和精确确认短语；rollback只声明旧应用兼容并保留表、事件、人工证据和审计，不自动DROP或删除。当前仅完成本地迁移验证，未操作CloudBase/MySQL生产库。

## 2026-07-27：schema 037研究者证据工作台

| 表/字段 | 用途与关键边界 |
|---|---|
| `therapeutic_assessment_researcher_workbench_drafts` | 按case与研究者保存内部记录、参与者可见草稿、筛选状态和乐观锁版本；两类文本字段不混用 |
| `therapeutic_assessment_researcher_workbench_draft_events` | 追加保存幂等键、结果版本和时间，用于重试恢复；不记录草稿原文 |
| `therapeutic_assessment_evidence_items.method_limitations` | 保存每条证据的方法限制，避免把观察或量表结果写成确定结论 |

迁移脚本为`backend/scripts/migrate_task38_f08_researcher_workbench.py`。SQLite与MySQL使用同一列检查工具；生产apply需要单独批准和精确确认短语。rollback保留草稿、事件、证据和审计，不自动DROP或删除。
## 2026-07-27 T38-F09分层反馈

- `therapeutic_assessment_feedback_versions`新增：`feedback_layer`、`recipient_user_id`、`letter_title`、`supersedes_feedback_id`、`withdrawn_at`、`withdrawal_reason`、`lifecycle_version`。
- `therapeutic_assessment_feedback_deliveries`：保存反馈版本每次发送的接收者、顺序、状态、发送人、时间、撤回信息和幂等键。
- `therapeutic_assessment_feedback_responses`：保存参与者每次核对选择、异议文字、所替代的上一条核对和幂等键。
- 当前schema：`2026_07_27_038 / therapeutic_assessment_layered_feedback`。
- 回滚原则：不删除反馈、发送或异议历史；需要回退时停用新入口并保留数据。

## 2026-07-27：schema 039小行动与随访

`therapeutic_assessment_actions`新增目的、计划日期、提醒方式与隐私、停止条件、未完成记录、训练卡/打卡引用、乐观锁版本和完成时间。行动随访继续写入O/U证据账本，完成次数不作为疗效。迁移脚本为`backend/scripts/migrate_task38_f10_action_followup.py`；回滚保留行动、随访和审计。

## 2026-07-28：schema 040胜任力与任务授权

| 表 | 用途与关键边界 |
|---|---|
| `therapeutic_assessment_authorizations` | 按人员、T1—T3级别、任务代码、对象/复杂度/准备度范围、督导、证据、起止时间和版本保存任务授权；默认拒绝 |
| `therapeutic_assessment_authorization_events` | 追加保存授予、撤销和重大事件后重新核验的前后版本、原因与幂等键，不保存参与者原文 |

T2只在`individual_adult_low_risk`且L1/L2范围生效；T3才可承担正式假设、复核和后续高复杂度任务。离岗、停用、到期、范围变化或重大安全事件会使授权失效或进入`review_required`。迁移脚本为`backend/scripts/migrate_task38_f11_competency_authorization.py`；生产apply要求独立批准和精确确认短语，rollback不DROP表、不删除授权历史。

## 2026-07-28：T37-B04生命周期与隐私删除范围

本任务不新增表，复用反馈版本、交付回执、参与者核对、小行动、事件和质量事件表。`workflow_state`由反馈与行动服务同步推进，反馈和行动各自继续使用`lifecycle_version`或`version`做乐观锁。

`therapeutic_assessment`隐私删除范围补齐以下关联层级：

- case直连：反馈、交付、核对、证据、资料、草稿、责任链、安全事件、工作台、小行动、质量复核/事件、人工队列和状态事件；
- 子事件：资料授权、草稿事件、工作台草稿事件、质量事件、队列事件；
- 发布链：治疗性反馈的发布候选、五道门检查和候选事件。

删除顺序先子表后主表，执行后逐表核验为零；备份清除仍按人工批准的数据保存策略处理。迁移脚本`backend/scripts/migrate_task37_b04_feedback_lifecycle.py`只做增量结构与隐私范围审计，不自动删除生产数据。

## 2026-07-28：schema 049协作式评估生产门禁

| 表 | 用途与关键边界 |
|---|---|
| `therapeutic_assessment_release_evidence` | 保存证据类型、引用、SHA-256、环境、待核验/通过/拒绝状态、记录人、核验人、版本和幂等键；记录人与核验人必须不同 |
| `therapeutic_assessment_release_gate_runs` | 保存每次门禁评估的策略版本、任务注册表哈希、五类结果和评估人；`production_release_approved`固定为0 |
| `therapeutic_assessment_release_gate_checks` | 保存一次运行内每一类门禁的通过/阻断、缺失项和已采用证据ID |

迁移脚本为`backend/scripts/migrate_task37_b05_production_gate.py`。开发环境可执行plan/apply/verify/rollback；生产apply需要明确开关和精确确认短语。rollback只关闭使用入口并保留证据和审计历史，不DROP表、不删除签署记录。
## 2026-07-28：T37-C01 AI用例冻结（schema 050）

- 当前本地工程版本：`2026_07_28_050 / ai_use_case_freeze`，不表示生产库已迁移。
- `ai_qa_sessions.use_case_id`：会话创建时冻结的允许用例；旧记录标记为`legacy_unscoped`并默认不能继续执行。
- `ai_qa_sessions.use_case_policy_version`：创建会话时使用的策略版本，支持审计和后续回放。

### T37-C02供应商合同证据

当前schema：`2026_07_28_051 / ai_provider_selection_evidence`。

新增`ai_provider_contract_evidence`，只保存供应商ID、证据类型、脱敏引用、SHA-256、状态、登记/复核人、幂等键、版本和时间，不保存合同正文或密钥。证据先写为`pending`，必须由不同人员通过乐观锁复核；即使单项证据通过，也不会自动选择供应商或打开出网。迁移仅追加表和索引，回滚保留证据并由应用版本关闭入口，不自动删表。
- 迁移只追加两列；生产应用需要`--allow-production`与精确确认短语。回滚只回退应用版本，不DROP列、不删除会话或审计记录。

### T37-C03真实Provider运行元数据

当前schema：`2026_07_28_052 / ai_provider_runtime_metadata`。

`ai_qa_provider_events`新增`provider_request_id`、`input_tokens`、`output_tokens`和`cost_currency`。原有`provider`、`model_version`、`status`、`latency_ms`、`token_estimate`、`cost_micros`及`error_code`继续保留。表内不增加请求文本、回答文本、Authorization或密钥字段。迁移为加法迁移；回滚关闭真实Provider并保留历史调用元数据，不自动DROP列或删除审计证据。

### T37-C04批准知识库与RAG

当前schema：`2026_07_28_053 / approved_knowledge_rag`。

| 表 | 用途与关键边界 |
|---|---|
| `ai_knowledge_documents` | 追踪内容治理版本、发布记录、来源、来源版本、权利、审核、有效期、适用人群、载荷哈希和撤回状态；`version_id`唯一 |
| `ai_knowledge_chunks` | 保存可重建切片、字段位置、标题、内容哈希、词项和本地确定性向量；每个文档的切片序号唯一 |
| `ai_knowledge_candidates` | 仅保存网页候选URL、标题、SHA-256和隔离状态；没有网页正文、HTML或原始文本字段 |
| `ai_knowledge_evaluation_runs` | 保存检索方法、评测指标、逐例结果、知识快照哈希和工程阈值状态；不保存原始参与者文本 |

迁移脚本为`backend/scripts/migrate_task37_c04_approved_knowledge.py`。迁移只追加表和索引；生产apply需要独立批准和精确确认短语。rollback不删除知识、候选或评测证据，只要求回退应用版本。

### T37-C05输入安全说明

本任务复用现有`ai_qa_messages`、`ai_qa_safety_events`和`audit_logs`，不新增表、不升级schema，当前仍为`2026_07_28_053 / approved_knowledge_rag`。`ai_qa_messages`只写入去标识后的合成输入；`ai_qa_safety_events`继续只保存请求HMAC、分类、结果和不含原文的最小输入安全摘要。回滚只需回退应用版本与提示模板，不涉及数据删除或结构回退。

### T37-C06输出契约说明

本任务不新增表、不升级schema，仍为`2026_07_28_053 / approved_knowledge_rag`。结构化候选经五道门通过后，公开回答写入`ai_qa_messages.content`，引用、模型和门禁摘要分别进入既有`citations_json`、`model_json`和`safety_json`。未通过候选不作为正式回答保存，只记录不含候选原文的违规代码和固定降级事件。回滚仅回退应用版本和输出schema，不删除历史消息或审计证据。

### T37-C07 AI候选人工审阅工作台

当前schema：`2026_07_28_054 / ai_review_workbench`。

| 表 | 用途与关键边界 |
|---|---|
| `ai_qa_review_cases` | 每条通过安全门的AI消息唯一对应一个内部审阅任务；保存候选哈希、批准来源与快照哈希、对象范围、所需任务/胜任力、乐观版本和人工决定。`formal_feedback_written=0`，发布者为空 |
| `ai_qa_review_actions` | 追加保存采用、修改、拒绝或无匹配决定、前后版本、候选/最终哈希、差异摘要、理由、请求哈希和操作者幂等键 |

迁移脚本为`backend/scripts/migrate_task37_c07_ai_review_workbench.py`，仅追加表和索引；生产apply要求独立批准和精确确认短语。逻辑回滚保留审阅证据并关闭新路由，不自动DROP表。删除合成会话时，审阅原文和动作随会话删除，关联AI发布候选清空正文并标记撤回，不含原文的公共审计记录保留。

### T37-C08持续质量说明

本任务不新增表、不升级schema，继续使用`2026_07_28_054 / ai_review_workbench`。评测指标、阈值、逐例合成结果、变更指纹和发布阻断状态写入既有`ai_qa_evaluation_runs`；人工修改率只聚合`ai_qa_review_actions.decision`，不复制候选正文。回滚只需回退应用、合成评测集和CI步骤，不涉及数据库结构回退。
