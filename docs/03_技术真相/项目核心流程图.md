# 项目核心流程图

> 项目：`safehome1.0 / 安心陪伴 / ReadFeedback / 安心家平台`  
> 文档用途：集中整理当前项目核心功能链路的 Mermaid 流程图，便于写文档、做 PPT、给导师讲解、给 Codex 或程序员交接。  
> 使用说明：本文档中的 Mermaid 图可以直接复制到支持 Mermaid 的 Markdown 编辑器、GitHub、Typora、Obsidian 或 PPT 转图工具中使用。  

---

## 1. 系统总体架构图

```mermaid
flowchart TD
    A[用户端 Web / 微信小程序] --> B[shared/constants/api.ts 接口路径]
    B --> C[Flask 后端 backend/app.py]
    C --> D[routes 业务路由层]
    D --> E[services 业务服务层]
    E --> F[(SQLite / MySQL 数据库)]
    E --> G[content 内容库]
    F --> H[Web 研究后台]
    F --> I[督导工作台]
    F --> J[研究导出]
    G --> D
```

说明：

```text
用户通过 Web 或小程序进入系统；前端根据 shared/constants/api.ts 或小程序 api.js 调用后端；后端由 app.py 统一注册蓝图；routes 调用 services；services 读取数据库和 content 内容库；最终结果流向用户端、研究后台、督导后台和导出模块。
```

---

## 2. 后端应用启动与健康检查流程

```mermaid
flowchart TD
    A[启动 Flask app] --> B[Config.validate 校验配置]
    B --> C[create_app 创建应用]
    C --> D[init_db 初始化数据库]
    D --> E[执行 SCHEMA_SQL 建表]
    E --> F[ensure_schema_columns 兼容旧库]
    F --> G[创建索引 INDEX_SQL]
    G --> H[sync_training_cards 同步训练卡]
    H --> I[record_schema_migration 记录 schema 版本]
    I --> J[注册 auth / goals / diaries / feedback / profile 等蓝图]
    J --> K[提供 /healthz]
    J --> L[提供 /healthz/deep]
    L --> M[检查数据库]
    L --> N[检查 content 必需文件]
```

说明：

```text
/healthz 用于轻量探活；/healthz/deep 用于检查数据库、schema、训练卡同步和 content 文件是否完整。
```

---

## 3. 目录与模块关系图

```mermaid
flowchart LR
    A[backend] --> A1[app.py]
    A --> A2[routes]
    A --> A3[services]
    A --> A4[database.py]
    A --> A5[models.py]

    B[apps/web] --> B1[main.tsx]
    B --> B2[pages]
    B --> B3[services]

    C[apps/miniprogram] --> C1[app.json]
    C --> C2[pages]
    C --> C3[services/api.js]

    D[shared] --> D1[constants/api.ts]
    E[content] --> E1[training_cards.json]
    E --> E2[feedback_rules.json]
    E --> E3[risk_keywords.json]
    E --> E4[student_profile_model.json]

    D1 --> B3
    D1 --> B1
    C3 --> A1
    B3 --> A1
    E --> A3
    A4 --> F[(Database)]
```

---

## 4. 用户角色与入口图

```mermaid
flowchart TD
    A[用户进入系统] --> B{用户角色}
    B -->|家长 parent| C[家长端]
    B -->|学生 student| D[学生端]
    B -->|研究者 researcher| E[研究后台]
    B -->|督导 supervisor| F[督导工作台]
    B -->|管理员 admin| G[系统管理/高级导出]
    B -->|匿名试用 anonymous| H[匿名试用流程]

    C --> C1[目标设定]
    C --> C2[情绪记录]
    C --> C3[反馈与训练卡]
    C --> C4[家长测评]
    C --> C5[家庭绑定]

    D --> D1[学生测评]
    D --> D2[阶段性画像]
    D --> D3[推荐任务]
    D --> D4[家庭绑定确认]

    E --> E1[脱敏数据查看]
    E --> E2[画像统计]
    E --> E3[研究导出]

    F --> F1[风险复核]
    F --> F2[人工支持请求]
    F --> F3[处置记录]

    G --> G1[高级导出]
    G --> G2[联调测试]
    G --> G3[系统配置]
```

---

## 5. 家长主流程：目标—记录—反馈—训练—周报

```mermaid
flowchart TD
    A[家长设定目标] --> B[填写情绪事件记录]
    B --> C[风险预检 check_text_risk]
    C -->|低/中风险| D[生成支持性反馈 generate_feedback]
    C -->|高风险| E[中止普通反馈]
    E --> F[写入 risk_review_records]
    D --> G[推荐训练卡 recommended_card_ids]
    G --> H[查看训练卡]
    H --> I[练习并打卡]
    I --> J[checkins 保存练习记录]
    J --> K[weekly_reports 周报复盘]
    K --> L[研究后台查看趋势]
```

涉及接口：

```text
POST /api/goals
POST /api/diaries
POST /api/feedback/generate
GET  /api/cards
GET  /api/cards/recommend
POST /api/checkins
GET  /api/weekly-report
```

涉及数据表：

```text
goals
emotion_diaries
feedback_results
training_cards
checkins
weekly_reports
records
risk_review_records
audit_logs
```

---

## 6. 支持性反馈生成流程

```mermaid
flowchart TD
    A[前端提交 diary_id 或 payload] --> B[/api/feedback/generate]
    B --> C{是否传入 diary_id}
    C -->|是| D[读取 emotion_diaries]
    C -->|否| E[直接使用 payload]
    D --> F[require_admin_or_owner 权限检查]
    E --> G[提取风险文本字段]
    F --> G
    G --> H[check_text_risk]
    H --> I{allow_auto_feedback?}
    I -->|true| J[generate_feedback 普通支持性反馈]
    I -->|false| K[高风险安全提示]
    J --> L[保存 feedback_results]
    K --> L
    L --> M[create_risk_review_record]
    M --> N[返回反馈结果]
```

关键边界：

```text
高风险时：
1. 不生成普通互动模式反馈。
2. 不推荐普通训练卡。
3. recommended_card_ids 返回空数组。
4. 写入 risk_review_records。
```

---

## 7. 训练卡推荐与打卡流程

```mermaid
flowchart TD
    A[反馈结果] --> B[recommended_card_ids]
    B --> C[前端请求训练卡]
    C --> D[content/training_cards.json 或 training_cards 表]
    D --> E[用户查看训练卡步骤]
    E --> F[完成练习]
    F --> G[POST /api/checkins]
    G --> H[checkins 表]
    H --> I[周报统计 completed_cards]
    H --> J[研究导出 checkins]
```

说明：

```text
训练卡是低压力练习，不是课程考试，也不是治疗方案。打卡更强调“尝试”和“复盘”，不应变成考核机制。
```

---

## 8. 学生画像生成流程

```mermaid
flowchart TD
    A[学生进入测评] --> B[填写学生支持性画像测评]
    B --> C[POST /api/profile]
    C --> D[generate_student_profile]
    D --> E[生成 profile_code / profile_name / confidence]
    E --> F[写入 assessment_results]
    E --> G[写入 student_profiles]
    G --> H[写入 records: module_type=student_profile]
    G --> I[check_text_risk 检查自由文本]
    I --> J[create_risk_review_record]
    G --> K[学生端报告]
    G --> L[后台 /profiles]
    G --> M[后台 /reviews]
    G --> N[研究导出 profile / records]
```

关键字段：

```text
profile_code
profile_name
confidence
risk_level
requires_review
model_version
model_type
rules_version
cluster_id
pc1
pc2
report_json
visuals_json
export_allowed
data_quality
```

边界：

```text
学生画像只能解释为阶段性压力反应线索，不能解释为人格类型或临床诊断。
```

---

## 9. 学生画像后续追踪流程

```mermaid
flowchart TD
    A[已有 student_profile] --> B{后续任务类型}
    B -->|追踪反馈| C[POST /profile-results/:id/followups]
    B -->|沙盘表达| D[POST /profile-results/:id/sandplay]
    C --> E[student_profile_followups]
    D --> F[student_sandplay_entries]
    C --> G[check_text_risk]
    D --> H[check_text_risk]
    G --> I[risk_review_records]
    H --> I
    E --> J[records: profile_followup]
    F --> K[records: sandplay_task]
    E --> L[画像趋势 visuals]
    F --> L
```

说明：

```text
沙盘表达只作为表达线索和访谈线索，不自动解释潜意识，不做诊断推断。
```

---

## 10. 家长测评流程

```mermaid
flowchart TD
    A[家长进入测评页面] --> B[填写问卷]
    B --> C[POST /api/parent-assessment]
    C --> D[计算 scores_json]
    D --> E[生成 report_json]
    E --> F[parent_assessment_submissions]
    F --> G[家长报告页]
    G --> H[parent_report_actions 记录报告动作]
    F --> I[研究导出 parent_assessments]
    F --> J[raw_wide / long / codebook]
```

研究字段：

```text
participant_code
research_consent
study_batch
source_channel
questionnaire_version
scoring_version
duration_seconds
quality_flags_json
export_allowed
```

---

## 11. 家长—学生绑定流程

```mermaid
flowchart TD
    A[家长登录 parent] --> B[POST /api/family/create-bind-code]
    B --> C[生成 10 位安全随机绑定码]
    C --> D[family_links 仅存摘要与尾号 status=pending]
    D --> E[学生登录 student]
    E --> F[输入绑定码]
    F --> G[POST /api/family/bind-student]
    G --> H{绑定码是否有效}
    H -->|有效且原子条件更新成功| I[family_links status=consumed]
    H -->|过期/错误/重放| J[统一返回不可用]
    H -->|账号/设备/IP/单码超限| Q[暂时限流或 locked]
    I --> K[写入 parent_view_student_summary 授权]
    I --> L[write_audit_log]
    I --> M[GET /api/family/members 查询绑定]
    M --> N[家长只能看授权摘要]
    M --> O[学生可查看绑定状态]
    I --> P[DELETE /api/family/unbind 可撤销]
```

绑定规则：

```text
1. 家长不能单方面完成绑定。
2. 学生必须输入绑定码确认。
3. 绑定码为 10 位安全随机数字，24 小时有效；数据库与日志不保存完整码。
4. 单码超过 5 次以及账号、设备、IP 超限时暂时阻断；生产 Redis 不可用时拒绝兑换。
5. 同一码只允许一个并发请求从 `pending` 原子进入 `consumed`。
6. 绑定不等于研究授权或监护人敏感数据处理同意。
7. 绑定不等于家长可查看学生原始文本。
```

---

## 12. 隐私中心与授权撤回流程

```mermaid
flowchart TD
    A[用户进入隐私中心] --> B[GET /api/privacy/consent-status]
    B --> C[查看 user_agreement / privacy_policy / non_diagnostic_notice / research_authorization]
    A --> D[POST /api/privacy/revoke-consent]
    D --> E[写入 consent_records agreed=0]
    E --> F[write_audit_log]
    E --> G[research_revoked_filter]
    G --> H[研究导出排除该用户]
    A --> I[POST /api/privacy/delete-my-data]
    I --> J[privacy_requests status=pending]
    J --> K[write_audit_log]
    A --> L[GET /api/privacy/export-my-data]
    L --> M[返回个人数据摘要]
```

隐私边界：

```text
1. export-my-data 返回摘要，不默认返回自由文本原文。
2. delete-my-data 当前生成删除请求，不做无审计硬删除。
3. revoke-consent 后研究导出应排除该用户。
```

---

## 13. 高风险复核流程

```mermaid
flowchart TD
    A[自由文本输入] --> B[check_text_risk]
    B --> C{风险等级 / allow_auto_feedback}
    C -->|低/中风险且允许自动反馈| D[普通支持性反馈]
    C -->|高风险或不允许自动反馈| E[安全边界提示]
    E --> F[create_risk_review_record]
    F --> G[risk_review_records]
    G --> H[/reviews 人工复核]
    G --> I[/supervision 督导工作台]
    H --> J[更新 review_status]
    I --> J
    J --> K[reviewed / need_followup / closed]
    K --> L[audit_logs 留痕]
```

风险处理边界：

```text
1. 不做诊断。
2. 不承诺治疗。
3. 不继续普通训练卡推荐。
4. 不默认暴露给研究者。
5. 必须进入人工复核或安全提示。
```

---

## 14. 研究导出流程

```mermaid
flowchart TD
    A[研究者/管理员进入 /export] --> B[GET /api/admin/export]
    B --> C[require_role admin/researcher]
    C --> D{actor role}
    D -->|admin| E[可访问高级导出]
    D -->|researcher| F[限制为脱敏研究导出]
    E --> G[选择 export_type]
    F --> G
    G --> H[过滤 export_allowed=1]
    H --> I[research_revoked_filter 排除撤回授权]
    I --> J{是否包含高风险/需复核}
    J -->|是且未确认| K[返回 409 需 confirm_high_risk]
    J -->|否或已确认| L[脱敏转换 rows]
    L --> M[write_audit_log]
    M --> N[返回 CSV]
```

导出类型：

```text
goals
diaries
feedback
checkins
assessments
profile
student_profiles
records
student_followups
sandplay
parent_assessments
raw_wide
long
codebook
reports
supervision
cards
```

导出原则：

```text
1. 默认脱敏。
2. 默认不导出真实身份和联系方式。
3. 自由文本默认转为长度、摘要或结构化字段。
4. 研究撤回用户会被过滤。
5. 高风险或需复核数据需要二次确认。
6. 所有导出写 audit_logs。
```

---

## 15. Web 前端路由结构图

```mermaid
flowchart TD
    A[apps/web/src/main.tsx] --> B{pathname}
    B -->|/| C[LandingPage]
    B -->|/login| D[LoginPage]
    B -->|/register| E[RegisterPage]
    B -->|/privacy| F[PrivacyCenterPage]
    B -->|/assessment| G[ParentAssessmentPage]
    B -->|/student| H[StudentEntryPage]
    B -->|/student/assessment| I[StudentAssessmentPage]
    B -->|/student/report/:id| J[StudentReportPage]
    B -->|/dashboard| K[ResearchDashboard]
    B -->|/profiles| L[ProfilesManagement]
    B -->|/reviews| M[ReviewManagement]
    B -->|/supervision| N[SupervisionManagement]
    B -->|/export| O[ExportManagement]
    B -->|/family| P[FamilyBindPage]
    B -->|/integration-test| Q[IntegrationSmokeTest]
```

---

## 16. Web 后台菜单角色过滤图

```mermaid
flowchart TD
    A[读取 getStoredAuthUser] --> B{是否有用户角色}
    B -->|否| C[显示公共链接: 隐私中心/登录/注册]
    B -->|有| D[读取 allAdminLinks]
    D --> E[根据 roles 过滤]
    E --> F{role}
    F -->|parent/student| G[家庭绑定等用户入口]
    F -->|researcher| H[总览/记录/反馈/训练卡/周报/画像/导出]
    F -->|supervisor| I[总览/学生画像/人工复核/督导工作台]
    F -->|admin| J[完整后台与联调测试]
```

提示：

```text
前端菜单过滤只改善体验，不能代替后端权限校验。
```

---

## 17. 小程序 API 调用流程

```mermaid
flowchart TD
    A[小程序页面] --> B[createSafeHomeApi]
    B --> C[getAnonymousUserId]
    C --> D[withDefaultUser 注入 user_id]
    B --> E[wx.cloud.callContainer]
    E --> F[CloudBase 云托管]
    F --> G[Flask 后端 API]
    G --> H{HTTP 状态 / payload.ok}
    H -->|成功| I[返回 data]
    H -->|失败| J[返回 code/message/debug]
    J --> K[页面显示错误或联调排查]
```

说明：

```text
小程序端会在错误中保留 env、service、path、method 等 debug 信息，用于排查云环境、服务名、路径和后端状态。
```

---

## 18. content 内容库加载与使用流程

```mermaid
flowchart TD
    A[content 内容文件] --> B{文件类型}
    B -->|training_cards.json| C[sync_training_cards]
    B -->|feedback_rules.json| D[feedback_service]
    B -->|risk_keywords.json| E[risk_service]
    B -->|student_profile_model.json| F[student_profile_model_service]
    C --> G[training_cards 表]
    D --> H[支持性反馈]
    E --> I[风险预检]
    F --> J[学生画像]
    G --> K[前端训练卡展示]
    H --> K
    I --> L[风险复核]
    J --> M[学生报告/后台画像]
```

---

## 19. records 统一研究摘要流程

```mermaid
flowchart TD
    A[业务模块写入原始表] --> B{模块类型}
    B -->|学生画像| C[student_profiles]
    B -->|追踪反馈| D[student_profile_followups]
    B -->|沙盘表达| E[student_sandplay_entries]
    B -->|其他模块| F[原始业务表]
    C --> G[records module_type=student_profile]
    D --> H[records module_type=profile_followup]
    E --> I[records module_type=sandplay_task]
    F --> J[后续可扩展 records 摘要]
    G --> K[研究导出 records]
    H --> K
    I --> K
    J --> K
```

说明：

```text
records 是研究摘要，不替代原始业务表。它用于把不同模块的数据统一到一个可查询、可导出的研究视角。
```

---

## 20. 审计日志流转图

```mermaid
flowchart TD
    A[敏感操作] --> B{操作类型}
    B -->|导出数据| C[export_xxx]
    B -->|查看画像| D[view_profile]
    B -->|家庭绑定| E[family_create_bind_code / family_bind_student / family_unbind]
    B -->|隐私撤回| F[privacy_revoke_consent]
    B -->|删除请求| G[privacy_delete_request]
    B -->|人工复核| H[review/update action]
    C --> I[audit_logs]
    D --> I
    E --> I
    F --> I
    G --> I
    H --> I
    I --> J[后台审计与问题追踪]
```

说明：

```text
审计日志用于回答“谁在什么时候做了什么敏感操作”，尤其适用于导出、风险复核、隐私请求和家庭绑定。
```

---

## 21. 权限校验流程

```mermaid
flowchart TD
    A[请求进入后端接口] --> B{是否有 X-Admin-Token}
    B -->|有| C[require_admin_token]
    C -->|通过| D[legacy admin actor]
    C -->|失败| E[401]
    B -->|无| F{是否有 Authorization Bearer}
    F -->|无| G[匿名/拒绝，取决于接口]
    F -->|有| H[verify_auth_token]
    H -->|过期/无效| I[401]
    H -->|有效| J[读取 users]
    J --> K{status active?}
    K -->|否| L[403]
    K -->|是| M[actor role]
    M --> N{require_role 是否满足}
    N -->|满足| O[继续业务逻辑]
    N -->|不满足| P[403]
```

---

## 22. 试点研究数据闭环图

```mermaid
flowchart TD
    A[家长使用系统] --> B[日记/反馈/训练卡/打卡]
    C[学生使用系统] --> D[测评/画像/追踪/沙盘]
    E[家庭绑定] --> F[family_links]
    B --> G[records / 原始业务表]
    D --> G
    F --> G
    G --> H[脱敏导出]
    H --> I[raw_wide / long / codebook / records]
    I --> J[统计分析]
    I --> K[质性访谈线索]
    J --> L[试点报告 / 论文材料]
    K --> L
```

---

## 23. 真实试点运行闭环图

```mermaid
flowchart TD
    A[试点开始] --> B[创建测试/正式账号]
    B --> C[家长完成目标和记录]
    C --> D[系统生成反馈和训练卡]
    D --> E[家长打卡]
    B --> F[学生完成画像测评]
    F --> G[学生查看阶段性报告]
    C --> H{是否触发风险}
    F --> H
    H -->|否| I[继续普通流程]
    H -->|是| J[进入人工复核]
    J --> K[督导处理]
    E --> L[周报]
    G --> L
    L --> M[研究后台查看]
    M --> N[脱敏导出]
    N --> O[试点数据分析]
    O --> P[向导师汇报]
```

---

## 24. 向导师讲解的一页图

```mermaid
flowchart LR
    A[家长情绪记录] --> B[非诊断支持性反馈]
    B --> C[训练卡练习]
    C --> D[打卡与周报]
    E[学生支持性测评] --> F[阶段性压力反应画像]
    F --> G[推荐任务与复核]
    D --> H[研究后台]
    G --> H
    H --> I[脱敏导出与试点分析]
    J[高风险文本] --> K[人工复核/督导]
    B -.边界.-> J
    F -.边界.-> J
```

适合口头解释：

```text
系统主线是“记录—反馈—练习—复盘”，学生端提供阶段性画像作为补充线索，高风险内容不走普通反馈，而进入人工复核。研究后台只看脱敏数据，用于试点评估和后续研究。
```
