<textarea
  class="compact-textarea"
  maxlength="{{maxlength}}"
  value="{{value}}"
  aria-label="{{ariaLabel}}"
  placeholder="{{placeholder}}"
  placeholder-class="compact-placeholder"
  bindinput="handleInput"
></textarea>
