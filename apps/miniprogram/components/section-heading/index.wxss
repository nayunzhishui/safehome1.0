:host {
  display: block;
}

.section-heading {
  min-height: 104rpx;
  display: flex;
  align-items: center;
  border-bottom: 1rpx solid var(--safe-border);
  color: var(--safe-title);
  font-size: 40rpx;
  font-weight: 700;
  line-height: 1.4;
}
