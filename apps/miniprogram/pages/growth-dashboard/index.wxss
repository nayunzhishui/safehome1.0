.growth-dashboard-page {
  display: grid;
  gap: 48rpx;
  padding-top: 48rpx;
  padding-bottom: calc(64rpx + env(safe-area-inset-bottom));
  background: var(--ui2-canvas);
}

.growth-kicker,
.growth-title,
.section-eyebrow,
.next-step-copy,
.section-title,
.section-copy,
.rail-count,
.rail-label,
.assessment-title,
.assessment-value,
.assessment-note,
.timeline-title,
.timeline-summary,
.timeline-date,
.relationship-title,
.fact-value,
.fact-label {
  display: block;
}

.growth-hero {
  padding: 0;
  text-align: center;
}

.growth-kicker {
  color: var(--safe-primary-deep);
  font-size: 26rpx;
  font-weight: 800;
  letter-spacing: 2rpx;
}

.growth-title {
  margin-top: 0;
  color: var(--safe-title);
  font-size: 44rpx;
  font-weight: 700;
  line-height: 1.6;
  text-align: center;
}

.meaning-boundary {
  padding: 0;
  background: transparent;
  color: var(--safe-text);
  font-size: 30rpx;
  line-height: 1.55;
  text-align: center;
}

.growth-content,
.section-stack {
  display: grid;
  gap: 48rpx;
}

.growth-rail {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 16rpx;
  border-bottom: 1rpx solid var(--safe-border);
}

.rail-tab {
  min-width: 0;
  min-height: 112rpx;
  padding: 18rpx 6rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 6rpx;
  border-bottom: 5rpx solid transparent;
  color: var(--safe-text);
}

.rail-tab::after {
  border: 0;
}

.rail-tab--active {
  border-bottom-color: var(--safe-primary);
  color: var(--safe-primary-deep);
}

.rail-count {
  color: inherit;
  font-size: 30rpx;
  font-weight: 700;
  font-variant-numeric: tabular-nums;
}

.rail-label {
  color: inherit;
  font-size: 26rpx;
  font-weight: 800;
  line-height: 1.35;
  text-align: center;
}

.next-step-card,
.dashboard-section,
.relationship-card,
.feedback-summary-card {
  padding-bottom: 32rpx;
  border-bottom: 1rpx solid var(--safe-border);
}

.section-eyebrow {
  color: var(--safe-primary-deep);
  font-size: 26rpx;
  font-weight: 800;
}

.next-step-copy {
  margin-top: 10rpx;
  color: var(--safe-title);
  font-size: 30rpx;
  line-height: 1.65;
}

.action-row {
  display: flex;
  gap: 16rpx;
  margin-top: 22rpx;
}

.section-action {
  min-height: 96rpx;
  padding: 16rpx 24rpx;
  display: flex;
  flex: 1;
  align-items: center;
  justify-content: center;
  border-radius: var(--safe-radius-md);
  background: var(--safe-primary);
  color: #fff;
  font-size: 30rpx;
  font-weight: 800;
}

.section-action::after {
  border: 0;
}

.section-action--quiet {
  border: 1rpx solid var(--safe-primary);
  background: transparent;
  color: var(--safe-primary-deep);
}

.section-heading,
.feedback-summary-card {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 20rpx;
}

.section-title {
  color: var(--safe-title);
  font-size: 32rpx;
  font-weight: 700;
  line-height: 1.4;
}

.section-copy {
  margin-top: 8rpx;
  color: var(--safe-muted);
  font-size: 26rpx;
  line-height: 1.55;
}

.thermometer-list,
.assessment-list,
.timeline-list {
  display: grid;
  gap: 20rpx;
  margin-top: 24rpx;
}

.thermometer-row {
  display: grid;
  gap: 12rpx;
}

.thermometer-meta {
  display: flex;
  justify-content: space-between;
  gap: 16rpx;
  color: var(--safe-text);
  font-size: 26rpx;
}

.bar-track {
  height: 12rpx;
  overflow: hidden;
  border-radius: var(--safe-radius-pill);
  background: var(--safe-primary-soft);
}

.bar-fill {
  height: 100%;
  border-radius: inherit;
  background: var(--safe-primary);
}

.assessment-group {
  padding: 24rpx 0;
  border-top: 1rpx solid var(--safe-border);
}

.assessment-title {
  color: var(--safe-title);
  font-size: 30rpx;
  font-weight: 800;
  line-height: 1.5;
}

.assessment-values {
  display: grid;
  gap: 8rpx;
  margin-top: 14rpx;
}

.assessment-value,
.assessment-note {
  color: var(--safe-text);
  font-size: 26rpx;
  line-height: 1.55;
}

.assessment-note {
  margin-top: 12rpx;
  color: var(--safe-muted);
}

.relationship-title {
  margin-top: 8rpx;
  color: var(--safe-title);
  font-size: 30rpx;
  font-weight: 700;
  line-height: 1.5;
}

.relationship-facts {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 12rpx;
  margin-top: 24rpx;
}

.fact-cell {
  min-width: 0;
  display: flex;
  gap: 8rpx;
  align-items: baseline;
  padding: 0;
  text-align: left;
}

.fact-value {
  color: var(--safe-primary-deep);
  font-size: 26rpx;
  font-weight: 700;
}

.fact-label {
  margin-top: 6rpx;
  color: var(--safe-muted);
  font-size: 26rpx;
}

.relationship-action,
.feedback-action {
  width: 100%;
  margin-top: 22rpx;
}

.timeline-item {
  position: relative;
  display: grid;
  grid-template-columns: 32rpx minmax(0, 1fr);
  gap: 16rpx;
}

.timeline-dot {
  position: relative;
  width: 18rpx;
  height: 18rpx;
  margin: 12rpx 0 0 6rpx;
  border-radius: 50%;
  background: var(--safe-primary);
}

.timeline-dot::before,
.timeline-dot::after {
  position: absolute;
  left: 7rpx;
  width: 4rpx;
  height: 18rpx;
  border-radius: 4rpx;
  background: var(--safe-primary);
  content: "";
}

.timeline-dot::before {
  bottom: 22rpx;
}

.timeline-dot::after {
  top: 22rpx;
}

.timeline-dot--relationship {
  background: var(--safe-blue);
}

.timeline-dot--feedback {
  background: var(--safe-orange);
}

.timeline-body {
  min-width: 0;
  padding-bottom: 20rpx;
}

.timeline-head {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 14rpx;
}

.timeline-title {
  color: var(--safe-title);
  font-size: 30rpx;
  font-weight: 800;
  line-height: 1.5;
}

.timeline-type,
.timeline-summary,
.timeline-date {
  color: var(--safe-text);
  font-size: 26rpx;
  line-height: 1.55;
}

.timeline-type {
  flex: none;
  color: var(--safe-primary-deep);
}

.timeline-summary,
.timeline-date {
  margin-top: 6rpx;
}

.timeline-date {
  color: var(--safe-muted);
}

.boundary-card {
  padding: 0;
  border: 0;
  color: var(--safe-text);
  font-size: 26rpx;
  line-height: 1.65;
}

@media (max-width: 360px) {
  .growth-rail {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }

  .action-row {
    flex-direction: column;
  }
}

.growth-dashboard-page { --ui2-growth-direction: row; --ui2-growth-justify: space-between; --ui2-growth-padding: 24rpx; --ui2-growth-height: 96rpx; --ui2-growth-gap: 16rpx; --ui2-growth-line: 48rpx; }
.growth-rail { border: 0; }
.meaning-boundary { color: var(--ui2-muted); text-align: left; }
.next-step-card { padding: 40rpx; border: 0; border-radius: 24rpx; background: var(--ui2-soft); }
.next-step-copy, .section-copy, .assessment-note, .assessment-value, .boundary-card { font-size: 30rpx; line-height: 1.6; }
.section-eyebrow { font-size: 30rpx; }
.section-action { line-height: 1.5; padding: 20rpx 16rpx; }
.thermometer-meta { flex-wrap: wrap; line-height: 1.6; }
.relationship-facts { display: flex; flex-wrap: wrap; gap: 16rpx 24rpx; }
.fact-label { margin-top: 0; }
.section-heading, .feedback-summary-card { flex-wrap: wrap; }
