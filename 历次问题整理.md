# 历次问题整理

本文件用于沉淀 safehome1.0 项目开发、联调和上线内测过程中遇到的关键问题，记录原因、过程、结果和可复用方法。

项目：safehome1.0 / 安心陪伴 / ReadFeedback

## 问题 1：CloudBase INVALID_HOST

日期：2026-05-24

### 一、问题现象

小程序端接入腾讯云 CloudBase 云托管后，页面请求后端接口时持续出现：

```text
请求失败（404）：Invalid host
error-code/service/INVALID_HOST
```

出现位置包括：

```text
最小联调测试页
填写工作表页面
测一测相关页面
```

这个错误不是 Flask 后端业务接口返回的普通 404，而是 CloudBase 云托管网关层返回的错误，表示网关无法根据当前请求定位到可访问的云托管服务。

### 二、原因梳理

本次问题经历了几个阶段：

#### 1. 小程序未初始化云开发

早期错误包括：

```text
Cloud API isn't enabled, please call wx.cloud.init first
```

原因是小程序端没有在启动时调用 `wx.cloud.init`，导致 `wx.cloud.callContainer` 无法正常使用。

处理方式是在小程序入口初始化 CloudBase 环境：

```text
env: prod-d3gl35otiaa7c8d24
```

#### 2. callContainer 未显式传入环境 ID

随后出现：

```text
cloud.callContainer:fail envId must be provided
```

原因是 `wx.cloud.callContainer` 调用中没有明确传入 CloudBase 环境 ID。

处理方式是在统一 API 封装中加入：

```text
config.env: prod-d3gl35otiaa7c8d24
```

#### 3. X-WX-SERVICE 使用了版本名

随后出现：

```text
请求失败（404）：Invalid host
error-code/service/INVALID_HOST
```

最初判断出一个明确问题：小程序端将云托管版本名写进了 `X-WX-SERVICE`：

```text
flask-gh3l-003
```

CloudBase 要求 `X-WX-SERVICE` 填写的是云托管服务名称，不是版本名称。

处理方式是将默认云托管服务名改为：

```text
flask-gh3l
```

#### 4. 继续 INVALID_HOST 时定位到 CloudBase 网关层

服务名改为 `flask-gh3l` 并重新上传小程序后，仍然出现 `INVALID_HOST`。

此时已经可以排除：

```text
小程序业务页面逻辑
Flask 具体接口 path
数据库结构
后端业务代码
```

原因范围缩小到 CloudBase 网关层，包括：

```text
服务名与 CloudBase 实际可访问服务不一致
服务不在当前环境下
服务没有运行中的版本
服务状态异常
当前小程序访问的环境和服务所在环境不一致
```

为避免继续盲目排查页面业务接口，联调页增加了 `/healthz` 入口诊断。

### 三、处理过程

#### 1. 统一小程序后端调用方式

将小程序端后端请求统一改为：

```text
wx.cloud.callContainer
```

不再使用：

```text
wx.request
tcloudbase.com 默认公网域名
```

#### 2. 固定 CloudBase 环境 ID

小程序启动时使用：

```text
wx.cloud.init env: prod-d3gl35otiaa7c8d24
```

每次调用云托管时使用：

```text
wx.cloud.callContainer config.env: prod-d3gl35otiaa7c8d24
```

#### 3. 修正云托管服务名

将：

```text
DEFAULT_CONTAINER_SERVICE = "flask-gh3l-003"
```

改为：

```text
DEFAULT_CONTAINER_SERVICE = "flask-gh3l"
```

使请求头实际发送：

```text
X-WX-SERVICE: flask-gh3l
```

#### 4. 增强错误显示

将原本笼统的：

```text
请求失败
```

增强为尽量显示：

```text
请求失败（状态码）：后端或网关错误信息
```

这样可以区分：

```text
404 Invalid host：CloudBase 网关找不到服务
404 Not Found：后端路由不存在
500：后端运行异常
```

#### 5. 增加 /healthz 入口诊断

在最小联调测试页增加运行时配置展示：

```text
env: prod-d3gl35otiaa7c8d24
service: flask-gh3l
healthz: /healthz
```

联调流程改为先请求：

```text
GET /healthz
```

只有 `/healthz` 成功后，才继续：

```text
创建情绪事件记录 -> 生成即时反馈 -> 获取训练卡推荐
```

这样可以判断请求是否已经进入云托管服务。

### 四、最终结果

问题已解决。

当前小程序端确认使用：

```text
CloudBase 环境 ID：prod-d3gl35otiaa7c8d24
云托管服务名：flask-gh3l
调用方式：wx.cloud.callContainer
入口诊断 path：/healthz
```

当前小程序端不再使用：

```text
wx.request
tcloudbase.com 默认公网域名
flask-gh3l-003 版本名作为 X-WX-SERVICE
```

本轮排查没有修改：

```text
Flask 后端业务逻辑
数据库结构
既有 API path
正式登录注册
AI 自由问答
```

### 五、可复用排查方法

以后遇到 CloudBase 云托管请求失败时，优先按以下顺序排查：

#### 1. 先区分错误发生层级

如果是：

```text
Cloud API isn't enabled
envId must be provided
INVALID_HOST
```

优先排查 CloudBase 初始化、环境 ID、服务名、云托管服务状态。

如果是：

```text
请求失败（404）：Not Found
```

再排查 Flask 路由是否存在。

如果是：

```text
请求失败（500）
```

再看 CloudBase 云托管日志和 Flask 后端异常。

#### 2. 用 /healthz 做最小入口验证

先验证：

```text
GET /healthz
```

如果 `/healthz` 都无法访问，不要继续排查业务页面和业务接口。

#### 3. 检查 X-WX-SERVICE

`X-WX-SERVICE` 必须填写 CloudBase 云托管服务名称。

不要填写：

```text
版本名
镜像名
应用展示名
部署包名称
```

#### 4. 检查环境一致性

必须确认以下三者在同一个 CloudBase 环境下：

```text
小程序 AppID
CloudBase 环境 ID
云托管服务
```

本项目当前环境 ID：

```text
prod-d3gl35otiaa7c8d24
```

#### 5. 检查服务状态

在 CloudBase 控制台确认：

```text
云托管服务存在
服务名称正确
服务有运行中的版本
版本部署成功
服务部署在正确环境
```

### 六、相关文件

本次涉及的主要文件：

```text
apps/miniprogram/app.js
apps/miniprogram/services/api.js
apps/miniprogram/pages/integration-test/index.js
apps/miniprogram/pages/integration-test/index.wxml
apps/miniprogram/pages/integration-test/index.wxss
docs/开发日志.md
docs/当前进度交接.md
docs/开发说明.md
```

其中 `pages/integration-test/index` 保留为后续最小联调和云托管入口诊断页面。
